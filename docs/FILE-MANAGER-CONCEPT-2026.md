# SLAED CMS 6.3 — File Manager / File Browser
## План реализации

**Статус:** решения закрыты, макеты утверждены, этапы 0 и 0.5 закрыты, следующий — этап 1  
**Объём одной сессии:** шапка, свой этап и разделы из его карты чтения — около 500–600 строк вместо всего документа  
**Целевая версия:** SLAED CMS 6.3 Phoenix  
**Принцип:** развить существующую подсистему `uploads`, не внедряя сторонний файловый менеджер.

Это не обсуждение. Каждое решение ниже принято и обосновано там, где названо;
возвращаться к ним — не работа, а переделка. Читать до конца перед первой правкой.

---

## Как работать над этим планом

**Один этап — одна сессия — один готовый к коммиту набор.** Этапы идут по
порядку §26 и каждый заканчивается работающим и проверенным куском. Не начинать
следующий, не закрыв предыдущий: порядок не произвольный, он объяснён в §26 у
каждого этапа.

Коммит делается **только по явной команде владельца** — `.rules/git.md` это
запрещает без неё, и план исключения не вводит. «Один коммит на этап» описывает
границу набора изменений, а не право закоммитить его самостоятельно. Работа
этапа заканчивается отчётом и предложением коммита с готовым сообщением.

**Перед первой правкой этапа прочитать:**

```text
.agents/SYSTEM-PREAMBLE.md            роль и рабочий процесс
.agents/skills/slaed/INDEX.md         выбрать рабочий скилл под задачу
.rules/*                              строгие правила, всегда обязательны
этот файл: шапку и свой этап          у каждого этапа есть карта чтения
разделы, названные в карте этапа      только их, а не документ целиком
```

**Документ целиком читать не нужно.** У каждого этапа в §26 стоит карта чтения:
какие разделы плана, какие файлы кода и какие тесты ему нужны. Разделы, в карту
не попавшие, к задаче отношения не имеют. Оглавление — ниже.

**Макеты открывать в браузере, а не читать как текст.** Это восемьсот строк HTML
каждый, и они написаны, чтобы на них смотреть:

```text
demo/filemanager-v2-rail.html      окно редактора
demo/filemanager-admin-2026.html   админка
```

В исходник заглядывать точечно — за именем класса или куском разметки. Макет —
рабочий образец: классы, разметка и поведение в нём такие, какими должны стать в
коде. Расхождение макета и кода — дефект кода, пока владелец не решил иначе.

**Оглавление:**

```text
§1    Цель
§2    Что уже есть в SLAED 6.3
§3    Основные архитектурные решения
§4    Компоненты
§5    Ответственность `FileManager`
§6    File context
§7    Нормализованная модель файла
§8    UI — «Каталог файлов»
§9    UI — ToastUI Compact Mode
§9.1  Три механизма, общие для обоих экранов
§10   UI — «Системные файлы»
§11   Определение типа системного файла
§12   System Files security boundary
§13   Безопасное сохранение исходников
§14   Capabilities
§15   Existing editor upload rules remain authoritative
§16   Ownership upload-файлов — сделано
§17   HTTP / routing
§18   HTMX / JavaScript
§19   Search
§20   Preview
§21   Работа с архивами
§22   Что делать с текущими под-вкладками uploads
§23   Что делать с `admin/modules/editor.php`
§24   Logging
§25   Производительность
§26   Этапы реализации
§27   Тестирование
§28   Acceptance criteria
§29   Out of scope для первой версии
§30   Итоговая архитектура
§31   Состояния процесса и ошибок
§32   Выравнивание тем — сделано
§33   Клавиатура и доступность
```

**Что считается закрытым этапом:**

```text
php -l на каждом изменённом PHP-файле
PHPUnit-тест на этапы с логикой: пути, права, дескриптор, конфликт версий
браузерная проверка на этапы с интерфейсом, со скриншотами всех состояний
storage/logs/error_php.log, error_sql.log, error_site.log — без новых записей
отчёт по .rules/report.md
```

Тест соразмерен риску этапа: правило путей и версии файла проверяются
модульно, раскладка и веер — глазами в браузере.

**Что кусается на каждом этапе:**

- Границу шаблона легко пробить незаметно. PHP отдаёт данные, флаги и адреса;
  имена CSS-классов и разметка живут в шаблонах. Это самое частое нарушение в
  этом проекте, и файловый браузер с его строками и плитками — первый кандидат.
- Тема одна на всех. Если компонента не хватает — он добавляется в тему под тем
  же именем, что во второй теме (§32), а не переопределяется в модуле.
- Языковые константы правятся сразу во всех шести локалях, иначе интерфейс
  разъезжается по языкам (`.rules/constants.md`).
- Изменяющее действие идёт только POST-ом с токеном. Ни одна новая ссылка не
  должна ничего менять (§17).

**Что уже сделано и на что можно опираться:**

```text
docs/EDITOR-UPLOADS-2026.md   закрыт: настройки решают, поле решает, гость изолирован
core/system.php               getEditorFileOwner()  — токен владельца на сессию гостя
core/system.php               getEditorFileJson()   — список только своих файлов, лимит guestfiles
core/system.php               checkEditorUploadAccess() — одно решение о праве загрузки
core/helpers.php              getEditorRoomData()   — ширина поля решает, можно ли embed
```

Гостевая изоляция и `guestfiles` работают. Прежний блокер «guest owner = 0»
снят, и каталог гостя показывает файлы его сессии — это больше не заглушка.

**Утверждённые макеты:**

```text
demo/filemanager-v2-rail.html      ToastUI, compact mode, боковой рельс
demo/filemanager-admin-2026.html   Админка, full mode, каталог и системные файлы
```

Оба построены на одной оболочке: навигация слева, работа в центре, свойства
справа, постоянная строка состояния внизу. Отличаются правами и корнем, а не
языком интерфейса.

**Три механизма общие для обоих экранов** и описаны один раз, в §9:

```text
sl-dial     веер действий на объекте
sl-modal    галерея предпросмотра
чекбоксы    отметки и панель массовых действий
```

---

## 1. Цель

Создать единый файловый слой SLAED, который используется в трёх сценариях:

1. **Администратор → Загрузки → Каталог файлов** — полноценный файловый менеджер для `uploads/*`.
2. **ToastUI → Файловый менеджер** — компактный браузер разрешённых файлов модуля для загрузки, выбора и вставки в материал.
3. **Администратор → Загрузки → Системные файлы** — отдельная вкладка для просмотра и редактирования файлов SLAED внутри `BASE_DIR`.

Главная схема:

```text
                         FileManager
                             │
              ┌──────────────┼──────────────┐
              │              │              │
              ▼              ▼              ▼
        Uploads Admin     ToastUI       System Files
          FULL MODE      COMPACT MODE      FULL MODE
              │              │              │
         uploads/*       uploads/mod      BASE_DIR
```

---

## 2. Что уже есть в SLAED 6.3

### 2.1. `admin/modules/uploads.php`

Модуль уже является естественным владельцем будущего File Manager:

- выбор upload-директории;
- загрузка файла с компьютера;
- загрузка файла по URL;
- правила расширений;
- лимиты размеров и квот;
- ограничения изображений;
- количество файлов;
- основная директория;
- дополнительная директория `thumb`;
- файловые списки через HTMX;
- настройка шаблонов отображения файлов.

Поэтому новый отдельный `admin/modules/filemanager.php` **не создаётся**.

### 2.2. `core/classes/upload.php`

Класс `Upload` уже отвечает за безопасную публикацию новых файлов:

- проверка extension;
- проверка MIME;
- проверка изображений;
- quota;
- maxbytes;
- maxwidth/maxheight;
- collision-free naming;
- временный `.part`;
- lock;
- atomic publication;
- remote URL security;
- удаление файлов, созданных Upload service.

`Upload` остаётся специализированным сервисом загрузки и **не превращается в File Manager**.

### 2.3. ToastUI

У ToastUI уже реализована существенная часть интеграции:

```text
editorUpload
editorFiles
```

Редактор уже получает:

- module upload rule;
- CSRF token;
- extensions;
- maxquota;
- maxbytes;
- maxwidth;
- maxheight;
- maxfiles;
- список сохранённых файлов;
- drag & drop;
- upload;
- attachment/embed modes;
- собственное окно «Файловый менеджер».

Это сохраняется и развивается.

### 2.4. `Editor::getCode()`

Для системных текстовых файлов уже существует CodeMirror 6 через:

```php
Editor::getCode(...)
```

Отдельный code editor для File Manager не нужен.

---

## 3. Основные архитектурные решения

### Решение 1. Хозяин File Manager — `uploads`

UI интегрируется в:

```text
admin/modules/uploads.php
```

Главные вкладки:

```text
[ Каталог файлов ]
[ Системные файлы ]
[ Шаблоны ]
[ Конфигурация ]
[ Справка ]
```

### Решение 2. «Системные файлы» — отдельная вкладка

Системная файловая область **не добавляется** в selector:

```text
Директория: uploads/all
```

Upload files и system files имеют разные права, разные риски и разные политики.

### Решение 3. Один filesystem engine

Создаётся общий backend:

```text
FileManager
```

Он работает с разными контекстами:

```text
uploads
system
editor
```

### Решение 4. `Upload` и `FileManager` разделены

```text
FileManager
└── путь, имя, замок, чтение и управление существующим filesystem
    ничего не знает про Upload

Upload
└── принимает и безопасно публикует новые upload-файлы
    берёт у FileManager примитивы: имя, замок, канонический путь

Adapter (маршрут)
└── решает, чем является запрос, и зовёт нужный из двух
```

**Зависимость односторонняя: `Upload` → `FileManager`, и никогда обратно.**
Загрузка файла — это по-прежнему `Upload`, но зовёт его маршрут, а не файловый
слой. `FileManager` не содержит ни одного вызова `Upload`: иначе классы
замкнутся друг на друга, а автозагрузки в проекте нет и порядок подключения
станет ловушкой.

**Подключение.** Автозагрузчика нет: `getUploadService()`
(`core/system.php:4290`) подключает `upload.php` по первому требованию, а тесты
и инструменты подключают его напрямую — например `tests/Support/upload_probe.php:13`.
Поэтому `core/classes/upload.php` в своём начале сам подключает файловый слой:

```php
if (!class_exists('FileManager')) require_once __DIR__.'/filemanager.php';
```

Тот же приём уже применён к `Parser` в `tests/Support/*_probe.php`. Любой, кто
подключил `Upload`, получает `FileManager` вместе с ним, и ни один существующий
вызывающий не ломается.

### Решение 5. Общий backend, но не один универсальный HTTP endpoint

Админка использует HTMX/HTML. ToastUI использует JSON.

Объединяется **domain layer и модель файлов**, а не транспорт:

```text
                     FileManager
                         │
            ┌────────────┴────────────┐
            │                         │
        Admin adapter            Editor adapter
        HTMX / HTML                 JSON
```

### Решение 6. Никаких сторонних SPA/file-manager frameworks

Не добавлять:

- Vue;
- React;
- CodeIgniter;
- jQuery;
- jQuery UI;
- elFinder backend;
- eXtplorer backend;
- Tiny File Manager runtime.

UI:

```text
Bootstrap 5
Bootstrap Icons
HTMX
Vanilla JS
```

---

## 4. Компоненты

```text
core/classes/filemanager.php          новый класс, единственный новый файл ядра
admin/modules/uploads.php             хозяин интерфейса, новый модуль не создаётся

templates/admin/partials/
├── file-browser.html                 оболочка: панель, три колонки, подвал
├── file-browser-tree.html            дерево
├── file-browser-list.html            список и плитки
└── file-browser-editor.html          редактор исходника

templates/admin/fragments/
├── file-browser-row.html             строка списка
├── file-browser-tile.html            плитка
└── file-browser-props.html           свойства объекта
```

**Новых файлов JS и CSS не заводится — но у двух экранов разные дома.**

Админский браузер живёт в административной теме:

```text
templates/admin/assets/js/admin-ui.js       поведение
templates/admin/assets/css/theme.css        стили
```

Окно редактора живёт в плагине и в теме сайта, и трогать надо именно их:

```text
plugins/editors/toastui/assets/editor-upload.js       поведение окна
plugins/editors/toastui/driver.php                    данные и ключи шаблона
templates/<theme>/partials/editor-toastui-files.html  разметка окна
templates/<theme>/assets/editors/toastui/skin.css     стили окна, в каждой теме
```

Тем две — `lite` и `admin`, — и `skin.css` правится в обеих: редактор
открывается и на сайте, и в административной форме. Свой `file-browser.js` не
заводится ни там, ни там: он означал бы второе подключение и растаскивание общих
компонентов по файлам.

Крошки и панель инструментов собственных фрагментов не получают: панель — это
`sl-searchbox` с готовыми `sl-but-mini`, крошки — цепочка ссылок. Отдельный
фрагмент на три элемента дороже, чем он экономит.

ToastUI сохраняет собственный wrapper:

```text
templates/<theme>/partials/editor-toastui-files.html
```

но получает данные через тот же FileManager domain layer.

---

## 5. Ответственность `FileManager`

Имена методов подчиняются `.rules/global.md`: camelCase, глагол из разрешённого
набора плюс существительное, 6–24 символа. Тот же закон, по которому названы
`Upload::addUploadedFile()` и `Upload::deleteStoredFile()`.

Минимальный публичный контракт:

```text
getFileList()        перечислить директорию
getFileData()        один объект как дескриптор §7
getFileBody()        прочитать содержимое текстового файла
setFileBody()        записать содержимое, с проверкой версии §13
addFileEntry()       создать пустой файл
addDirectory()       создать директорию
deleteFileEntry()    удалить файл или директорию
updateFileName()     переименовать
addFileCopy()        скопировать
updateFilePath()     переместить
addFileArchive()     упаковать существующий файл в ZIP
```

Дополнительно:

```text
getFileOwner()       владелец из managed-имени, единственный разбор в проекте
getCapabilities()    что разрешено в этом контексте, §14
checkFileAccess()    допустима ли операция над этим путём, §12
isFileEditable()     открывается ли файл в редакторе исходников
getCodeLanguage()    режим подсветки для CodeMirror, §11
```

`stat`, `read`, `write`, `list`, `create`, `rename`, `copy`, `move` как имена
методов не используются: ни одно из них не проходит правило, а половина ещё и
занята встроенными функциями PHP.

Upload выполняется через:

```text
Upload::addUploadedFile()
Upload::addUploadedFiles()
Upload::addRemoteFile()
```

а не собственной реализацией FileManager.

---

## 6. File context

Каждый запрос FileManager выполняется внутри заранее созданного контекста.

### Upload context

```text
mode     = uploads
root     = UPLOADS_DIR/<module>
access   = upload rules / admin
editor   = false
```

### ToastUI context

```text
mode     = editor
root     = UPLOADS_DIR/<module>
access   = module upload rules + owner
editor   = false
compact  = true
```

### System context

```text
mode     = system
root     = BASE_DIR
access   = isAdmin(true)
editor   = true
```

Клиент никогда не задаёт физический root. Он передаёт только относительный путь внутри уже выбранного context.

---

## 7. Нормализованная модель файла

FileManager должен возвращать единый descriptor:

```php
[
    'name'         => 'picture.webp',
    'path'         => 'news/picture.webp',
    'kind'         => 'image',
    'extension'    => 'webp',
    'size'         => 245760,
    'mtime'        => 1786082400,
    'url'          => 'uploads/news/picture.webp',
    'thumbnail'    => '',
    'width'        => 1280,
    'height'       => 720,
    'managed'      => true,
    'editable'     => false,
    'previewable'  => true,
    'capabilities' => [...],
]
```

Абсолютный серверный путь в дескрипторе **отсутствует**: клиент работает
относительным путём внутри context, и в JSON редактора абсолютного пути нет
никогда.

Исключение одно и оно явное: админский full mode дополнительно получает поле
`realpath` и показывает его в свойствах. Администратор полноправен, и знать
расположение файла ему нужно. Решение о выдаче принимает сервер по context, а не
клиент по запросу.

---

## 8. UI — «Каталог файлов»

Текущий экран сохраняет существующую структуру:

```text
Загрузки
[ Каталог файлов ] [ Системные файлы ] [ Шаблоны ] [ Конфигурация ] [ Справка ]

Директория: [ uploads/all ▼ ]

[ Файловый менеджер ] [ Основная директория ] [ Дополнительная директория ]
```

На первом этапе старые под-вкладки не удаляются.

### Новый «Файловый менеджер»

Утверждённый макет: `demo/filemanager-admin-2026.html`.

```text
┌──────────────────────────────────────────────────────────────────────────┐
│ [Каталог файлов] [Системные файлы] [Шаблоны] [Конфигурации] [Справка]     │
├──────────────────────────────────────────────────────────────────────────┤
│ ← ↑ ⟳ │ uploads / files          🔍 Фильтр    ▤ ▦     [ ⬆ Загрузить ]   │
├──────────────┬─────────────────────────────────────┬─────────────────────┤
│ ДИРЕКТОРИИ   │ Имя           Тип   Размер  Изменён │ СВОЙСТВА            │
│ 📁 uploads   │ 📁 thumb      дир.    —    10.08.26 │ [ предпросмотр ]    │
│ ├ 📁 all     │ 🖼 screen.png PNG  248 KB  сегодня  │ Имя    screen.png   │
│ ├ 📁 files ◀ │ 🖼 logo.webp  WEBP  34 KB  вчера    │ Тип    image · png  │
│ │ └ 📁 thumb │ 📄 manual.pdf PDF  1.2 MB  28.07.26 │ Размер 248 KB       │
│ ├ 📁 news    │ 📦 pack.zip   ZIP  860 KB  21.07.26 │ Адрес  uploads/…    │
│ └ … ещё 11   │                                     │ [Скачать][В архив]  │
├──────────────┴─────────────────────────────────────┴─────────────────────┤
│ ДОСТУПНО: просмотр · предпросмотр · скачивание · загрузка POST ·         │
│           удаление POST · архивирование POST · ✕ переим. · ✕ перенос     │
├──────────────────────────────────────────────────────────────────────────┤
│ 7 объектов · 3.0 MB · выбран screenshot-2026-08-10.png         [1][2][›] │
└──────────────────────────────────────────────────────────────────────────┘
```

Три колонки: дерево — список — свойства. Дерево раскрывается по мере надобности
и никогда не обходит всё хранилище сразу. Свойства собираются из дескриптора §7
и меняются вместе с выделением, отдельного диалога «информация о файле» нет.

Основные функции:

- breadcrumb;
- refresh;
- filter текущей директории;
- list view;
- grid view;
- сортировка;
- preview;
- download;
- upload;
- delete;
- file information в правой колонке;
- pagination/limit.

### Панель инструментов

Навигация, действия над директорией и переключатели вида — круглые `sl-but-mini`
одной группой слева, на плашке `sl-searchbox`:

```text
← ↑ ⟳ │ создать папку · загрузить │ крошки … фильтр · список ▤ ▦
```

Загрузка выделена заливкой как главное действие, остальные — `sl-is-muted`.
Прямоугольных кнопок в панели нет: они занимали место и спорили с крошками.

Действия над файлом живут в веере строки, действия над отмеченными — в
`sl-bulk-bar` (§9.1). В панели инструментов их нет.

### Строка возможностей

Под рабочей областью выводится, что разрешено в текущем контексте. Значения
приходят с сервера (§14), интерфейс их не вычисляет. Запрещённое остаётся на
месте приглушённым, чтобы пользователь видел границу прав, а не искал пропавшую
кнопку.

Метод запроса в подписи не упоминается. «Удаление · POST» ничего не говорит
администратору: POST — способ отправки, а не право, и требование «мутации только
POST» живёт в §17, а не в интерфейсе.

### Абсолютный путь в админском режиме

Текущая под-вкладка «Основная директория» печатает серверный путь в заголовке:

```text
Директория: C:/OSPanel/home/slaed.loc/public/uploads/all
```

Само по себе это не нарушение: администратор полноправен и скрывать от него
расположение файла незачем. Меняется место и роль. Путь перестаёт быть
заголовком раздела и становится справочным полем «Полный путь» в свойствах
выбранного объекта, рядом с относительным адресом.

Правило §7 остаётся в силе для остальных контекстов: в JSON редактора и в любом
интерфейсе не-администратора абсолютный путь не отдаётся никогда. Различие
делает context, а не клиент.

### Администратор полноправен

Администратору доступны все директории и все файлы без изъятий.

Поддерживаемые операции v1:

```text
browse
preview
upload
download
delete
compress
rename
move
mkdir
```

Ограничения снимаются, предупреждения остаются. Имена managed upload-файлов
создаёт Upload service, и они хранят владельца, а на сам файл уже могут
ссылаться опубликованные материалы. Поэтому rename и move не запрещены, но
интерфейс сообщает последствие рядом с кнопкой: ссылки в материалах не
обновляются автоматически, старый адрес перестанет открываться.

Автоматическое обновление ссылок остаётся вне первой версии (§29). Это разные
задачи: право на операцию у администратора есть уже сейчас, а переписывание
содержимого материалов — отдельный проект.

---

## 9. UI — ToastUI Compact Mode

Окно остаётся перетаскиваемым, разворачиваемым и закрываемым, но его содержимое
перестраивается: способы вставки становятся разделами бокового рельса.

Утверждённый макет: `demo/filemanager-v2-rail.html`.

```text
┌────────────────────────────────────────────────────────────────┐
│ ✥  Файловый менеджер                                  ⤢    ×   │
├──────────────────────┬─────────────────────────────────────────┤
│ ДОБАВИТЬ             │ Загрузить на сервер                     │
│ ▸ Загрузить на сервер│ Файл будет загружен и вставлен как       │
│   Ссылка на изобр.   │ изображение.                            │
│   Встроить в текст   │ ┌─────────────────────────────────────┐ │
│ ──────────────────── │ │  Перетащите файлы сюда или нажмите  │ │
│ ХРАНИЛИЩЕ            │ └─────────────────────────────────────┘ │
│   Мои файлы          │ Вставить как: (•)Изображение ( )Файл    │
│                      │ Описание [                           ]  │
│ Занято 12 из 100 MB  │                                         │
│ Модуль: files        │                                         │
├──────────────────────┴─────────────────────────────────────────┤
│ Файл не выбран           [Обновить] [Отмена] [Вставить]        │
└────────────────────────────────────────────────────────────────┘
```

### Три способа вместо трёх радио

Сегодняшние радио `upload` / `attach` / `embed` неравноправны: `attach` — это не
источник файла, а форма вставки уже загруженного. Поэтому рельс делит окно так:

```text
Загрузить на сервер   → приёмник файла + «вставить как» (изображение | файл)
Ссылка на изображение → адрес + описание, доступна всегда
Встроить в текст      → приёмник файла, base64, лимит поля
Мои файлы             → каталог модуля
```

Поля адреса и описания больше не висят над всем содержимым: они принадлежат
своему разделу. Выбор «изображение или миниатюра/файл» живёт внутри загрузки и
внутри каталога, потому что относится к обоим.

### Разделы отражают права

`canupload` и `canlist` — одно серверное решение, `canembed` — свойство поля.
Недоступный раздел не исчезает: он гаснет, получает замок и подменяет описание
причиной. Если закрыт текущий раздел, окно открывается на первом доступном.

```text
автор,  поле с текстом  → все четыре раздела
автор,  поле-анонс      → «Встроить в текст» закрыт: поле не хранит base64
гость,  загрузка есть   → разделы открыты, каталог — файлы его сессии (§16)
гость,  загрузки нет    → живёт только «Ссылка на изображение»
```

### Compact view

Раздел «Мои файлы» по умолчанию компактный.

Показывать:

- preview/icon;
- filename;
- size;
- kind;
- mtime;
- select/insert;
- refresh;
- последние N файлов.

Не показывать:

- дерево всего `uploads`;
- system directories;
- rename;
- move;
- delete;
- raw filesystem paths.

Вместо «Нет информации» пустой каталог объясняет причину: ещё ничего не
загружено, либо фильтр ничего не нашёл.

### Expanded view

Существующая кнопка expand разворачивает окно и одновременно переводит «Мои
файлы» в расширенный браузер:

- filter текущей директории;
- больше файлов;
- list/grid;
- preview;
- pagination;
- file metadata из дескриптора §7.

ToastUI остаётся внутри разрешённого module root. Набор действий над файлом
решает контекст (§14): автору доступны вставка и скачивание, администратору —
ещё архивирование и удаление.

---

## 9.1. Три механизма, общие для обоих экранов

Описаны здесь один раз. Админский полный режим (§8) использует их же.

### Веер действий — `sl-dial`

Действия принадлежат объекту, а не списку. Под каждым файлом раскрывается веер
из системного компонента темы: `sl-dial` + `sl-dial-toggle` + `sl-dial-item`,
каждый пункт — `sl-but-mini` с иконкой и `title`. Раскрытие по наведению даёт
CSS, закрытие чужих вееров — тот же обработчик, что в `plugins/system/slaed.js`.

```text
редактор, автор       вставить изображением · вставить вложением · скачать
редактор, админ       + сжать в ZIP · удалить
админка               открыть/редактировать · предпросмотр · скачать ·
                      переименовать · переместить · сжать · удалить
```

Пункт «вставить изображением» не рисуется для не-картинок, «сжать» — для
директорий. Ширина плашки веера считается темой через `--sl-dial-count` по
`:last-child`, поэтому скрытые правами пункты остаются в разметке и ломают счёт:
переменную задавать явно по числу видимых пунктов.

Удаление помечается `data-sl-confirm` и открывает системную модалку `#sl-confirm`,
а не `window.confirm`.

### Галерея предпросмотра — `sl-modal`

Нажатие на миниатюру открывает `dialog.sl-modal.sl-modal-wide`: снимок,
свойства из дескриптора §7 справа, стрелки, клавиши ← →, счётчик позиции и те же
действия, что в веере. Список закольцован.

Перебираются только просматриваемые объекты: директории, архивы и документы в
галерею не попадают, и счётчик считает от их числа, а не от длины списка.

### Отметки и массовые действия — чекбоксы и `sl-bulk-bar`

Отметка — обычный `input[type=checkbox]` из `base.css`: первой колонкой списка
(`sl-col-check` в админке) и в углу плитки. В шапке — «отметить все» с
промежуточным состоянием при частичном выборе.

Отметки хранятся одним набором на список, поэтому плитка и строка одного файла
всегда показывают одно состояние. Панель `sl-bulk-bar` со счётчиком `sl-bulk-count`
появляется, когда отмечен хотя бы один объект, и скрывается, когда снята
последняя отметка. Смена директории или контекста отметки сбрасывает — иначе
действие уйдёт не туда.

Отметка и «текущий объект» — разные вещи. Текущий тот, чьи свойства показаны
справа; он меняется нажатием на строку или плитку и не зависит от отметок.

Действия панели говорят числом: при одной отметке «вставить изображением», при
нескольких — «вставить изображениями».

---

## 10. UI — «Системные файлы»

Новая отдельная главная вкладка:

```text
[ Каталог файлов ] [ Системные файлы ] [ Шаблоны ] [ Конфигурация ] [ Справка ]
```

Доступ:

```php
isAdmin(true)
```

Root:

```text
BASE_DIR
```

Оболочка та же, что и у «Каталога файлов»: меняются корень, содержимое дерева и
строка возможностей. Селектор upload-директорий в этом контексте не выводится.

```text
┌──────────────────────────────────────────────────────────────────────────┐
│ ← ↑ ⟳ │ SLAED / templates / lite / assets / css   🔍 Фильтр    ▤ ▦      │
├──────────────┬─────────────────────────────────────┬─────────────────────┤
│ ДЕРЕВО САЙТА │ Имя           Тип   Размер  Изменён │ СВОЙСТВА            │
│ 📁 SLAED     │ 📁 assets     дир.    —    09.08.26 │ Имя    theme.css    │
│ ├ 📁 admin   │ 📄 theme.css  CSS  198 KB  сегодня  │ Тип    text · css   │
│ ├ 📁 config  │ 📄 base.css   CSS   14 KB  08.08.26 │ Строк  5 753        │
│ ├ 📁 core    │ 📄 index.php  PHP   19 KB  04.08.26 │ Версия 8f3ac1       │
│ └ 📁 templa… │ 🖼 favicon.svg SVG   2 KB  12.06.26 │ [Редактировать]     │
├──────────────┴─────────────────────────────────────┴─────────────────────┤
│ ДОСТУПНО: просмотр · скачивание · правка текста POST ·                   │
│           сохранение с проверкой версии POST · ✕ удаление · ✕ загрузка   │
└──────────────────────────────────────────────────────────────────────────┘
```

### Открытие текстового файла

Список уступает место editor view, дерево остаётся:

```text
┌──────────────┬───────────────────────────────────────────────────────────┐
│ ДЕРЕВО САЙТА │ [← К списку]  templates/lite/…/theme.css                  │
│ 📁 SLAED     │                          🛡 версия 8f3ac1   [Сохранить]   │
│ ├ 📁 admin   ├───────────────────────────────────────────────────────────┤
│ ├ 📁 config  │                                                           │
│ └ 📁 templa… │                       CodeMirror 6                        │
│              │                                                           │
└──────────────┴───────────────────────────────────────────────────────────┘
```

Версия выдаётся при открытии и уходит обратно при сохранении (§13). Она видна в
шапке редактора, чтобы конфликт не выглядел необъяснимым отказом.

Используется только:

```php
Editor::getCode(...)
```

---

## 11. Определение типа системного файла

Resolver:

```text
.php                 → php
.html .htm .tpl      → html
.css                 → css
.js                  → js
.json                → json
.sql                 → sql
.xml                 → xml
.txt .md .ini .log   → text
```

Изображения:

```text
png jpg jpeg gif webp avif    → preview
svg                           → preview только через <img>, §20
```

PDF → preview/download.

Неизвестный binary → info/download, без editor.

Markdown в System Files по умолчанию открывается как source через CodeMirror `text`.
ToastUI для raw `.md` автоматически не используется.

---

## 12. System Files security boundary

Это отдельная политика от Upload.

### Обязательные правила

1. Root всегда определяется сервером.
2. Browser передаёт только относительный path.
3. Все пути нормализуются.
4. `..` не может выйти за root.
5. Запрещены absolute paths.
6. Запрещены stream wrappers: `php://`, `file://`, `data://` и т.п.
7. Проверяется symlink escape.
8. Mutating operations — только POST.
9. Все mutating requests — CSRF protected.
10. Никакого shell execution.
11. Никакого chmod/chown в v1.
12. Никакого произвольного archive extraction в v1.

### Hidden/protected paths

**Спрятать из списка — не значит защитить.** Путь, убранный из перечисления,
по-прежнему открывается по прямому запросу: его можно прочитать, скачать,
переписать и удалить, зная имя. Скрытие — это удобство, а защита — это отказ,
и они задаются раздельно.

Политика централизована и отвечает на каждую операцию отдельно:

```text
путь                    список  чтение  скачивание  запись  удаление
.git/                   нет     нет     нет         нет     нет
.env                    нет     нет     нет         нет     нет
config/*.php            да      да      да          да      подтверждение
storage/sessions/       нет     нет     нет         нет     нет
storage/logs/*.log      да      да      да          нет     да
*.part, lock-файлы      нет     нет     нет         нет     нет
```

Пять столбцов таблицы — не весь набор операций. Переименование, копирование и
перемещение трогают **два** пути сразу, и каждый проверяется своей стороной:

```text
операция          источник           назначение
переименование    write + delete     write
копирование       read               write
перемещение       write + delete     write
создание файла    —                  write
создание папки    —                  write
```

Читается так: переместить можно только то, что разрешено менять и удалять, и
только туда, куда разрешено писать. Копирование источник не меняет, поэтому ему
довольно чтения. Проверяются оба пути, а не один: иначе закрытый `.env`
копируется в открытую директорию и читается оттуда свободно.

Решение принимает один метод — `checkFileAccess()` (§5), — и каждый маршрут
спрашивает его перед действием, а не после того, как путь уже открыт. Для
операций с двумя путями он вызывается дважды, по разу на сторону. Проверки не
размазываются по контроллерам.

Тест обязан бить не по списку, а по прямому обращению: запросить закрытый путь
на чтение, на скачивание, на запись и на удаление и получить отказ на каждом.
Проверки «его нет в перечислении» недостаточно — она проходит и тогда, когда
защиты нет вовсе.

### Critical files

Супер-администратору доступны все операции и над этими файлами:

```text
index.php
admin.php
setup.php
.htaccess
config/*
core/*
```

Запрета нет, но список остаётся: он помечает пути, для которых удаление,
переименование и перенос требуют явного подтверждения в интерфейсе, а сама
операция пишется в audit log (§24). Ошибка здесь останавливает сайт, поэтому
случайного клика быть не должно.

Список ведётся централизованно в system policy. Не размазывать эти проверки
по controller.

---

## 13. Безопасное сохранение исходников

Обычный:

```php
file_put_contents($file, $text)
```

для System File Editor недостаточен.

При `open` сервер отдаёт version/hash. При `save` browser возвращает version.

### Сверка версии происходит под замком, а не рядом с ним

Сравнить версию и потом заменить файл — это две операции, и между ними успевает
вклиниться второй запрос: оба читают одну версию, оба считают её актуальной, оба
пишут, и правка того, кто пришёл первым, исчезает без следа. Проверка версии без
замка не защищает ни от чего.

Поэтому весь участок идёт под исключительной блокировкой назначения:

```text
взять замок назначения (LOCK_EX)
 ↓
прочитать текущее содержимое и посчитать версию
 ↓
сравнить с версией, пришедшей от клиента
 ├─ разошлись → отпустить замок, ответить 409, ничего не писать
 └─ совпали   → записать временный файл в той же директории
                 ↓
                заменить назначение одним переименованием
                 ↓
                посчитать новую версию и вернуть её
 ↓
отпустить замок
```

**Замок общий на проект, и его надо извлечь.** `Upload` уже делает ровно это при
публикации: `flock(LOCK_EX)` на файл-замок, временный `.part` в директории
назначения и одно переименование под тем же замком
(`core/classes/upload.php:561`). Но методы `getLockHandle()` и `deleteLockHandle()`
(`core/classes/upload.php:639` и `:651`) объявлены `private`, и вызвать их
`FileManager` не может.

Копировать протокол нельзя, и не только из-за дубля: **два независимых замка не
сериализуют участников между собой**. Если `Upload` публикует файл под своим
замком, а `FileManager` пишет под своим, они спокойно столкнутся на одном пути.

Поэтому протокол переезжает в `FileManager` как публичная пара:

```text
FileManager::getPathLock()      взять исключительный замок пути
FileManager::deletePathLock()   отпустить его
```

### Ключ замка — директория, а не файл

Так уже устроено в `Upload`: замок берётся на `dirname()` временного файла
(`core/classes/upload.php:569`), а имя файла-замка — `sha1()` этого
канонического пути (`:641`). Ключ сохраняется без изменений, иначе новый писатель
встанет в другую очередь и сериализации не будет вовсе.

Директория, а не файл, выбрана не из удобства: `Upload` под этим замком **ищет
свободное имя** в каталоге, и замок отдельного файла ему бесполезен — файла ещё
нет. Один ключ на всех означает, что публикация и правка в одном каталоге ждут
друг друга. Цена известна и принимается: две правки разных файлов одной
директории тоже сериализуются.

### Кто обязан брать замок

```text
запись содержимого файла        да, этап 4
создание файла и директории     да, этап 5
переименование                  да, этап 5
копирование                     да, этап 5
перемещение                     да, этап 5, два замка
удаление                        да, этап 5
загрузка через Upload           да, уже берёт
чтение, перечисление, скачивание нет
```

Операция этапа 5 без замка — незакрытая работа: она делает ровно то, ради чего
протокол и заводился.

### Две директории и порядок захвата

Перемещение и копирование между каталогами трогают два ключа, и два процесса,
взявшие их в разном порядке, встанут насмерть. Поэтому ключи **сортируются и
берутся в лексикографическом порядке**, а при совпадении берётся один замок:

```text
$keys = array_unique([$srcdir, $dstdir]);
sort($keys);
берём по очереди, отпускаем в обратном порядке
```

Это правило обязательно к соблюдению во всех операциях с двумя путями, и тест
этапа 5 проверяет именно его: перемещение A→B и одновременное B→A не образуют
взаимной блокировки.

### Переименование поверх существующего файла

`rename()` в PHP на Windows перезаписывает существующий файл — проверено на
стенде проекта, старое содержимое заменяется, временный файл исчезает. Отдельная
ветка «сначала unlink, потом rename» не нужна и вредна: она открывает окно, в
котором файла нет вообще.

Тест обязан проверять именно замену существующего файла, а не запись в пустоту.

### Чужие процессы

Замок держат только писатели, которые его берут: `FileManager` и `Upload`. Правка
файла по FTP, редактором на диске или другой программой замка не берёт — и не
может быть им остановлена.

Гарантия формулируется без преувеличения:

```text
два писателя через приложение   сериализуются полностью, потери правки нет
правка мимо приложения          обнаруживается best-effort сверкой версии
```

«Best-effort» здесь буквально: между повторным чтением версии под замком и
переименованием остаётся окно, и внешняя программа, попавшая в него, будет
перезаписана без предупреждения. Закрыть это окно средствами приложения нельзя —
можно только не обещать того, чего оно не делает.

Практический вывод для системной области: правка файлов мимо интерфейса во время
работы с ним — не поддерживаемый сценарий, а не защищённый.

Сохранение:

1. validate path (§12);
2. validate writable;
3. take the destination lock;
4. re-read and compare version;
5. create temp in same directory;
6. write complete content;
7. flush/close;
8. preserve permissions where applicable;
9. atomic rename/replace;
10. release the lock and return new version.

Пустой файл должен сохраняться корректно.

---

## 14. Capabilities

UI не должен самостоятельно вычислять права по роли.

FileManager/context возвращает capabilities.

### Admin uploads

```text
browse       yes
preview      yes
upload       yes
download     yes
delete       yes
rename       yes, интерфейс предупреждает о ссылках
copy         yes
move         yes, интерфейс предупреждает о ссылках
mkdir        yes
compress     yes
edit         no, для этого есть «Системные файлы»
```

### ToastUI

```text
browse       according to module rule
upload       according to module rule
insert       yes when file visible
embed        according to editor contract
download     yes when file visible
compress     moderator of the module only
delete       moderator of the module only
rename       no
move         no
edit         no
```

Удаление и упаковка в окне редактора — единственное, что зависит здесь от роли,
и решает её `is_moder($mod)`, то самое единственное ролевое правило, которое
закрытый план загрузок оставил стоять над настройками. Автору материала эти два
действия не показываются вовсе, администратору — показываются в том же веере
(§9.1). Переименования и перемещения нет ни у кого: в окне редактора им нечего
делать.

### System

```text
browse       super-admin
preview      yes
edit         text/code files
create       yes
rename       yes
copy         yes
move         yes
delete       yes
download     yes
```

Для критических путей операция не запрещена, но требует подтверждения и
попадает в audit log (§12, §24).

---

## 15. Existing editor upload rules remain authoritative

Настройки upload-модуля остаются единственной точкой принятия решений для content editor.

File Manager не создаёт второй набор:

```text
editor_allow_upload
editor_allow_images
filemanager_allow_user
...
```

Используются существующие:

```text
extensions
maxquota
maxbytes
maxwidth
maxheight
maxfiles
thumbwidth
adminlist
moderfiles
userfiles
userupload
guestupload
guestfiles
```

`guestfiles` реализован вместе с `docs/EDITOR-UPLOADS-2026.md` и уже ограничивает список гостя (§16).

---

## 16. Ownership upload-файлов — сделано

Реформа владельца закрыта вместе с `docs/EDITOR-UPLOADS-2026.md`. Действующая модель:

```text
authenticated user → id пользователя строкой
guest              → непрозрачный токен сессии, hash_hmac от session_id
moderator/admin    → null, потому что владельца у файла нет, а не как скрытый флаг
```

Токен гостя выводится из сессии, но никогда не равен её идентификатору: сегмент
попадает в публичное имя файла и ничего не должен подтверждать, будучи прочитан
из URL. Сравнение владельца — строковое: приведение к числу обнуляет каждый
токен и роднит одного гостя с другим.

Реализация лежит в `getEditorFileOwner()` и `getEditorFileJson()` (`core/system.php`).

**Что из этого следует для FileManager.** Общего разбора managed-имени в проекте
**нет**, и это надо исправить прежде, чем появится третий читатель. Сегодня шаблон
повторён дважды:

```text
core/system.php:4408          регулярка с захватом владельца, внутри getEditorFileJson()
core/classes/upload.php:148   тот же шаблон без захвата, внутри deleteStoredFile()
```

Длина соли уже разъехалась: `Upload::SALTLEN` объявлена константой — и притом
`private`, — а в `core/system.php` вписана числом, и комментарий на строке 4394
честно это признаёт: «класс на этом маршруте не загружен, поэтому длина
повторена».

**Имя файла становится собственностью `FileManager`.** Разбирать имя и знать его
формат — работа файлового слоя, а не службы загрузки, поэтому туда переезжают и
шаблон, и длина соли:

```text
FileManager::SALTLEN            public const, длина соли
FileManager::getFileOwner()     разбор владельца из имени
FileManager::checkFileName()    соответствует ли имя managed-формату
```

`Upload::SALTLEN` удаляется, генератор имени берёт длину из `FileManager::SALTLEN`,
а `Upload::deleteStoredFile()` спрашивает `checkFileName()` вместо собственной
регулярки. Зависимость `Upload` → `FileManager` здесь только на константу и на
чистую проверку строки, поэтому Решение 4 §3 не нарушается: сервис загрузки
по-прежнему не читает и не перечисляет файловую систему.

Оба потребителя переводятся **в одном коммите этапа 2**: и `getEditorFileJson()`,
и `Upload::deleteStoredFile()`. Этап, оставивший хоть одну из двух регулярок на
месте, не закрыт.

Клиент имя файла не разбирает вообще — принадлежность приходит в дескрипторе (§7).

---

## 17. HTTP / routing

### ToastUI

Сохраняем существующий contract:

```text
index.php?go=4&op=editorUpload&mod=...
index.php?go=4&op=editorFiles&mod=...
```

Они становятся adapters над общим FileManager/Upload layer.

### Admin read operations

Можно сохранить `go=5` для HTMX fragments:

```text
getAdminFileBrowser
getAdminFileList
getAdminFileTree
getAdminFilePreview
```

### Скачивание

Прямая ссылка на файл годится только внутри `uploads/*`, который отдаётся
веб-сервером как статика. Для системной области она непригодна: `index.php`
по ссылке будет **исполнен**, а не отдан, `.htaccess` веб-сервер закроет, а
шаблон вернётся клиенту в обход всякой проверки прав.

Поэтому скачивание системного файла — отдельный маршрут `go=5`, а не ссылка:

```text
getAdminFileDownload
```

Он обязан:

```text
проверить isAdmin(true) заново, а не полагаться на то, что список уже открывался
проверить путь заново через checkFileAccess(), §12
отдать Content-Type: application/octet-stream и никогда угаданный по расширению
отдать Content-Disposition: attachment с безопасным именем
отдать Content-Length и завершиться exit после readfile()
```

`Content-Type` не выводится из расширения намеренно: `text/html` или
`image/svg+xml`, отданные из административной области, исполняются в её же
источнике и превращают просмотр файла в XSS. Активные типы — HTML, SVG, JS —
скачиваются, но не показываются в браузере: предпросмотр для них не строится
(§20).

Существующая функция `stream()` (`core/system.php:4418`) для этого не берётся:
её никто не вызывает, имя не проходит правило именования, заголовки устарели
(`application/force-download`, пустой `Content-Range`), пути она не проверяет и
не завершает вывод. Она либо переписывается под новый контракт, либо удаляется
как мёртвая — решается на этапе уборки.

### Admin write operations

Не использовать GET.

Выполнять POST через `admin.php?name=uploads`:

```text
op=fmupload     принять загруженный файл
op=fmcreate     создать пустой файл
op=fmmkdir      создать директорию
op=fmrename     переименовать
op=fmcopy       скопировать
op=fmmove       переместить
op=fmdelete     удалить
op=fmcompress   упаковать файл в ZIP
op=fmsave       сохранить содержимое из редактора
```

Каждая операция §5, меняющая файловую систему, имеет ровно один маршрут в этом
списке. Операция без маршрута — незакрытая работа, а не «сделаем позже».

Все через `checkAdminPost(...)` или эквивалентный scoped CSRF contract.

### Важная миграция

Текущее удаление в `getAdminUploadFiles()` выполняется через GET/HTMX GET.
При переходе на File Manager delete/compress должны стать POST-actions.

---

## 18. HTMX / JavaScript

HTMX используется для:

- directory navigation;
- tree lazy-load;
- list refresh;
- pagination;
- preview panel;
- editor open/close;
- save response;
- dialogs where удобно.

Vanilla JS используется для:

- Ctrl/Shift multi-select;
- keyboard navigation;
- context menu;
- drag & drop upload;
- list/grid switch;
- local UI state;
- unsaved editor guard.

Не строить SPA router.
URL страницы остаётся обычным SLAED admin URL.

---

## 19. Search

Первая версия:

```text
filter current directory
```

без рекурсивного обхода всего сайта.

Позже:

```text
recursive search
```

только по явному запросу и с ограничением количества результатов/времени.

---

## 20. Preview

### Upload mode

Preview:

- image;
- audio;
- video;
- PDF;
- text metadata;
- archive metadata.

**Активные типы предпросмотра не получают.** HTML и JS не показываются вовсе —
отданные из административного источника, они в нём же и исполнятся. SVG
показывается только как `<img src>`: в этом контексте браузер скрипты внутри
файла не выполняет. Вставлять SVG в разметку страницы — inline или через
`<object>` — запрещено.

Скачать эти типы можно всегда: скачивание идёт отдельным маршрутом с
`application/octet-stream` (§17), и исполнить их браузер уже не может.

### System mode

Preview для media, по тем же правилам активных типов. Text/code сразу
открывается CodeMirror.

---

## 21. Работа с архивами

V1:

```text
download
compress existing upload file
```

Существующую функцию compress можно сохранить через новый POST action.

Не делать в первой версии:

```text
extract arbitrary ZIP/TAR into filesystem
```

Если extraction добавляется позже:

- canonical destination;
- запрет `../`;
- запрет absolute entry names;
- symlink policy;
- file count limit;
- total extracted bytes limit;
- overwrite policy.

---

## 22. Что делать с текущими под-вкладками uploads

Сейчас:

```text
[ Файловый менеджер ]
[ Основная директория ]
[ Дополнительная директория ]
```

На первом этапе оставить без изменений.

### Phase A

Новый File Browser появляется в «Файловый менеджер».
Старые «Основная директория» и «Дополнительная директория» продолжают работать.

### Phase B

После стабилизации File Browser сравнить функциональность.
Если всё покрыто, legacy lists можно убрать или превратить в быстрые views.

---

## 23. Что делать с `admin/modules/editor.php`

Не переносить File Manager туда.

`editor.php` остаётся специализированным quick editor системных файлов:

```text
system.php
header.php
.htaccess
robots.txt
```

Новая вкладка «Системные файлы» даёт общий filesystem browser.

В будущем quick editor может:

- остаться как быстрый инструмент;
- либо открыть соответствующий файл в `Uploads → System Files`.

Это не является блокером File Manager.

---

## 24. Logging

Для system mutating operations audit entry **обязателен**: операция без записи
в журнал считается незакрытой, и тест этапа это проверяет. Ошибка в системной
области останавливает сайт, и вопрос «кто и когда это сделал» задаётся именно
тогда, когда журнала уже не завести.

Пишется:

```text
admin
operation
relative path
target path
timestamp
result
```

Особенно:

```text
save
delete
rename
move
upload
```

Содержимое файла в log не писать.

---

## 25. Производительность

### Directory listing

Не сканировать рекурсивно всё дерево.

Использовать:

```text
lazy tree
current-directory listing
pagination
```

### Metadata

Не выполнять дорогой `getimagesize()` для каждого неизвестного файла без проверки extension/type.

### Hash

Полный hash нужен для редактируемых text/code файлов при open/save.
Для обычного directory list hash не вычисляется.

---

## 26. Этапы реализации

Порядок фиксирован и объяснён у каждого этапа. Один этап — одна сессия — один
готовый к коммиту набор; сам коммит делается только по команде владельца
(`.rules/git.md`). Не начинать следующий этап, не закрыв предыдущий.

```text
0    Editor Uploads contract              сделано
0.5  Выравнивание тем                     сделано
1    Состояния и ошибки в макетах         следующий
2a   FileManager core: путь, дескриптор, политика
2b   Имя файла и замок переезжают в FileManager
3    Системные файлы, чтение и скачивание
4    Редактор исходников
5    Операции над системными файлами
6    Каталог файлов админки
7    Окно ToastUI
8    Настольные удобства
9    Уборка
```

---

### Этап 0 — Editor Uploads contract — **сделано**

`docs/EDITOR-UPLOADS-2026.md` закрыт: `guestfiles`, владелец сессии гостя, явная
привилегия модератора, единое окно ToastUI, безусловный blob hook, лимиты embed
и записи. Действующая модель владельца — §16.

Последний открытый пункт того плана закрыт 2026-08-10: режим `editor` в
`tools/upload-route-check.php` пройден по живому стенду, все строки прошли —
четыре сочетания настроек против модератора, участника и гостя, изоляция двух
гостевых сессий и защита записи по реальному HTTP. `config/uploads.php`
восстановлен байт в байт, дерево загрузок осталось прежним, логи ошибок пусты.

Команда прогона, если понадобится повторить:

```text
SLAED_ADMIN_NAME, SLAED_ADMIN_PASS   учётная запись администратора
SLAED_USER_NAME,  SLAED_USER_PASS    та же учётная запись как пользователь сайта
php tools/upload-route-check.php https://slaed.loc editor
```

Учётных записей нужно две, а не четыре: `is_moder()` читает только админскую
сессию, поэтому один и тот же аккаунт отвечает модератором в админской банке
cookie и обычным участником во фронтовой.

**Следствие:** ничего не блокирует. Каталог гостя работает и показывает файлы
его сессии.

---

### Этап 0.5 — Выравнивание тем — **сделано**

Семь расхождений между `templates/lite` и `templates/admin` закрыты правкой тем.
Таблица и правило на будущее — §32.

---

### Этап 1 — Состояния и ошибки в макетах

**Карта чтения.**

```text
план      §31 состояния и ошибки · §9.1 веер, галерея, отметки · §20 предпросмотр
код       core/classes/upload.php  какими ошибками отвечает загрузка
          lang/ru.php              готовые тексты отказов, новых не сочинять
править   demo/filemanager-v2-rail.html · demo/filemanager-admin-2026.html
```

**Зачем первым.** Решения по текстам и поведению отказов принимаются один раз и
на макете, а не кусками посреди реализации. Все следующие этапы переносят готовое.

**Что сделать.** Дорисовать в обоих макетах состояния из §31: полоса выполнения
и очередь загрузки, отказ с причиной, скелетон каталога, пустой результат
фильтра, ошибка загрузки списка, подтверждение удаления через `#sl-confirm`
вместо `window.confirm`, переименование полем прямо в строке.

Тексты отказов брать те, которыми отвечает `Upload`: тип, размер, размеры
изображения, квота, число файлов за раз. Сочинять новые не нужно — они уже есть
в языковых файлах.

**Проверка.** Браузер: каждое состояние снято и просмотрено, консоль чистая,
горизонтальной прокрутки нет ни на одной ширине.

**Коммит.** `Docs:` — макеты и план идут одним набором.

**Стоп.** Если для состояния нет готовой константы и её придётся заводить —
остановиться и согласовать имя: правила в `.rules/constants.md`, префикс — в конце этого раздела.

---

### Этап 2a — FileManager core: путь, дескриптор, политика

**Карта чтения.**

```text
план      §5 контракт · §6 контексты · §7 дескриптор · §12 политика путей
          §14 capabilities · §25 производительность · §27 тесты
код       core/system.php:4290 getUploadService() — образец подключения класса
          core/classes/upload.php — образец стиля класса и его комментариев
создать   core/classes/filemanager.php
          tests/Unit/FileManagerPathTest.php
```

**Зачем сейчас.** Слой читает файловую систему и ничего не меняет: ошибиться
здесь дёшево, а весь остальной план на него опирается.

**Что сделать.** Создать `core/classes/filemanager.php`:

```text
контексты                uploads | system | editor (§6)
корневой сторож          root задаёт сервер, клиент передаёт только относительный путь
каноничный путь          §12: .. , абсолютные пути, stream wrappers, symlink escape
checkFileAccess()        матрица политики путей §12, ответ на каждую операцию
getFileList()            перечисление директории
getFileData()            дескриптор §7, realpath только для админского контекста
getCapabilities()        §14, ответ сервера, а не вычисление в интерфейсе
isFileEditable()         открывается ли файл в редакторе исходников
getCodeLanguage()        режим подсветки, §11
```

Имена методов — по §5 и `.rules/global.md`. Ни `list()`, ни `stat()`, ни `read()`
в классе не появляются.

Ни одной операции записи: `setFileBody()`, `deleteFileEntry()`, `updateFileName()`
в этом этапе не появляются даже пустыми. Имя файла и замок — следующий этап,
здесь их ещё нет.

**Проверка.** PHPUnit, обязательный набор путей из §27: обычный относительный,
`../`, вложенный `../../`, `..\\`, абсолютный, диск Windows, NUL, stream wrapper,
symlink внутри и наружу корня, несуществующий родитель, UTF-8 имя, скрытый файл,
пустой файл, нечитаемый файл. Плюс политика §12: закрытый путь отвечает отказом
не только в перечислении, но и на прямой запрос чтения.

**Коммит.** `Feature:` — новый класс и его тест.

**Стоп.** Если для контекста не хватает данных, которые сегодня собирает
`getUploadRuleData()` — не расширять правило молча, а остановиться: правило
модуля остаётся единственным источником (§15).

---

### Этап 2b — Имя файла и замок переезжают в FileManager

**Карта чтения.**

```text
план      §3 Решение 4 направление зависимости · §13 замок · §16 владелец
          §5 контракт · §27 тесты
код       core/system.php:4408    регулярка владельца в getEditorFileJson()
          core/classes/upload.php:148  та же регулярка в deleteStoredFile()
          core/classes/upload.php:15   private const SALTLEN
          core/classes/upload.php:569  взятие замка каталога
          core/classes/upload.php:639  getLockHandle(), :651 deleteLockHandle()
          tests/Support/upload_probe.php:13  подключает upload.php напрямую
править   core/classes/filemanager.php · core/classes/upload.php · core/system.php
тесты     tests/Unit/UploadContractTest.php · UploadFormatTest · UploadIntegrationTest
```

**Зачем отдельно.** Это не создание нового кода, а перенос работающего из двух
чужих файлов. Риск другой: сломать то, что уже везут `Upload` и маршрут
редактора. Отдельный этап держит эту миграцию отдельно от нового класса, и если
она пойдёт тяжело, этап 2a уже закрыт и лежит в дереве.

**Что сделать.** Перенести в `FileManager` две вещи, которые сегодня заперты в
`Upload`, и подключить обоих потребителей:

```text
FileManager::SALTLEN          public const; Upload::SALTLEN удаляется
FileManager::checkFileName()  формат managed-имени, один на проект
FileManager::getFileOwner()   разбор владельца, один на проект
FileManager::getPathLock()    исключительный замок каталога, §13
FileManager::deletePathLock() отпустить замок
```

Ключ замка — канонический путь каталога, тот же `sha1()`, что сейчас
(`core/classes/upload.php:641`): другой ключ означает другую очередь и отсутствие
сериализации.

Переводятся оба места регулярки — `getEditorFileJson()` и
`Upload::deleteStoredFile()`, — и `Upload` переходит на общий замок вместо
приватного `getLockHandle()`. Этап, оставивший хоть одну регулярку или второй
протокол замка, не закрыт.

`core/classes/upload.php` в своём начале подключает файловый слой, потому что
автозагрузки в проекте нет и `upload.php` подключают напрямую (§3):

```php
if (!class_exists('FileManager')) require_once __DIR__.'/filemanager.php';
```

**Проверка.** Владелец: имя участника, имя гостя, имя без владельца, имя не по
формату. Замок: два писателя на один каталог сериализуются, `Upload` и
`FileManager` встают в одну очередь.

**Существующие тесты `Upload` — часть проверки этапа, а не соседняя забота.**
После переноса обязаны пройти `UploadContractTest`, `UploadFormatTest` и
`UploadIntegrationTest`. Первый читает исходник класса и проверяет его форму,
поэтому удаление `Upload::SALTLEN` и замена приватного замка почти наверняка
потребуют правки самого теста. Правка теста допустима, ослабление проверки —
нет: если утверждение больше не про то, оно переписывается на новый инвариант,
а не удаляется.

**Коммит.** `Refactor:` — перенос без изменения поведения.

**Стоп.** Если после переноса падает тест `Upload`, который не про имя и не про
замок — остановиться: значит переезд задел то, чего не должен был.

---

### Этап 3 — Системные файлы, чтение и скачивание

**Карта чтения.**

```text
план      §4 компоненты · §10 UI системных файлов · §11 типы · §12 политика
          §17 маршруты и скачивание · §20 предпросмотр · §25 производительность
код       admin/modules/uploads.php    хозяин вкладок
          core/admin.php:867           getAdminUploadFiles(), образец go=5
          index.php:187                диспетчер go=5
макет     demo/filemanager-admin-2026.html, вкладка «Системные файлы»
править   admin/modules/uploads.php · templates/admin/partials/file-browser*.html
          templates/admin/assets/js/admin-ui.js · templates/admin/assets/css/theme.css
```

**Зачем сейчас.** Первая проверка архитектуры на живом интерфейсе там, где
ничего нельзя испортить: читаем дерево сайта и ничего не пишем.

**Что сделать.** Новая главная вкладка «Системные файлы» в
`admin/modules/uploads.php` (§10), доступ `isAdmin(true)`, корень `BASE_DIR`.
Дерево с ленивой подгрузкой, крошки, список, свойства, предпросмотр. Разметка —
партиалы из §4, поведение — в `admin-ui.js`, стили — в `theme.css`.

Селектор upload-директорий в этом контексте не выводится, кнопок загрузки и
создания папки нет.

Чтения идут через `go=5` (`index.php:187`), рядом с существующими
`getAdminUploadFiles` и по тому же образцу.

**Скачивание входит в этот этап.** Оно заявлено в возможностях системного
контекста (§14), а прямая ссылка для системной области не годится: `index.php`
по ней исполнится вместо того, чтобы отдаться. Маршрут `getAdminFileDownload`
и его требования описаны в §17. Активные типы — HTML, SVG, JS — скачиваются,
но не показываются в предпросмотре (§20).

**Проверка.** Браузер под супер-администратором: дерево раскрывается, крошки
ведут, свойства следуют за выбранной строкой, абсолютный путь показан только в
свойствах. Проверить отказ не-супер-администратору. Скачать `index.php` и
убедиться, что пришёл исходник, а не результат его выполнения; скачать закрытый
путь из §12 и получить отказ.

**Коммит.** `Feature:`.

**Стоп.** Если для дерева понадобится рекурсивный обход — остановиться: §25
запрещает, дерево ленивое.

---

### Этап 4 — Редактор исходников

**Карта чтения.**

```text
план      §11 типы · §12 политика и критические пути · §13 сохранение и замок
          §17 маршруты · §24 журнал · §27 тесты
код       core/classes/editor.php:55   Editor::getCode()
          core/security.php:718        checkAdminPost()
          core/classes/filemanager.php getPathLock() из этапа 2b
править   admin/modules/uploads.php · templates/admin/partials/file-browser-editor.html
```

**Зачем сейчас.** Первая операция записи в плане, и самая опасная: ошибка здесь
останавливает сайт. Поэтому она идёт после того, как чтение уже проверено.

**Что сделать.** Открытие текстового файла в `Editor::getCode()`, определение
типа по §11, версия при открытии и сверка при сохранении по §13.

Весь участок «прочитать версию → сравнить → записать временный → заменить» идёт
под общим замком пути — `FileManager::getPathLock()`, той самой парой, что этап 2
извлёк из `Upload` (§13). Своего протокола редактор не заводит: иначе он не
сериализуется с загрузкой, которая пишет в те же директории. Конфликт версий
отвечает 409 и не пишет ничего.

Переименование поверх существующего файла на Windows работает — проверено на
стенде проекта, — поэтому ветка «сначала unlink» не пишется: она открывает окно,
в котором файла нет вообще.

Сохранение — POST через `admin.php?name=uploads`, `checkAdminPost()` (§17).
Критические пути из §12 требуют подтверждения и пишут запись в audit log (§24).

**Проверка.** PHPUnit по §27: PHP, CSS, JS, JSON, текст; пустое сохранение;
двоичный файл отклонён; файл изменён после открытия — конфликт; замена именно
существующего файла, а не запись в пустоту; сохранение прав; отказ без CSRF;
отказ не-супер-администратору; запись в audit log на каждом удавшемся
сохранении (§24).

**Коммит.** `Feature:`.

**Стоп.** Если понадобится писать без замка назначения — остановиться: сверка
версии без блокировки не защищает ни от чего (§13).

---

### Этап 5 — Операции над системными файлами

**Карта чтения.**

```text
план      §5 контракт · §12 политика и две стороны пути · §13 замки и их порядок
          §17 маршруты · §24 журнал · §29 что вне первой версии
код       core/security.php:718        checkAdminPost()
          core/classes/filemanager.php всё, что добавили этапы 2a и 2b
править   core/classes/filemanager.php · admin/modules/uploads.php
          templates/admin/assets/js/admin-ui.js
```

**Зачем сейчас.** §14 обещает системному контексту создание, переименование,
копирование, перемещение и удаление, но до этого этапа их не реализовал никто:
этап 3 только читает, этап 4 только сохраняет открытый файл. Обещание без этапа
— дыра в плане, и она закрывается здесь, сразу после того, как путь записи уже
проверен редактором.

**Что сделать.** Довести контракт §5 до полного набора для контекста `system`:

```text
addFileEntry()       создать пустой файл
addDirectory()       создать директорию
updateFileName()     переименовать
addFileCopy()        скопировать
updateFilePath()     переместить
deleteFileEntry()    удалить файл или пустую директорию
addFileArchive()     упаковать файл в ZIP
```

Каждая операция получает свой POST-маршрут из §17 — `fmcreate`, `fmmkdir`,
`fmrename`, `fmcopy`, `fmmove`, `fmdelete`, `fmcompress` — через
`checkAdminPost()`, спрашивает
`checkFileAccess()` перед действием и пишет запись в audit log (§24).

У переименования, копирования и перемещения проверяются **оба** пути, источник и
назначение, по таблице §12, и берутся **оба** замка в порядке из §13 — иначе два
встречных перемещения встают насмерть.

Каждая операция этого этапа работает под замком: список, кто обязан его брать, —
в §13.

Удаление непустой директории в первой версии не делается: рекурсивное удаление
в системной области — отдельный разговор, и §29 его туда и относит.

Критические пути (§12) требуют подтверждения в интерфейсе; отказ по политике
приходит с сервера, а не проверяется только на клиенте.

**Проверка.** PHPUnit на каждую операцию отдельно: успех, отказ по политике
пути, отказ без CSRF, отказ не-супер-администратору, имя, уже занятое другим
объектом, перемещение внутрь самого себя, копирование в закрытую директорию.
Плюс запись в журнал на каждой удавшейся операции. Браузер: все действия из
веера и панели массовых действий доходят до результата.

**Коммит.** `Feature:`.

**Стоп.** Если операция требует рекурсии по дереву — остановиться: в первой
версии её нет (§29).

---

### Этап 6 — Каталог файлов админки

**Карта чтения.**

```text
план      §7 дескриптор · §8 UI каталога · §9.1 веер, галерея, отметки
          §12 политика · §14 capabilities · §15 правила модуля · §17 маршруты
          §19 поиск · §22 старые под-вкладки · §31 состояния
код       admin/modules/uploads.php    текущие под-вкладки и форма загрузки
          core/admin.php:867           getAdminUploadFiles(), его GET-мутации
          core/classes/upload.php      публикация файла
макет     demo/filemanager-admin-2026.html, вкладка «Каталог файлов»
править   admin/modules/uploads.php · core/admin.php · templates/admin/*
```

**Зачем сейчас.** Тот же слой, но с правами администратора и операциями над
пользовательскими файлами. Идёт после редактора, потому что переиспользует уже
проверенный путь записи.

**Что сделать.** Подключить браузер к под-вкладке «Файловый менеджер» по макету
`demo/filemanager-admin-2026.html`: дерево, крошки, список и плитки, свойства,
галерея предпросмотра, веер действий, отметки и панель массовых действий,
пагинатор, строка возможностей.

Операции: загрузка, скачивание, удаление, архивирование, переименование,
копирование, перемещение, создание директории. Все изменяющие — POST по §17,
под замком и с записью в журнал, по правилам §13 и §24.

Здесь же две миграции, обе обязательные:

```text
удаление и архивирование    из GET-ссылок HTMX в POST-действия
серверный путь              из заголовка раздела в поле свойств
```

Старые под-вкладки «Основная директория» и «Дополнительная директория»
остаются нетронутыми (§22, Phase A).

**Проверка.** PHPUnit по §27: изоляция корня модуля, неверный модуль, фильтрация
по владельцу, квота, число файлов, размеры изображения, неподдерживаемое
расширение, удаление managed-файла, обработка `thumb`. Браузер: все состояния
из §31, оба вида списка, массовые действия, галерея. После прогона — проверить
три файла логов.

**Коммит.** `Feature:`.

**Стоп.** Если окажется, что переименование ломает уже сохранённые ссылки в
материалах сильнее, чем предупреждение об этом покрывает — остановиться и
согласовать: автоматическое обновление ссылок вне первой версии (§29).

---

### Этап 7 — Окно ToastUI

**Карта чтения.**

```text
план      §9 окно и рельс · §9.1 веер, галерея, отметки · §14 capabilities
          §16 владелец · §17 контракт go=4 · §26 языковые константы
код       plugins/editors/toastui/driver.php            ключи шаблона и данные
          plugins/editors/toastui/assets/editor-upload.js  поведение окна
          core/system.php:4408                          getEditorFileJson()
          core/helpers.php:549                          getEditorRoomData()
макет     demo/filemanager-v2-rail.html, все четыре сценария прав
править   plugins/editors/toastui/* · templates/lite|admin/partials/editor-toastui-files.html
          templates/lite|admin/assets/editors/toastui/skin.css · lang/{locale}.php
```

**Зачем сейчас.** Окно перестраивается последним из основных, потому что
переиспользует всё: дескриптор, галерею, веер, отметки.

**Где править.** Не в админской теме: окно живёт в плагине и в темах сайта —
`plugins/editors/toastui/assets/editor-upload.js`, `driver.php`,
`templates/<theme>/partials/editor-toastui-files.html` и `skin.css` **каждой**
из двух тем (§4).

**Что сделать.** Перевести `editorFiles` на дескриптор FileManager и перестроить
окно по макету `demo/filemanager-v2-rail.html`: боковой рельс из трёх способов
вставки и хранилища, разделы отражают capabilities, компактный и развёрнутый
каталог, фильтр, галерея, веер, отметки и массовая вставка.

Загрузка продолжает идти через `Upload`. Контракт `go=4` не меняется (§17).

Нужны новые языковые константы во всех шести локалях: подписи разделов рельса,
«Вставить как», причины недоступности, тексты пустого каталога и отказов. Они
идут в `lang/{locale}.php` под префиксом `_EDITOR_*` (§26, языковые константы).
Существующие `_E*` — `_EUPLOAD`, `_EIMGURL` и прочие — переиспользуются как есть,
но новых имён в этой группе не появляется.

**Проверка.** Браузер под четырьмя ролями: автор с полным полем, автор с
полем-анонсом, гость с правом загрузки, гость без него. У каждой — свой набор
разделов и своё содержимое каталога. Плюс состояния из §31.

**Коммит.** `Feature:`.

**Стоп.** Если для раздела не хватает флага, который сервер сегодня не отдаёт —
остановиться: интерфейс не вычисляет права сам (§14).

---

### Этап 8 — Настольные удобства

Карты чтения у этого и следующего этапа нет: оба опираются на всё, что
сделано раньше, и читаются вместе с этапами, которые дорабатывают.

Множественное выделение с клавишами, навигация стрелками по списку, контекстное
меню, перетаскивание файлов в каталог, клавиатура и фокус по §33.

**Коммит.** `Feature:`.

---

### Этап 9 — Уборка

Убрать дублирующие реализации списков, решить судьбу старых под-вкладок (§22,
Phase B), удалить устаревшие GET-мутации, сократить повторяющиеся файловые
помощники.

**Коммит.** `Refactor:`.

---

### Языковые константы

Собственный префикс `_FM_*` не вводится: `.rules/constants.md` требует, чтобы
префикс точно совпадал с namespace модуля или конфигурации, а namespace у
файлового менеджера свой не появляется — он живёт внутри уже существующих.
Строки распределяются по областям, которым принадлежат:

```text
админский браузер   admin/lang/{locale}.php     префикс _UPLOADS_*
                    namespace config/uploads.php → $conf['uploads']

окно редактора      lang/{locale}.php           префикс _EDITOR_*
                    namespace config/global.php → $conf['editor']
```

Оба префикса точно совпадают с namespace своей конфигурации, как требует
`.rules/constants.md`.

**Существующие `_E*` не переименовываются и переиспользуются как есть** —
`_EUPLOAD`, `_EIMGURL`, `_EMODEEMBED` и ещё семь десятков подписей редактора
работают и трогать их этот план не будет. Но расширять `_E*` новыми именами
нельзя: правило про точный префикс исключений не знает, а `_E*` под него не
подходит. Всё новое заводится как `_EDITOR_*`.

Административный браузер получает `_UPLOADS_*` — префикс своего
конфигурационного namespace.

Существующие константы переиспользуются, дублировать их запрещено: `_DELETE`,
`_UPDATE`, `_CLOSE`, `_DESCRIPTION`, `_ERROR_SIZE`, `_ERROR_FILE` и остальные
уже есть.

Каждая новая константа заводится сразу в шести локалях: `de`, `en`, `fr`, `pl`,
`ru`, `uk`.

## 27. Тестирование

### FileManager unit tests

Обязательно:

- normal relative path;
- `../`;
- nested `../../`;
- Windows `..\\`;
- absolute Unix path;
- Windows drive path;
- NUL;
- stream wrapper;
- symlink inside root;
- symlink outside root;
- non-existing parent;
- UTF-8 filename;
- hidden file;
- empty file;
- unreadable file;
- unwritable directory.

### Policy tests (§12)

- закрытый путь не появляется в перечислении;
- закрытый путь отвечает отказом на прямое чтение;
- закрытый путь отвечает отказом на скачивание;
- закрытый путь отвечает отказом на запись;
- закрытый путь отвечает отказом на удаление;
- копирование из закрытого пути в открытый отказано;
- перемещение в закрытый путь отказано.

### Managed name tests (§16)

- имя участника отдаёт его идентификатор;
- имя гостя отдаёт токен сессии;
- имя без сегмента владельца отдаёт `null`;
- имя, не подходящее под формат, отвергается;
- длина соли читается из `FileManager::SALTLEN`, а не повторяется.

### Lock tests (§13)

- два писателя на один каталог сериализуются, второй ждёт первого;
- `Upload` и `FileManager` встают в одну очередь, а не в две;
- операция с двумя каталогами берёт замки в отсортированном порядке;
- встречные перемещения A→B и B→A не образуют взаимной блокировки;
- существующие `UploadContractTest`, `UploadFormatTest` и
  `UploadIntegrationTest` проходят после переноса протокола.

### Download tests (§17)

- `index.php` скачивается исходником, а не результатом выполнения;
- заголовок типа всегда `application/octet-stream`;
- скачивание закрытого пути отказано;
- скачивание не-супер-администратору отказано.

### Audit tests (§24)

- каждая удавшаяся мутация системной области оставляет запись;
- запись несёт администратора, операцию, путь, время и результат;
- содержимое файла в журнал не попадает.

### System editor tests

- PHP open/save;
- CSS/JS/JSON/text;
- empty save;
- binary refused;
- file changed after open → conflict;
- замена именно существующего файла, а не запись в пустоту;
- сохранение идёт под замком каталога;
- permission preservation;
- CSRF denied;
- non-super-admin denied.

### Upload catalogue tests

- module root isolation;
- invalid module;
- owner filtering;
- user file filtering;
- guest session isolation;
- moderator list;
- admin list;
- quota;
- max file count;
- image dimensions;
- unsupported extension;
- delete managed file;
- thumb handling.

### Routing tests

- no mutating GET;
- correct CSRF scope;
- `go=4` editor contract unchanged;
- `go=5` admin reads super-admin only;
- admin writes POST only.

---

## 28. Acceptance criteria

File Manager считается готовым, когда:

- один domain layer используется Admin и ToastUI;
- зависимость классов односторонняя: `Upload` знает про `FileManager`, обратного
  вызова нет ни одного;
- протокол замка в проекте один, и оба писателя берут его на одном ключе;
- разбор managed-имени в проекте один, и обе прежние регулярки сняты;
- каждая операция §5, меняющая файловую систему, имеет ровно один маршрут §17,
  проверку `checkFileAccess()` и запись в audit log;
- Upload service не дублирован;
- System Files имеет отдельную вкладку;
- System Files доступен только super-admin;
- absolute server path не покидает сервер ни на одном маршруте, кроме одного:
  админский full mode получает поле `realpath` в дескрипторе и показывает его в
  свойствах (§7). В JSON редактора и в любом ответе не-администратору его нет;
- path traversal и symlink escape закрыты;
- system edit использует существующий `Editor::getCode()`;
- save имеет conflict detection, и сверка версии идёт под замком, а не рядом с ним;
- save заменяет существующий файл одним переименованием, без окна отсутствия файла;
- mutating actions не работают через GET;
- ToastUI не видит чужие upload roots/files;
- текущие module upload rules остаются authoritative;
- существующая загрузка файлов не ломается;
- текущая ToastUI интеграция не заменяется новым editor subsystem;
- нет jQuery/Vue/React/стороннего File Manager runtime.

---

## 29. Out of scope для первой версии

Не реализовывать сразу:

- FTP;
- SFTP;
- S3;
- WebDAV;
- Git client;
- shell/terminal;
- chmod/chown UI;
- recursive full-site search;
- automatic references update after rename;
- arbitrary archive extraction;
- recursive delete of a non-empty directory;
- file version history;
- recycle bin;
- remote filesystem mounts.

Это можно добавлять отдельно после стабильного core.

---

## 30. Итоговая архитектура

```text
                              SLAED CMS
                                  │
                    ┌─────────────┴─────────────┐
                    │                           │
             admin/modules/uploads.php      Content Editor
                    │                           │
      ┌─────────────┴─────────────┐          ToastUI
      │                           │             │
Каталог файлов              Системные файлы    │
      │                           │             │
      └─────────────┬─────────────┴─────────────┘
                    │
                    ▼
               FileManager
          filesystem/domain layer
                    │
       ┌────────────┴─────────────┐
       │                          │
    Upload                     Editor
 upload/publish             code/content
       │                          │
 core/classes/upload.php     Editor::getCode()
                                  │
                             CodeMirror 6
```

### Финальное правило

```text
Upload       = безопасно принять новый файл
FileManager  = безопасно работать с filesystem
FileBrowser  = показать filesystem пользователю
ToastUI      = выбрать/вставить разрешённый upload-файл
CodeMirror   = редактировать разрешённый system text/code файл
```

Ни один компонент не дублирует ответственность другого.

---

## 31. Состояния процесса и ошибок

Утверждённые макеты показывают установившееся состояние: файлы лежат, каталог
загружен. Реализация обязана показать и остальные — их список закрыт здесь,
чтобы каждый экран не решал заново.

### Загрузка

```text
идёт        полоса выполнения на файл, имя, размер, отмена
несколько   очередь: файлы грузятся по одному, видно который из скольких
удалась     файл появляется в каталоге, каталог не перезагружается целиком
не удалась  причина у файла, остальные файлы очереди продолжают грузиться
```

Причина берётся с сервера и не сочиняется на клиенте. `Upload` уже отвечает
разными ошибками: тип, размер, размеры изображения, квота, число файлов за раз.

### Каталог

```text
грузится    скелетон на месте плиток, не пустой экран и не «загрузка…»
пуст        объяснение почему: ещё ничего не загружено
фильтр пуст «ничего не найдено», с кнопкой сбросить фильтр
не удался   текст ошибки и «повторить», список не подменяется пустотой
```

Пустой каталог и пустой результат фильтра — разные состояния и разные тексты.

### Действие над файлом

```text
удаление       подтверждение через #sl-confirm, затем строка исчезает
архивирование  индикатор на строке, по завершении список обновляется
переименование поле прямо в строке, Enter применяет, Esc отменяет
конфликт       имя занято — сообщение у поля, а не общий алерт
```

Ни одно изменяющее действие не выполняется без ответа сервера: строка не
исчезает «оптимистично», пока запрос не подтверждён.

---

## 32. Выравнивание тем — **сделано**

Макеты собраны на системных компонентах, и при сборке нашлись расхождения между
`templates/lite` и `templates/admin`. Все они закрыты правкой тем, а не
переопределением в макете:

| Класс | Что было | Где не хватало |
| --- | --- | --- |
| `sl-but-mini` | браузер рисовал вторую рамку поверх круглой заливки | `admin` |
| `sl-but-mini.sl-is-muted` | белый глиф на светлой заливке не читался | `admin` |
| `sl-but-mini[disabled]` | недоступная кнопка не отличалась от доступной | `admin` |
| `sl-bulk-bar` | панель массовых действий существовала только в `lite` | `admin` |
| `sl-bulk-count` | счётчик отмеченных не имел общего имени | обе |
| `sl-modal-wide`, `sl-modal-image` | галерее негде было взять ширину и снимок | `admin` |
| `tr[aria-selected]` | выбранная строка не отличалась от наведённой | `admin` |

**Правило на будущее:** если компонента не хватает в одной теме, он добавляется в
неё под тем же именем, что в другой. Локальное переопределение в модуле или
макете — не решение, а отложенный дубль.

---

## 33. Клавиатура и доступность

```text
Esc            закрывает галерею, затем окно
← →            перебор в галерее
Enter          применяет действие в фокусе
Tab            не выходит за пределы открытой модалки
```

Фокус при открытии окна уходит на первый управляющий элемент, при закрытии
возвращается на кнопку, которая окно открыла. Строка состояния — `aria-live`,
чтобы результат действия читался озвучкой, а не только глазами.

Каждая кнопка-иконка несёт `title` и `aria-label`: в веере и в панели
инструментов подписи нет, и без них действие не назвать.
