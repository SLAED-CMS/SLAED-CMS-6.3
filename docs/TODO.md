# TODO

## Позже после удаления старого BB-редактора

Этот список содержит только задачи, которые нельзя безопасно закрыть во время
первого удаления старого BB-редактора. Сейчас удаляются только части, которые не
создают проблем.

### Современная вставка текста

- `plugins/system/insert-code.js`
  - После удаления старого BB-редактора файл временно остается, потому что он
    еще используется вне самого редактора.
  - Нужно заменить оставшиеся вызовы `InsertCode(...)` современным помощником
    вставки, который работает с ToastUI и обычным `textarea`.
  - Места для замены:
    - `core/system.php` — вставка имени пользователя в комментариях.
    - `modules/forum/index.php` — вставка имени пользователя на форуме.

### Старый HTML-список файлов

- `core/system.php::getEditorFiles()`
  - Позже удалить или заменить старый HTML-endpoint.
  - ToastUI уже использует новый JSON-поток для загрузки и списка файлов.
  - Перед удалением проверить, что больше нет вызовов `go=1&op=getEditorFiles`.
- `templates/*/fragments/editor-action-insert.html`
  - Позже удалить вместе со старым `getEditorFiles()`.
  - Сейчас фрагменты завязаны на `javascript: InsertCode(...)`.

### Legacy-смайлы

- `templates/*/images/smilies/*.gif`
  - Оставить после первого удаления BB-редактора.
  - Парсер все еще поддерживает старые смайлы формата `*01`.
  - Позже принять отдельное решение:
    - оставить поддержку legacy-смайлов для старого контента;
    - или перенести старый контент на Unicode Emoji и удалить GIF-смайлы.

### Финальная зачистка

После замены всех старых вызовов:

1. Удалить `plugins/system/insert-code.js`.
2. Удалить `getEditorFiles()` из `core/system.php`.
3. Удалить route `go=1&op=getEditorFiles` из `index.php`.
4. Удалить `templates/*/fragments/editor-action-insert.html`.
5. Проверить документацию и убрать последние упоминания legacy BB-инструментов.
