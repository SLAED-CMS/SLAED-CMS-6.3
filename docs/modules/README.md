# Модули SLAED CMS

SLAED CMS построена на модульной архитектуре, где каждый модуль представляет собой отдельную функциональность системы. Модули можно активировать, деактивировать и настраивать независимо друг от друга.

---

*© 2005-2026 Eduard Laas. All rights reserved.*

## Обзор модульной системы

### Принципы работы модулей

1. **Независимость** - каждый модуль работает автономно
2. **Стандартизация** - единый API для всех модулей  
3. **Безопасность** - собственные права доступа для каждого модуля
4. **Масштабируемость** - возможность добавления новых модулей

### Структура модуля

```
modules/module_name/
├── index.php              # Основной файл модуля
├── admin/                 # Административный интерфейс
│   └── index.php
└── language/              # Языковые файлы
    ├── lang-english.php
    ├── lang-russian.php
    └── lang-german.php
```

## Стандартные модули

### 📰 News - Система новостей
- Публикация новостей с категориями
- Комментарии и рейтинги
- RSS лента
- SEO-оптимизация

### 📄 Pages - Статические страницы
- Создание информационных страниц
- Иерархическая структура
- Встроенный редактор
- Контроль доступа

### 💬 Forum - Форум
- Многоуровневые обсуждения
- Модерация сообщений
- Система рейтингов
- Уведомления

### 🛒 Shop - Интернет-магазин
- Каталог товаров
- Корзина и заказы
- Способы оплаты
- Управление складом

### 📁 Files - Файловый архив
- Загрузка и хранение файлов
- Категоризация файлов
- Контроль доступа
- Статистика скачиваний

### 🖼️ Media - Медиа-галерея
- Фото и видео галереи
- Автоматическое создание превью
- Слайдшоу
- Теги и категории

### 🔗 Links - Каталог ссылок
- Организация внешних ссылок
- Проверка доступности
- Рейтинговая система
- Категории ссылок

### 👥 Users - Управление пользователями
- Регистрация и профили
- Система групп и прав
- Личные сообщения
- Активность пользователей

### 🗳️ Voting - Система голосований
- Создание опросов
- Множественный выбор
- Ограничения по времени
- Статистика результатов

### ❓ FAQ - Часто задаваемые вопросы
- База знаний
- Поиск по вопросам
- Категоризация
- Рейтинг полезности

### 😄 Jokes - Анекдоты
- Коллекция юмора
- Пользовательские добавления
- Рейтинговая система
- Случайный анекдот

### 📞 Contact - Обратная связь
- Контактные формы
- Обработка сообщений
- Защита от спама
- Уведомления

### 🔍 Search - Поиск
- Полнотекстовый поиск
- Поиск по модулям
- Фильтрация результатов
- Индексация контента

### 📊 Auto Links - Автоматические ссылки
- Автоматическая перелинковка
- SEO-оптимизация
- Управление анкорами
- Статистика кликов

## API модулей

### Базовые функции

Каждый модуль должен реализовывать стандартные функции:

```php
// Проверка активности модуля
function is_active($module, $view = '1') {
    global $db, $prefix;
    list($active) = $db->sql_fetchrow(
        $db->sql_query("SELECT active FROM {$prefix}_modules WHERE title='{$module}'")
    );
    return ($active == $view);
}

// Получение конфигурации модуля
function get_module_config($module) {
    include("config/config_{$module}.php");
    return $conf;
}

// Проверка прав доступа
function is_module_admin($module) {
    return is_moder($module);
}
```

### Стандартные операции

```php
// Основные операции модуля
switch($op) {
    case 'view':        view_item(); break;
    case 'add':         add_item(); break;
    case 'edit':        edit_item(); break;
    case 'delete':      delete_item(); break;
    case 'category':    show_category(); break;
    default:            show_main(); break;
}
```

### Система прав доступа

```php
// Уровни доступа к модулю
// 0 - публичный доступ
// 1 - только зарегистрированные пользователи
// 2 - только администраторы модуля
// 3 - только суперадминистратор

function check_module_access($module, $operation = 'view') {
    global $user;
    
    $module_config = get_module_config($module);
    $required_level = $module_config[$operation.'_access'];
    
    switch($required_level) {
        case 0: return true;
        case 1: return is_user();
        case 2: return is_moder($module);
        case 3: return is_admin();
        default: return false;
    }
}
```

## Разработка собственных модулей

### Структура нового модуля

1. **Создание папки модуля:** `/modules/my_module/`

2. **Основной файл** `index.php`:

```php
<?php
if (!defined('MODULE_FILE')) die('Illegal file access');

// Подключение языка модуля
get_lang('my_module');

// Подключение конфигурации
include('config/config_my_module.php');

// Проверка активности модуля
if (!is_active('my_module')) {
    head();
    echo setTemplateBasic('title', array('{%title%}' => _MODULE_DISABLED));
    foot();
    exit;
}

// Основная логика модуля
$op = getVar('req', 'op', 'var');

switch($op) {
    case 'view':
        view_item();
        break;
    default:
        show_main();
        break;
}

function show_main() {
    head();
    echo '<h1>Мой модуль</h1>';
    echo '<p>Содержимое модуля...</p>';
    foot();
}

function view_item() {
    $id = getVar('req', 'id', 'num');
    head();
    echo '<h1>Просмотр элемента '.$id.'</h1>';
    foot();
}
?>
```

3. **Языковой файл** `language/lang-russian.php`:

```php
<?php
if (!defined('FUNC_FILE')) die('Illegal file access');

define('_MY_MODULE_TITLE', 'Мой модуль');
define('_MY_MODULE_DESCRIPTION', 'Описание модуля');
define('_MY_MODULE_ADD', 'Добавить');
define('_MY_MODULE_EDIT', 'Редактировать');
define('_MY_MODULE_DELETE', 'Удалить');
?>
```

4. **Административный файл** `admin/index.php`:

```php
<?php
if (!defined('ADMIN_FILE')) die('Illegal file access');

// Проверка прав администратора
if (!is_moder('my_module')) {
    admin_access_denied();
    exit;
}

$op = getVar('req', 'op', 'var');

switch($op) {
    case 'add':
        admin_add_item();
        break;
    case 'edit':
        admin_edit_item();
        break;
    default:
        admin_main();
        break;
}
?>
```

### Регистрация модуля

После создания модуля его нужно зарегистрировать в системе:

```sql
INSERT INTO {prefix}_modules (
    title, description, active, view, blocks, blocks_c, mod_group
) VALUES (
    'my_module', 'Описание модуля', 1, 0, 1, 1, 0
);
```

## Конфигурация модулей

### Файл конфигурации

Каждый модуль имеет свой файл конфигурации `/config/config_module.php`:

```php
<?php
if (!defined('FUNC_FILE')) die('Illegal file access');

$conf_module = array(
    'items_per_page' => '10',      // Элементов на странице
    'allow_comments' => '1',        // Разрешить комментарии
    'moderation' => '0',           // Предмодерация
    'rss_enable' => '1',           // RSS лента
    'seo_urls' => '1',             // ЧПУ
    'upload_enable' => '1',        // Загрузка файлов
    'rating_enable' => '1',        // Система рейтингов
);
?>
```

## Интеграция с ядром

### Использование системных функций

```php
// Работа с базой данных
global $db, $prefix;
$result = $db->sql_query("SELECT * FROM {$prefix}_my_table");

// Проверка прав пользователя
if (is_user()) {
    // Код для авторизованных пользователей
}

// Работа с шаблонами
echo setTemplateBasic('title', array('{%title%}' => $title));

// Получение переменных
$id = getVar('req', 'id', 'num');
$text = getVar('post', 'text', 'html');

// Работа с файлами
upload_file('image', 'uploads/my_module/', array('jpg', 'png'));
```

### Хуки и события

```php
// Регистрация хука
add_hook('module_init', 'my_module_init');

function my_module_init() {
    // Код инициализации модуля
}

// Использование системных событий
trigger_event('item_added', array('module' => 'my_module', 'id' => $id));
```

Модульная система SLAED CMS обеспечивает гибкость и расширяемость, позволяя легко добавлять новую функциональность при сохранении стабильности основной системы.