# Состояние реализации

Единственная точка входа. Команда пользователя во всех окнах одна: «Работай по плану docs/node/PROGRESS.md». Она означает: выполнить этап из блока «Передача следующему окну» по протоколу ниже. Закрытые задачи проектирования не означают выполненную реализацию.

## Протокол окна

1. Текущий этап — тот, что назван в блоке «Передача следующему окну»; таблица ниже его подтверждает. Этап не выбирается по памяти, по истории чата или по желанию исполнителя.
2. Прочитать раздел «Вход в каждое новое окно» и карточку этапа в [14-roadmap.md](14-roadmap.md), затем документы из строки «Прочитать» блока передачи. Остальное не читать.
3. Проверить git status и фактическое состояние зависимостей по коду и артефактам, а не по записям предыдущего окна. Расхождение с записью — сначала в блок передачи, потом работа.
4. Выполнить ровно один этап. Следующий этап в том же окне не начинать, даже если остались силы и контекст.
5. Развилка, которую не решает план, задаётся пользователю через AskUserQuestion; молчаливый выбор запрещён. Ответ вносится в документ-владелец до продолжения.
6. Закрыть окно: обновить строку таблицы (состояние, команды проверок и их результат), затем целиком заменить блок передачи — следующий этап, что прочитать, изменённые файлы, выполненные проверки, остаток. Незавершённый этап остаётся в блоке под тем же ID с точным остатком. Отчёт — по .rules/report.md. Коммит — только по команде пользователя.
7. Блок передачи всегда указывает этап, с которого начнёт следующее окно; после S18 он сообщает, что план выполнен. Окно без обновлённого блока считается незакрытым.

## Этапы

| Этап | Состояние | Результат и проверка |
|---|---|---|
| S00 — Правка плана по аудиту готовности | выполнен 2026-09-19 | Находки сверки перенесены в документы-владельцы и карточки, решения NOD-224–NOD-235, партия H в TODO.md, AUDIT.md удалён. Контроль документации: UTF-8, один корневой заголовок, парность блоков кода, локальные ссылки, последовательность NOD, git diff --check по docs/node — пройден |
| S01 — Point — класс | выполнен 2026-09-19 | core/classes/point.php, config/points.php, DDL `_points` и строка совместимости в table.sql, tests/Unit/PointTest.php с tests/Support/point_probe.php на одноразовой схеме MariaDB 11.7. `php vendor/bin/phpunit --filter PointTest` — 18 тестов, 149 утверждений, пройден; `php -l`, phpstan, `php-cs-fixer check` по новым файлам, `php tools/ui-audit.php`, `--markup` и шесть фильтров ui:gates — пройдены. Полный phpunit: 958 тестов, один провал вне этапа — CommentIsolationTest на неизменённом modules/presentation/index.php |
| S02 — Rating — класс | выполнен 2026-09-20 | core/classes/rating.php; guard, checkWriteGuard и addEpoch(force) в core/classes/cache.php; маркеры и поколение в checkPageCache, getPageHash и setFoot в core/system.php; три таблицы Rating в table.sql; tests/Unit/RatingTest.php с tests/Support/rating_probe.php на одноразовой схеме MariaDB 11.7 и scratch-кеше. `php vendor/bin/phpunit --filter RatingTest` — 22 теста, 242 утверждения, пройден; `php -l`, phpstan, `php-cs-fixer check` по изменённым файлам, `npm run ui:gates` (ui-audit, markup, 195 тестов) — пройдены; HTTP стенда: news и главная 200, error_php.log без новых записей. Полный phpunit: 981 тест, один провал вне этапа — CommentIsolationTest на неизменённом modules/presentation/index.php |
| S03 — Конфигурация и файловые блокировки | выполнен 2026-09-20 | core/system.php: getConfig(fresh), setConfigFile(string\|Closure): bool, getConfigCode, setConfigSource, getConfigJournal, setConfigRestore; повторно входимые getPathLock()/deletePathLock() в core/classes/filemanager.php; op=restore и предупреждение о маркере в admin/modules/config.php, восемь констант `_CONFIG_*` в шести admin/lang; писатели fields.php, uploads.php, ratings.php на форме Closure; tests/Unit/ConfigFileTest.php с tests/Support/config_probe.php на scratch-копии config/, дополнение FileManagerLockTest. `php vendor/bin/phpunit --filter "ConfigFileTest\|FileManagerLockTest"` — 20 тестов, 185 утверждений, пройден; `php -l`, phpstan, `php-cs-fixer check` по изменённым файлам, `npm run ui:gates` (ui-audit, markup, 195 тестов) — пройдены. HTTP стенда: экран restore в чистом состоянии, сохранение uploads без изменений, настоящий погибший писатель по fields.php — предупреждение на экране настроек, отказ сохранения ratings, диагностика, отказ поддельного токена, POST-восстановление, исходники байт в байт; error_php.log, error_sql.log и error_site.log без новых записей. Полный phpunit: 995 тестов, один провал вне этапа — CommentIsolationTest на неизменённом modules/presentation/index.php |
| S03A — Удаление девяти старых модулей | выполнен 2026-09-20 | Удалены modules/{news,pages,faq,help,jokes,content,links,files,media}/, их config/*.php и записи config/modules.php, десять блоков, сид `_news` с категориями и опросом, девять таблиц в table.sql и 131 оператор с восемью заголовками в table_update6_3.sql; ключи девяти разделов убраны из config/fields.php, uploads.php, ratings.php и search.php; главная — presentation, карта HTML-кеша пуста до S13; общие helpers сведены к остающимся модулям, setContentActive() и место `files.dist` удалены; Comment::MODULES — account, shop, voting; несущая цель comment-тестов и contract_probe — shop. `php vendor/bin/phpunit` — 991 тест, 11979 утверждений, один провал вне этапа — CommentIsolationTest на демо-строке `_comments` в modules/presentation/index.php, девять пропусков, из них два новых — сценарии RatingTest с кешируемым маршрутом до S13; `php vendor/bin/phpstan analyse` — без ошибок; `php-cs-fixer check` по изменённым PHP — чисто; `npm run ui:gates` (ui-audit, markup, 195 тестов) — пройдены; `php -l` всех затронутых PHP — чисто. Пара `npm run ui:before` / `ui:after`: до — 193 кадра, после — сдвинулись только ожидаемые страницы: front (главная теперь presentation), presentation (пять фактов, одна карточка пульса), profile (вкладки ленты), все светлые админ-страницы (меню без девяти модулей, монитор без двух плиток); forum-list, forum-topic, voting, login, oauth-finish, settings, favorites и private в тёмной схеме без сдвигов; не сравнены тёмные админ-страницы (в проходе не удался вход в админку) и светлые страницы участника (их нет в наборе «до»); новое состояние forum-topic-button-hover заменило article-button-hover; tools/ui-contrast.json перегенерирован, 547 пар. HTTP стенда: главная, forum, shop, voting, users, search (GET и POST), rss, sitemap — 200; `name=news`, `name=files`, `name=content` — 404; error_php.log и error_site.log без записей от кода этапа, в error_sql.log только прежняя ошибка стенда — у `_favorites` нет колонки `time` (в журнале с 2026-09-15) |
| S04 — Point — подключение | не завершён, окно 2026-09-20/21 | Код переключён полностью: `updatePoints`, `addPointsAction`, `users.point`, `users.points` и константы `_POINTS01`–`_POINTS45`, `_DESC01`–`_DESC45` удалены вместе с вызовами, все владельцы карты points.md вызывают `$pnt->addEvent()`/`getEventId()`; `_order.uid` и `_points` в table.sql и table_update6_3.sql; ветка update6_3 с preflight, снимком, manifest и отметкой config/update.php; экран правил и журнал в admin/modules/groups.php, adjust и сброс в modules/account/admin/index.php. Стенд обновлён штатно через setup.php: preflight на открытом сайте отказал без изменений; после закрытия — `_points` InnoDB с обоими ограничениями, `_order.uid` на позиции 2, manifest verified, снимок 11848 аккаунтов = `_users`, сумма 340292 = живой, отметка points = 6.3.0. `php vendor/bin/phpunit` — 991 тест, 11995 утверждений, один провал вне этапа (CommentIsolationTest на modules/presentation/index.php), 9 пропусков; `phpstan analyse` — без ошибок; `php-cs-fixer check` по изменённым PHP — чисто; `npm run ui:gates` — ui-audit, markup 0, 195 тестов; `php -l` всех затронутых. HTTP в браузере (Playwright, 13 из 13): экран правил 14×3 поля и переключатель, отказ периода 30, сохранение visit 0→2→0 с перечитанными значениями, баланс только для чтения, adjust +7 и −7 со строками журнала (aid, note), отказ без причины и отказ «+07» с видимым текстом, фильтры журнала. Не выполнено — см. остаток в блоке передачи |
| S05 — Rating — подключение | не начат | — |
| S06 — Field — класс и остающиеся потребители | не начат | — |
| S07 — Feed — класс и RSS-потребители | не начат | — |
| S08 — Node — схема, DTO и загрузка | не начат | — |
| S09 — NodeQuery — чтение | не начат | — |
| S10 — NodeService — типы и настройки | не начат | — |
| S11 — NodeService — материалы и ресурсы | не начат | — |
| S12 — Файлы и кеш Node | не начат | — |
| S13 — Node — HTTP и представление | не начат | — |
| S14 — Комментарии и NodeSupport | не начат | — |
| S15 — NodeSync | не начат | — |
| S16 — Остальные интеграции Node | не начат | — |
| S17 — Все десять профилей | не начат | — |
| S18 — Целевая поставка и итоговая приёмка | не начат | — |

## Передача следующему окну

- Этап: S04 — Point — подключение, продолжение под тем же ID. Код и обновление стенда сделаны; остаток — приёмка из списка ниже. S05 не начинать, пока остаток не закрыт.
- Прочитать: CLAUDE.md и .rules/global.md; карточку S04 в 14-roadmap.md; в points.md — «Приёмка S04 — подключение», «Заметки реализации S04» и «Карта остающихся владельцев»; tests/Support/point_probe.php как образец одноразовой схемы.
- Остаток S04, по пунктам:
  1. Общий сброс не проверен ничем, кроме `php -l`, phpstan и php-cs-fixer. По решению пользователя 2026-09-21 на живом стенде он не запускается (операция глобальна: 11848 балансов). Проверить на одноразовой схеме: больше 500 аккаунтов с положительным балансом, аккаунт с балансом больше 1000000 (несколько частей), обрыв посреди партии и повтор с тем же ID операции без двойного списания, удаление состояния из сессии после завершения. setPointsReset() живёт в modules/account/admin/index.php и зависит от `$_SESSION`, `$admin`, `$pnt` — пробе нужен админ-контекст либо функция выносится по согласованию.
  2. auto_links по HTTP не проверен: на стенде в `_auto_links` нет ни одной строки. Нужен реальный переход с проверкой `outs` и события visit — при стартовой нулевой награде (строки журнала нет, `outs` растёт), при ненулевой (одна строка `link:<id>`), при повторном переходе (второй строки нет).
  3. По HTTP не проверены остальные владельцы: poll, тема и сообщение форума с одобрением и удалением, favorite, privat, contact, recommend, order activate/деактивация/удаление, shop clientset/clientsave/clientdel, login и register (включая OAuth). Comment проверен на реальной БД через contract_probe (начисление, скрытие без движения, компенсация при удалении), но не через HTTP.
  4. Ветки возобновления блока update6_3 не исполнялись: состояния prepared и applying, verified без отметки, остановка при строках журнала без manifest. Исполнены только чистый путь (дважды) и отказ preflight на открытом сайте. Отказ preflight по версии сервера и по не-InnoDB не исполнялся.
  5. Новых тестов баллов не написано: существующие приведены к контракту (CommentIsolationTest — два теста переписаны на отсутствие слотов и позиционных настроек, CommentStateTest — точная компенсация при удалении скрытого комментария, RatingTest — без проверки class_exists('Point'), SchemaUpdateValidationTest — имя после CONSTRAINT, contract_probe — ожидания из points.actions). Экран правил, adjust, журнал и обновление покрыты только ручным HTTP-прогоном; сценарий удалён вместе с c:\tmp, в дереве его нет.
  6. Пара `npm run ui:before` / `ui:after` не снималась: CSS и фрагменты тем не менялись, но изменились экраны admin groups&op=points, форма пользователя, страница правил modules/users и колонка баллов в списке. tools/ui-shots.json не правился.
  7. Открытый вопрос без решения: без отметки update.points класс пишет в error_site.log строку на каждый запрос (на стенде 286 строк за окно между правкой ядра и обновлением). Троттлинг или иная диагностика не вводились.
  8. Открытый вопрос без решения: preflight требует `close = 1`, а поставочный config/global.php несёт `close = 0`. При настоящем обновлении 6.2 → 6.3 файлы поставки ложатся поверх сайта раньше, чем можно открыть админку 6.3, поэтому закрыть сайт получится только ручной правкой config/global.php. Так требует 12-migration.md; сценарий на настоящей базе 6.2 не исполнялся.
  9. Подписи 15 действий (`_POINTS_PUBLISH` … `_POINTS_ADJUST`) читаются только через `constant('_POINTS_'.strtoupper($name))` в admin/modules/groups.php и modules/users/index.php: поиск по имени их не находит, при чистке словарей они выглядят неиспользуемыми. Пагинация журнала (больше 50 строк) не исполнялась: в журнале стенда шесть строк.
- Изменённые файлы окна: core/system.php (bootstrap `$pnt`, setHead, getRatingView, updateVotingResult; удалены updatePoints и addPointsAction), core/classes/comment.php (конструктор с Point, MODULES без слотов, updateTargetPoints), core/user.php, core/helpers.php, blocks/user_info.php; admin/modules/groups.php (points, pointssave, новая getPointsJournal); modules/account/index.php (activate, setUserLogin, oauthfinish) и admin/index.php (форма пользователя, addsave, новая setPointsReset, resave, настройки без users.point; показ отказов формы через `messages` — раньше массив уходил в `text` и любой отказ формы выводился пустым); modules/users/index.php, forum/index.php, contact/index.php, recommend/index.php, auto_links/index.php, order/index.php и admin/index.php, shop/index.php и admin/index.php; config/users.php (без point и points); setup/index.php (checkUpdateBase, setUpdateBackup, setUpdatePoints, отметки чистой установки, удаление local.php); setup/sql/table.sql и table_update6_3.sql (`_order.uid`, `_points`, строка совместимости); 17 констант `_POINTS_*` в lang/*.php вместо 90 удалённых и 9 констант `_POINTS_*` в admin/lang/*.php; тесты — SchemaUpdateValidationTest, CommentIsolationTest, CommentStateTest, RatingTest, tests/Support/contract_probe.php; справка админки — раздел «Пункты» в admin/info/groups/ru.md и две строки о баллах в modules/account/admin/info/ru.md приведены к текущему экрану. В docs/node: points.md — два решения пользователя 2026-09-20, решение 2026-09-21 и «Заметки реализации S04». Файлы S01–S04 закоммичены 2026-09-21 одним общим коммитом вместе с правками demo/; config/security.php (только secret стенда) и config/update.php (отметка стенда, теперь в .gitignore) в коммит не входят.
- Состояние стенда: config/update.php с points = 6.3.0 и storage/backup/update/points/ (manifest.json, balances.json) созданы обновлением и в git не входят; config/points.php, modules.php, scheduler.php, newsletter.php, global.php, security.php и users.php после прогонов равны состоянию до обновления, у config/db.php установщик нормализовал только заголовок файла. В `_points` шесть строк adjust с note «S04 acceptance plus/minus» для uid 7885 — три прогона приёмки, баланс 45575 не изменился; журнал неизменяем, строки оставлены. Полный дамп БД до обновления — storage/backup/slaed_pre-s04_2026-09-20_22-33-38.sql (28 МБ, вне git); удалить, когда не нужен. `_favorites`.`time` добавлена штатным обновлением: ошибка избранного из передачи S03A закрыта.
- Журналы: error_php.log — три TypeError `Comment::__construct()` в 23:11:48–23:11:56, окно между правкой core/system.php и comment.php в этом же окне, после — записей нет; error_sql.log — последняя запись 22:32:30, прежняя ошибка `_favorites`.`time` до обновления; error_site.log — 286 строк «Point … switched off» до появления отметки (последняя 23:35:40) и фикстуры Mail-тестов из прогона phpunit.
- Вне этапа: провал CommentIsolationTest::commentTableIsReachedThroughItsClassAlone на демо-строке `_comments` в modules/presentation/index.php существовал до S01; config/security.php хранит сгенерированный secret — перед коммитом обнулить `'secret' => ''`; demo/* изменены не этим планом. Остальные пометки для S05, S07, S13 и S16 из передачи S03A действуют без изменений: для S05 — в RatingTest приостановлены thePageCacheFollowsTheJournal и thePageIsFilledAndReadByGenerationAndMarkers; для S13 — вернуть маршруты в `$ops` и `$allow`, снять оба markTestSkipped, положительный случай в PageCacheContractTest::cleanRouteProducesStableIdentity, маршруты Node в tools/ui-shots.json, seo-audit.php и upload-route-check.php, комнаты редактора типов, слот `faqtitle` темы lite, подписи уровней кеша; для S16 — показатели presentation и плитки монитора от NodeQuery, константы `_PRES_*` сохранены; для S07 — getRssChannel() и modules/rss знают только shop. tools/label-audit-baseline.json не перезаписан; docs/PAGE-CACHE-ROUTES-2026.md заморожен до конца S18 (NOD-231).

Для завершённого этапа в таблице указываются команды и результат либо ссылка на сохранённый отчёт/тестовый артефакт. Если коммит уже создан по указанию пользователя, можно указать hash; создание коммита не является условием передачи. Незавершённое записывается явно, без статуса «готово».
