# Состояние реализации

Единственная точка входа. Команда пользователя во всех окнах одна: «Работай по плану docs/node/PROGRESS.md». Она означает: выполнить этап из блока «Передача следующему окну» по протоколу ниже. Закрытые задачи проектирования не означают выполненную реализацию.

## Протокол окна

1. Текущий этап — тот, что назван в блоке «Передача следующему окну»; таблица ниже его подтверждает. Этап не выбирается по памяти, по истории чата или по желанию исполнителя.
2. Прочитать раздел «Вход в каждое новое окно» и карточку этапа в [14-roadmap.md](14-roadmap.md), затем документы из строки «Прочитать» блока передачи. Остальное не читать.
3. Проверить git status и фактическое состояние зависимостей по коду и артефактам, а не по записям предыдущего окна. Расхождение с записью — сначала в блок передачи, потом работа.
4. Выполнить ровно один этап. Следующий этап в том же окне не начинать, даже если остались силы и контекст.
5. Развилка, которую не решает план, задаётся пользователю через AskUserQuestion; молчаливый выбор запрещён. Ответ вносится в документ-владелец до продолжения.
6. Закрыть окно: обновить строку таблицы (состояние, команды проверок и их результат), затем целиком заменить блок передачи — следующий этап, что прочитать, изменённые файлы, выполненные проверки, остаток. Незавершённый этап остаётся в блоке под тем же ID с точным остатком. Отчёт — по .rules/report.md. Коммит — только по команде пользователя.
7. Блок передачи всегда указывает этап, с которого начнёт следующее окно; после S20.8 он сообщает, что план выполнен. Окно без обновлённого блока считается незакрытым.

## Этапы

| Этап | Состояние | Результат и проверка |
|---|---|---|
| S00 — Правка плана по аудиту готовности | выполнен 2026-09-19 | Находки сверки перенесены в документы-владельцы и карточки, решения NOD-224–NOD-235, партия H в TODO.md, AUDIT.md удалён. Контроль документации: UTF-8, один корневой заголовок, парность блоков кода, локальные ссылки, последовательность NOD, git diff --check по docs/node — пройден |
| S01 — Point — класс | выполнен 2026-09-19 | core/classes/point.php, config/points.php, DDL `_points` и строка совместимости в table.sql, tests/Unit/PointTest.php с tests/Support/point_probe.php на одноразовой схеме MariaDB 11.7. `php vendor/bin/phpunit --filter PointTest` — 18 тестов, 149 утверждений, пройден; `php -l`, phpstan, `php-cs-fixer check` по новым файлам, `php tools/ui-audit.php`, `--markup` и шесть фильтров ui:gates — пройдены. Полный phpunit: 958 тестов, один провал вне этапа — CommentIsolationTest на неизменённом modules/presentation/index.php |
| S02 — Rating — класс | выполнен 2026-09-20 | core/classes/rating.php; guard, checkWriteGuard и addEpoch(force) в core/classes/cache.php; маркеры и поколение в checkPageCache, getPageHash и setFoot в core/system.php; три таблицы Rating в table.sql; tests/Unit/RatingTest.php с tests/Support/rating_probe.php на одноразовой схеме MariaDB 11.7 и scratch-кеше. `php vendor/bin/phpunit --filter RatingTest` — 22 теста, 242 утверждения, пройден; `php -l`, phpstan, `php-cs-fixer check` по изменённым файлам, `npm run ui:gates` (ui-audit, markup, 195 тестов) — пройдены; HTTP стенда: news и главная 200, error_php.log без новых записей. Полный phpunit: 981 тест, один провал вне этапа — CommentIsolationTest на неизменённом modules/presentation/index.php |
| S03 — Конфигурация и файловые блокировки | выполнен 2026-09-20 | core/system.php: getConfig(fresh), setConfigFile(string\|Closure): bool, getConfigCode, setConfigSource, getConfigJournal, setConfigRestore; повторно входимые getPathLock()/deletePathLock() в core/classes/filemanager.php; op=restore и предупреждение о маркере в admin/modules/config.php, восемь констант `_CONFIG_*` в шести admin/lang; писатели fields.php, uploads.php, ratings.php на форме Closure; tests/Unit/ConfigFileTest.php с tests/Support/config_probe.php на scratch-копии config/, дополнение FileManagerLockTest. `php vendor/bin/phpunit --filter "ConfigFileTest\|FileManagerLockTest"` — 20 тестов, 185 утверждений, пройден; `php -l`, phpstan, `php-cs-fixer check` по изменённым файлам, `npm run ui:gates` (ui-audit, markup, 195 тестов) — пройдены. HTTP стенда: экран restore в чистом состоянии, сохранение uploads без изменений, настоящий погибший писатель по fields.php — предупреждение на экране настроек, отказ сохранения ratings, диагностика, отказ поддельного токена, POST-восстановление, исходники байт в байт; error_php.log, error_sql.log и error_site.log без новых записей. Полный phpunit: 995 тестов, один провал вне этапа — CommentIsolationTest на неизменённом modules/presentation/index.php |
| S03A — Удаление девяти старых модулей | выполнен 2026-09-20 | Удалены modules/{news,pages,faq,help,jokes,content,links,files,media}/, их config/*.php и записи config/modules.php, десять блоков, сид `_news` с категориями и опросом, девять таблиц в table.sql и 131 оператор с восемью заголовками в table_update6_3.sql; ключи девяти разделов убраны из config/fields.php, uploads.php, ratings.php и search.php; главная — presentation, карта HTML-кеша пуста до S13; общие helpers сведены к остающимся модулям, setContentActive() и место `files.dist` удалены; Comment::MODULES — account, shop, voting; несущая цель comment-тестов и contract_probe — shop. `php vendor/bin/phpunit` — 991 тест, 11979 утверждений, один провал вне этапа — CommentIsolationTest на демо-строке `_comments` в modules/presentation/index.php, девять пропусков, из них два новых — сценарии RatingTest с кешируемым маршрутом до S13; `php vendor/bin/phpstan analyse` — без ошибок; `php-cs-fixer check` по изменённым PHP — чисто; `npm run ui:gates` (ui-audit, markup, 195 тестов) — пройдены; `php -l` всех затронутых PHP — чисто. Пара `npm run ui:before` / `ui:after`: до — 193 кадра, после — сдвинулись только ожидаемые страницы: front (главная теперь presentation), presentation (пять фактов, одна карточка пульса), profile (вкладки ленты), все светлые админ-страницы (меню без девяти модулей, монитор без двух плиток); forum-list, forum-topic, voting, login, oauth-finish, settings, favorites и private в тёмной схеме без сдвигов; не сравнены тёмные админ-страницы (в проходе не удался вход в админку) и светлые страницы участника (их нет в наборе «до»); новое состояние forum-topic-button-hover заменило article-button-hover; tools/ui-contrast.json перегенерирован, 547 пар. HTTP стенда: главная, forum, shop, voting, users, search (GET и POST), rss, sitemap — 200; `name=news`, `name=files`, `name=content` — 404; error_php.log и error_site.log без записей от кода этапа, в error_sql.log только прежняя ошибка стенда — у `_favorites` нет колонки `time` (в журнале с 2026-09-15) |
| S04 — Point — подключение | выполнен 2026-09-21 | Код: `updatePoints`, `addPointsAction`, `users.point`, `users.points` и константы `_POINTS01`–`_POINTS45`, `_DESC01`–`_DESC45` удалены вместе с вызовами, все владельцы карты points.md вызывают `$pnt->addEvent()`/`getEventId()`; `_order.uid` и `_points` в table.sql и table_update6_3.sql; ветка update6_3 с preflight, закрытием сайта, снимком, manifest и отметкой config/update.php; экран правил и журнал в admin/modules/groups.php, adjust и сброс в modules/account/admin/index.php. Приёмка второго окна нашла и закрыла три дефекта: scope `auto_links` не проходил грамматику класса — переход никогда не начислялся, scope стал `auto-links`; ответ форума, удалённый через корзину, не компенсировался (перенос обнуляет pid) — окончательное удаление ищет `topic:` и затем `post:`; форма правки клиента магазина затирала `_clients.uid` ID партнёра и снимала награду с заказчика. Решения пользователя 2026-09-21: пустая область Point не журналируется; update6_3 закрывает сайт сам, preflight по close снят. Тесты: PointTest — общий сброс на одноразовой схеме (две партии по 500, баланс 2500000 тремя частями, отказ коммита, коммит без ответа, повтор с тем же ID без двойного списания, очистка сессии) и пустая область без записи в журнал; UpdatePointsTest с tests/Support/update_probe.php — чистый путь, повтор, verified без отметки, prepared, applying, строки журнала и отметка без manifest, подменённый снимок, неполный points.php, preflight по версиям 10.2.0/10.2.1/8.0.15/8.0.16 и по не-InnoDB; PointOwnersTest — карта владельцев, отсутствие старых helpers и настроек, оценки и просмотр без Point, 15 подписей действий в шести локалях. `php vendor/bin/phpunit` — 1003 теста, 15995 утверждений, один провал вне этапа (CommentIsolationTest на modules/presentation/index.php), 9 пропусков; `phpstan analyse` — без ошибок; `php-cs-fixer check` по изменённым PHP — чисто; `npm run ui:gates` — ui-audit 0, markup 0, 195 тестов; `php -l` всех затронутых. HTTP в браузере (Playwright, артефакт storage/backup/s04-acceptance_2026-09-21.json вне git: 74 записанных шага, 57 принятых; остальные 17 — первые прогоны до исправления кода или сценария, каждый повторён позже, и один шаг s7c, помеченный как невыполненный): auto_links — нулевая награда растит `outs` без строки, награда 2 даёт одну строку `link:<id>`, повтор и гость строк не дают; login (одна строка на день UTC), contact, recommend, poll с повторным голосом, privat, favorite; форум — тема, ответ, скрытие и одобрение модератором без второй награды, корзина без движения, окончательное удаление ответа и темы с компенсацией автору, повторные удаления; comment — премодерация без награды, одобрение +5, скрытие без движения, мягкое удаление −5; order — создание активным без награды, 0→1 +10 с aid, 1→0 −10, повторное 0→1 без награды, удаление подтверждённого −10; shop — новый активный клиент, clientset в обе стороны, clientdel, вход и уход из статуса 1 через форму правки; register — активация +2 новому аккаунту, повтор без строки; журнал — 50 строк на странице, вторая страница, фильтр uid + действие; update6_3 на открытом стенде — сайт закрыт веткой (гость получает 503), строка отчёта, manifest и снимок не тронуты. error_php.log без записей; error_sql.log — только старая ошибка пейджера клиентов магазина; error_site.log — 404 темы форума из-за отставания часов БД и отказы mail() стенда, строк Point нет. Не проверено: вход и регистрация через OAuth (живых учётных данных провайдеров на стенде нет), 20-секундный бюджет одного запроса сброса, начисление записи форума, созданной скрытой (нужен премодерируемый участник), оформление заказа магазина с сайта (дефект вне этапа), пара ui:before/ui:after — CSS и фрагменты тем не менялись, а состояние «до» невоспроизводимо: S01–S04 лежат одним коммитом и база стенда уже обновлена |
| S05 — Rating — подключение | выполнен 2026-09-21 | Код: `getRatingService()` и POST-обработчик `getRatingView()` в core/system.php (метод, затем токен из тела, пять полей по регулярным выражениям, коды 405/403/422/404/409/429 с Retry-After/500), самозащищённый вход в index.php; `getRatingAsync()` и чтения правил в core/user.php и modules/account/index.php на формате четырёх ключей за отметкой `update.ratings`; rating-фрагменты lite на `hx-vals` без адреса с параметрами, ключ доставки и warn-тост в plugins/system/slaed.js, правило `.sl-toast-warn` в theme.css (решение пользователя 2026-09-21: отказ показывает тост-предупреждение); admin/modules/ratings.php — форма четырёх ключей, журнал `op=votes`, аннулирование `op=annul`; массовый сброс оценок удалён из modules/account/admin/index.php (NOD-228); блок `setUpdateRatings()` ветки update6_3 и три таблицы в table_update6_3.sql, preflight InnoDB дополнен таблицами Rating и `_products`, `opcache_invalidate()` в писателе установщика; шесть констант `_RATINGS_*` в шести lang и девять в шести admin/lang; config/ratings.php в формате четырёх ключей. Тесты: UpdateRatingsTest на tests/Support/update_probe.php (второй аргумент `ratings`: чистый путь на 1205 целях в трёх партиях, повтор после нового голоса, verified без отметки, уже преобразованные правила с `guests = 0`, prepared и applying, строки и отметка без manifest, подменённый снимок, строка и агрегат владельца вне снимка, сводный отчёт preflight без единой записи, порядок блоков и DDL); RatingOwnersTest — старая `_rating` только у опросов, обработчик не читает адрес, правила за отметкой, сброс удалён, 15 констант в шести локалях; CommentIsolationTest — строка карты рейтинга под новую карту. `php vendor/bin/phpunit` — 1016 тестов, 16210 утверждений, один провал вне этапа (CommentIsolationTest::commentTableIsReachedThroughItsClassAlone на modules/presentation/index.php), 9 пропусков; `phpstan analyse` — без ошибок; `php-cs-fixer check` по изменённым PHP — чисто; `npm run ui:gates` — ui-audit 0, markup 0, 195 тестов; `php -l` всех затронутых, `node --check` slaed.js. Обновление на стенде: дамп storage/backup/slaed_pre-s05_2026-09-21_12-58-25.sql, прогон update6_3 через setup.php — без красных строк, 14099 целей (11848 + 2222 + 29) и 5 сроков, суммы base/votes по трём областям равны суммам агрегатов владельцев, `_rating_votes` пуста, строка voting на месте, config/ratings.php преобразован блоком, отметки points и ratings; повторный прогон не изменил ни одного файла снимка. HTTP-приёмка (Playwright и настоящие POST, артефакт storage/backup/s05-acceptance_2026-09-21.json вне git, 50 шагов из 50 с первого полного прогона; первый запуск упал до первого шага на ожидании networkidle и ничего не записал): GET 405 с Allow и no-store; без токена и с чужим токеном 403; дробь, 0, 6, текстовый ID, scope вне грамматики и в верхнем регистре, короткий и прописной request, неверный и пустой typ — 422; старая форма с query-параметрами 422; клик по виджету форума сохраняет голос, шлёт 32-hex ключ и заменяет блок; повтор ключа 200 без записи, тот же ключ с другим значением 409, новый ключ 429 с Retry-After; отказной клик оставляет виджет и показывает warn-тост; отсутствующая тема, ответ форума, скрытая тема и скрытый товар — 404 без счётчиков; гость оценивает аккаунт и товар; свой профиль и своя тема 403; аккаунт голосует по цели, уже оценённой гостем того же адреса, его второй голос 429, после выхода гостевой срок адреса действует; журнал цели, аннулирование без токена без изменений, аннулирование формой снимает ровно значение голоса, пишет aid и причину и не меняет last, повтор пуст, интервал голосовавшего не открывается; дробные сутки отклонены, форма сохраняет нулевой период и закрытых гостей, гость получает 403, аккаунт при нулевом периоде голосует дважды, правила возвращены; поля сброса оценок в форме account нет; после аннулирования всех тестовых голосов агрегаты равны исходным; журнал и баланс Point и `_rating` с голосом опроса не изменились; три журнала ошибок за время приёмки не выросли. Пара `npm run ui:before` / `ui:after`: «до» снято на HEAD-версии core/helpers.php, 160 кадров; «после» — forum-list, forum-topic с виджетами, voting, login, oauth-finish, светлые profile, settings, favorites, private и все снятые админ-страницы без сдвигов; front и presentation сдвинулись на 0,32–0,54 % — страница печатает живые показатели (визиты дня и месяца, сессии, события безопасности за сутки), между наборами прошли обновление и приёмка с десятками отказов 403; счётчики ui-audit не выросли. Не сравнены: тёмные profile, settings, favorites, private (в проходе «до» не удался вход участника в тёмном контексте, 16 MISSING) и админ-страницы одного из двух контекстов «после» (не удался вход администратора). Не проверено: сбой хранения 500 и сохранение ключа после обрыва сети — только тестами класса S02 и чтением кода; голос премодерируемого участника; вход через OAuth |
| S06 — Field — класс и остающиеся потребители | выполнен 2026-09-21 | Код: `final class Field` в core/classes/field.php (шесть публичных методов, без конструктора и состояния, закрытый реестр десяти типов, одна нормализация за проверкой и фильтром, шесть кодов отказа, пределы 256/256/64/4096/262144/254/2048/1 МиБ), `$fld = new Field()` в core/system.php; в core/helpers.php `getFieldRules()` и `getFieldsPost()` за отметкой `update.fields`, переписаны `getFieldsInRows()`, `getTplFieldsIn()`, `getTplAddFieldRows()`, `getTplViewFieldRows()`; формы, запись и вывод account, forum и order на JSON, ручной построитель формы в modules/account/admin/index.php удалён, фильтр `field` снят с `getVar()`, комната `order.info` снята с карты редактора; admin/modules/fields.php — области из `getFieldAreas()`, структурные определения, варианты таблицей, отказ с путём первой ошибки и возвратом отправленного текста, запись Closure-формой `setConfigFile()`; шесть `_FIELDS_*` в шести lang, двенадцать `_FIELDS_*` и новый текст `_FIELDINFO` в шести admin/lang, `_FIELDOUT` удалён; слот подсказки в templates/lite/fragments/form-field-row.html; блок `setUpdateFields()` с `getUpdateRules()` и `getUpdateValue()` и параметр `$raw` писателя в setup/index.php; `MEDIUMTEXT` для `_users.field`, `_forum.field`, `_order.info` в table.sql и в сверочных `MODIFY` table_update6_3.sql. Решения пользователя 2026-09-21: строка `0` в позициях select, date и datetime — заглушка пустого выбора и переходит в отсутствие значения; определения стенда восстановлены из данных, у заказа 261 значения 3 и 4 соединены; поставочный config/fields.php — демо-определения в новом формате. Приёмка нашла и закрыла три дефекта: сверочные `ALTER … MODIFY` файла обновления возвращали трём колонкам `TEXT` после расширения (и сужали бы их при каждом повторе); preflight возвращался с незакрытой транзакцией чтения, чья блокировка метаданных держала последующий DDL; экран полей отклонял `bool` без значения по умолчанию путём `<имя>.default`. Находка чтения кода: `Parser::filterContent()` с `safe = false` пропускает HTML, поэтому `getFieldView()` вызывает парсер в безопасном режиме — значения теперь хранятся необработанным текстом. Тесты: FieldTest — 135 тестов (форма класса, реестр, 51 отказ определений с путём, 76 граничных значений одним набором для проверки и фильтра, пределы 64 и 1 МиБ, константы в шести локалях); FieldViewTest — форма из общих фрагментов, массив вывода по контракту, безопасный режим парсера, владельцы только через общие помощники; UpdateFieldsTest с tests/Support/update_probe.php (второй аргумент `fields`: чистый путь на 1202 строках в трёх партиях, повтор, verified без отметки, именованные определения без строк, возобновление с курсора 0, 500 и 1000, отметка без manifest, именованные определения поверх позиционных строк, подменённый снимок, чужая строка, сводный отчёт шести битых строк и четырёх битых определений без записи, ширина колонок, порядок блоков); EditorRoomTest — без `order.info`. `php vendor/bin/phpunit` — 1165 тестов, 17494 утверждения, один провал вне этапа (CommentIsolationTest::commentTableIsReachedThroughItsClassAlone на modules/presentation/index.php), 9 пропусков; `phpstan analyse` — без ошибок; `php-cs-fixer check` по изменённым PHP — чисто; `npm run ui:gates` — ui-audit 0, markup 0, 195 тестов; `php -l` всех затронутых, `node --check` скрипта приёмки. Обновление на стенде: дамп storage/backup/slaed_pre-s06_2026-09-21_14-50-06.sql, два прогона update6_3 через setup.php без красных строк — 7677 аккаунтов, 13923 сообщения форума, 843 заказа; после второго прогона колонки `mediumtext`, ни один файл снимка и config/fields.php не изменились; сверка: 306 + 3006 + 843 непустых значений — JSON-объекты только из известных имён, 7371 строка аккаунтов состояла из одних заглушек. HTTP-приёмка (Playwright, артефакт storage/backup/s06-acceptance_2026-09-21.json вне git, 25 шагов из 25 на четвёртом прогоне; первые три прогона падали на селекторах сценария и на третьем из названных дефектов, каждый упавший шаг повторён): форма заказа — девять именованных элементов; отказ обязательного поля на сервере без записи и с возвратом введённого; разметка и кавычки сохраняются необработанным JSON и выводятся экранированными, текстовый `0` сохраняется; правка заказа в админке заменяет активные значения и удаляет опустошённое; экран полей — девять полей с запертыми именами, отказ `Bad-Name` с путём и возвратом текста без изменения файла, новый `bool` с нативными типами, скрытый `0` перед флажком и подсказка с `aria-describedby`, `false` и `true`, отключённое поле уходит из формы и сохраняет значение через запись, удалённое определение очищает значение при следующей записи, файл после удаления байт в байт равен опубликованному установщиком; профиль — отказ обязательного поля в своём разделе, ключ варианта и текст в JSON, подпись варианта в публичном профиле, поддельный вариант — отказ `choice`, форма аккаунта в админке сохраняет без изменений; форум — три ключа вариантов и их подписи в теме, очистка всех выборов даёт пустое значение; без отметки формы без полей, запись сохраняет прежний текст, строка диагностики в журнале сайта, экран полей без формы; error_php.log и error_sql.log не выросли, в error_site.log — только отказы mail() стенда и ожидаемая строка диагностики. Пара `npm run ui:before` / `ui:after`: «до» снято на HEAD до первой правки, 160 кадров (вход участника в тёмном контексте не удался — 16 кадров profile, settings, favorites, private без базы); «после» — login, oauth-finish, forum-list, voting, светлые profile, settings, favorites, private и снятые админ-страницы без сдвигов (в проходе «после» не удался один вход администратора); forum-topic сменил размер во всех 16 кадрах — тема 16304 несёт два сообщения со значениями полей, прежние демо-определения показывали по две строки «Test»/«Test2», определения стенда показывают по три с подписями; элементов формы полей и подсказок на странице нет, CSS не менялся; front (21 кадр) и presentation (8 кадров) сдвинулись на 0,34–0,53 % — живые показатели страницы, находка (ж). Слот подсказки form-field-row.html без подсказки ничего не выводит; страница с подсказкой в наборе рига отсутствует, её разметка проверена шагом приёмки. Счётчики ui-audit не выросли. Не проверено: запись полей при регистрации (форма регистрации полей не показывает, колонка получает пустую строку), множественный `select`, `int`, `decimal`, `date`, `url` и `email` в браузере — только тестами класса; предел 1 МиБ — только тестами класса; время блока на стенде не замерялось |
| S07 — Feed — класс и RSS-потребители | выполнен 2026-09-23 | Код: `final class Feed` в core/classes/feed.php (конструктор `array $conf, ?Closure $send = null` и один публичный `getFeedContent()` с семью ключами; транспорт `resolve`/`get`, штатный — DNS системы и один закреплённый cURL-запрос без перенаправлений и прокси со сверкой `CURLINFO_PRIMARY_IP`; пределы `bytes`, `timeout`, `redirects`, `max` целыми десятичными до запроса; валидаторы только первым переходом; XML без DTD и сущностей, HTML описаний через `Dom\HTMLDocument`; канонический Markdown с экранированием всей ASCII-пунктуации); `Upload::checkPublicAddress()` стал публичным статическим — одна политика адресов на Upload и Feed (решение пользователя 2026-09-23); Parser — правило литерала `\`+пунктуация (`LITERAL`, `filterLiteral()`, литеральный токен `L`) во всех режимах кроме сырых областей (код Markdown, `[code]`, `[php]`, `[usephp]`, `[usehtml]`, HTML-тег, script/style), экранированная обратная кавычка не открывает код, общая грамматика `ATTACH`/`ATTSIZE`/`ATTREL` и публичный `getAttachList()`; в core/system.php `rss_read()` удалён, `rss_load()` стал `getRssBlock()` (безопасный режим, при сбое тело сохраняется, время сдвигается), новые `getRssBody()` (предел колонки 65535) и `getRssView()`, `getBlocks()` не рендерит RSS-блоки в доверенном режиме; пять вызовов переведены — admin/modules/blocks.php (добавление и правка, правка при сбое сохраняет тело), modules/account/index.php, modules/rss/index.php, показ блока; config/rss.php — `bytes`, `timeout`, `redirects` добавлены, `temp` удалён вместе с полем и сохранением в modules/rss/admin/index.php и константами `_RSSTEMP`, `_RSSTEMPINFO` в шести admin/lang; ветка update6_3 после `setUpdateFields()` дополняет config/rss.php сайта и обнуляет `content` и `time` RSS-блоков. Тесты: FeedTest — 39 тестов (форма класса; RSS 2.0 в windows-1251, Atom html/xhtml/text, RSS 1.0 байт в байт; `rss.max` и пустая лента; независимость от `temp`; враждебный текст и ссылки; условные запросы и валидаторы чужого адреса; три перенаправления и отказ четвёртого; частная цель на каждом переходе, смешанный ответ, rebinding при повторном разрешении, закрепление адреса; предел байтов и тайм-аут всей операции; девять неверных пределов без вызова транспорта; 14 адресов вне политики без вызова транспорта; битый XML, DOCTYPE/ENTITY, неверная форма ответа транспорта; владельцы, config/rss.php и ветка обновления); ParserFixturesTest — 17 фикстур литералов, 16 случаев одного набора тегов для `getAttachList()` и `filterAttach()`, порядок и уникальность, сырые BB-пары, враждебная лента через полный Parser в обоих режимах. `php vendor/bin/phpunit` — 1240 тестов, 17758 утверждений, один провал вне этапа (CommentIsolationTest::commentTableIsReachedThroughItsClassAlone), 9 пропусков; первый полный прогон дал ещё пять провалов StatsContractTest — прогон пересёк полночь, отдельный повтор и второй полный прогон чисты; `phpstan analyse` — без ошибок; `php-cs-fixer check` по изменённым PHP — чисто; `npm run ui:gates` — ui-audit 0, markup 0, 231 тест; `php -l` всех затронутых. HTTP-приёмка (Playwright и настоящие POST, артефакт storage/backup/s07-acceptance_2026-09-23.json вне git, 17 шагов из 17; первый прогон упал на гонке ожидания навигации в сценарии и повторён целиком; удаление тестовых блоков выполнено отдельным проходом — у формы удаления нет кнопки, её отправляет меню, поэтому отправлена та же форма страницы): добавление блока с частным адресом — `_RSSFAIL` без строки; с https://www.php.net/feed.atom — Markdown 31084 байта в `_blocks`.`content`; на странице форума 50 заголовков h4 и ссылки php.net без сырого Markdown и script; старый HTML в блоке и config/rss.php старого формата с `temp` → настоящий update6_3 через setup.php без красных строк: строка отчёта «rows: 2», `content` пуст, `time` = 0, config/rss.php дополнен и байт в байт равен поставочному; следующий показ заполнил блок Markdown; частный адрес при устаревшем блоке — тело сохранено, время сдвинуто; правка блока загрузила ленту заново; account — заголовок «Информация с сайта» и h3 записей, адрес 169.254.169.254 — предупреждение; модуль rss — лента и предупреждение для 10.1.2.3; настройки rss без поля шаблона, сохранение сохраняет три предела; три журнала ошибок не выросли. Штатный транспорт отдельно: редирект http→https php.net, 304 по полученным валидаторам, отказ 127.0.0.1 и 10.0.0.1 до соединения. Пара `npm run ui:before` / `ui:after` не снималась: CSS и фрагменты тем не менялись, а вывод Parser меняется только для пар `\`+пунктуация, которых на снимаемых страницах нет. Не проверено: сверка `CURLINFO_PRIMARY_IP` при настоящем rebinding (только чтением кода; сценарий повторного разрешения — тестами), прерывание потока на пределе байтов и тайм-аут штатного транспорта на живой сети, IDN-хосты вживую, лента больше 65535 байт в блоке |
| S08 — Node — схема, DTO и загрузка | выполнен 2026-09-23 | Код: восемь таблиц Node в setup/sql/table.sql между `_newsletter` и `_oauth_temp` в порядке зависимостей (`_node_types`, `_nodes`, затем `_node_assets`, `_node_categories`, `_node_publish`, `_node_relations`, `_node_support`, `_node_sync`) с именованными индексами, FK и CHECK из 03; те же определения `CREATE TABLE IF NOT EXISTS` отдельным батчем «Batch Node» перед Cleanup в table_update6_3.sql; `_admins`.`modules` → `TEXT NOT NULL` в table.sql и правкой существующего `MODIFY` (второго нет). core/classes/node/: entity, type, typeinput, input, relation, asset, target, context (`final readonly`, конструкторы 05 байт в байт), status (`enum NodeStatus` с закрытой матрицей `MOVES` и `checkStatusMove(self $to): bool` — решение пользователя 2026-09-23, внесено в 02 и 05), exception (`final NodeException extends RuntimeException`, коды 1–5), extension (`interface NodeExtension`, девять методов), load.php (анонимный автозагрузчик по закрытой карте из 11 классов); core/classes/node/ext/load.php — `getNodeExtension()` с пустой картой (`''` → null, иначе INVALID); core/system.php подключает только node/load.php. NodeContext отклоняет противоречивый снимок (права без `aid`, `task` с личностью, повторы и чужие значения в `groups`/`mods`) — записано в 05 «Контекст запроса». Тесты: tests/Unit/NodeModelTest.php (46 тестов: форма восьми моделей, неизменяемость, полная матрица 5×5, коды ошибки, девять методов интерфейса, 20 противоречивых контекстов, карта load.php = файлы каталога, закрытая фабрика, порядок CREATE по REFERENCES в обоих файлах без FOREIGN_KEY_CHECKS, тождество DDL Node в table.sql и обновлении, один MODIFY `modules`, отсутствие modules/node/sql); tests/Support/node_probe.php — загрузка после настоящего bootstrap (только load.php, первый `NodeContext` грузит context.php, будущие и подложные имена не грузят ничего) и две одноразовые базы MariaDB 11.7.2: чистая установка всего table.sql при `foreign_key_checks = 1` без единой ошибки, индексы и ограничения ровно по 03, 31 попытка записи на строках — допустимые вставки и отказы 1062/1452/1451/4025, значения по умолчанию, каскад удаления материала по шести таблицам и обеим сторонам связи; установка без Node со старым `VARCHAR(255)` + весь table_update6_3.sql дважды — без ошибок, `SHOW CREATE TABLE` восьми таблиц и `_admins` совпадает с чистой установкой после каждого прогона, значение `modules` сохранено. UpdateFieldsTest: подсчёт `MEDIUMTEXT` колонок полей теперь исключает создания таблиц Node (иначе `_nodes.field` давал пятое вхождение). `php vendor/bin/phpunit --filter NodeModelTest` — 46 тестов, 354 утверждения, пройден; `php -l` всех затронутых, `phpstan analyse` — без ошибок, `php-cs-fixer check` по изменённым PHP — чисто, `npm run ui:gates` — ui-audit 0, markup 0, 231 тест. Полный phpunit: 1286 тестов, 18427 утверждений, один провал вне этапа (CommentIsolationTest), 9 пропусков. HTTP стенда: главная, forum, shop, rss, admin.php — 200, `name=news` — 404; error_php.log и error_sql.log не выросли, в error_site.log только ожидаемая 404. Не проверено: MySQL 8.0.16+ (на стенде только MariaDB); DDL на базе стенда не исполнялся — таблиц Node на стенде нет |
| S09 — NodeQuery — чтение | выполнен 2026-09-23 | Код: `final class NodeQuery` в core/classes/node/query.php и строка `'NodeQuery' => 'query.php'` карты load.php; `getNodeContext()` в core/system.php (группы тем же соединением, что `is_acess()`, `node` → `manage`, `node-<name>` → `mods`, `lang` = язык при `multilingual`, статический снимок на запрос, значения запроса не читаются). Решения пользователя 2026-09-23 (05 → «Заметки реализации S09», 02): единый валидатор настроек — публичный `NodeQuery::filterNodeSettings()`; проекция списка по настройкам типа (fields при активном поле, assets при активной роли, cids при categories, rels при related/tree, body — null); язык категории фильтрует только списки; модератор в одиночном чтении видит любое состояние и отключённый тип, списки и цели без `setNodeStatus()` — только опубликованное. Методы: типы (сборка из строки и `$conf`, память экземпляра, одно перечитывание `getConfig()` при расхождении версии), `getNode`/`getNodeContent`/`getNodeAsset`, список и счётчик с одинаковыми предикатами, фильтры категории/буквы/автора/состояния/периода/поиска/главной, сортировки с закреплёнными первыми, смешанные выборки UNION ALL с расширением на тип, цели ≤ 2 SQL, дерево, sitemap по курсору, `getNodeDeadline()`. Тесты: tests/Unit/NodeQueryTest.php (57 тестов с поставщиком данных) на режиме `query` пробы tests/Support/node_probe.php: одноразовая MariaDB 11.7.2 с полным table.sql, scratch-конфигурация с типами news/docs/files (профили 06), off, bad, ghost, probe, stale, drift, plain, bare, копия query.php байт в байт рядом с копией ext/load.php, отличающейся одной строкой карты (тестовое расширение `probe`), дочерний режим `context` на девять посетителей с поддельными `task/super/aid/mods/groups` в GET/POST; NodeModelTest — `NodeQuery` в карте, 13 файлов. `php vendor/bin/phpunit --filter "NodeQueryTest|NodeModelTest"` — 103 теста, 1068 утверждений, пройден (проба ~6 с); самопроверка окна нашла и исправила ужесточение: правило рейтинга или загрузок типа делало невалидным весь тип вопреки ratings.md — теперь блокируется только подсистема, доказано типом `bare` пробы; бюджеты: список без категорий 3, news/files 7, docs 6, полный материал 5, текст 2, ресурс 2, цели 2, deadline 1 + предвыборка; размер страницы число SQL не меняет. `php -l`, `phpstan analyse` — без ошибок, `php-cs-fixer check` по изменённым PHP — чисто, `npm run ui:gates` — ui-audit 0, markup 0, 231 тест. Полный phpunit: 1343 теста, 19164 утверждения, 9 пропусков, один провал вне этапа (CommentIsolationTest). HTTP стенда: главная, forum, shop, admin.php — 200, `name=news` — 404; error_php.log и error_sql.log не выросли, в error_site.log только ожидаемая 404. Не проверено: HTTP-маршрут с `getNodeContext()` (маршрутов Node нет до S13 — контекст проверен дочерними процессами пробы на настоящем bootstrap); MySQL 8.0.16+ |
| S10 — NodeService — типы и настройки | выполнен 2026-09-23 | Код: `final class NodeService` в core/classes/node/service.php (конструктор `Database, NodeContext, Field, Point`; `addNodeType`, `updateNodeType`, `updateNodeTypeStatus`, `deleteNodeType`, `addNodeTypeImport`) и строка `'NodeService' => 'service.php'` карты load.php; `NodeQuery::getNodeTypeExport()` и публичные `FORMAT`, `EXPORT`, `UPLOAD`, `ROLEDEF`; удержание типов из marker.json в NodeQuery; поставочный config/node.php (`types => []`); Node-proof в `setConfigRestore()` без аргумента и кнопка restore для `why = proof` в admin/modules/config.php; `getNodeTypeMap()` и `updateNodeTypePart()` в core/admin.php (решение пользователя 2026-09-23); области `node.<name>` в admin/modules/fields.php, правила типов в uploads.php и ratings.php через NodeService с `ver[<name>]` и сверкой отрендеренного списка, права `node` и `node-<name>` в admin/modules/admins.php с сохранением ключей неотображаемых типов; четыре константы `_NODE_*` в шести admin/lang. Решения пользователя 2026-09-23 (05 → «Заметки реализации S10»): пустые `uploads`/`rating` при создании — копия `uploads.all` и новое правило рейтинга; приёмка через update6_3 на стенде и HTTP. Тесты: tests/Unit/NodeConfigTest.php (14 тестов) на режиме `service` пробы tests/Support/node_probe.php — scratch-источники, backup, cache и корень загрузок, одноразовая MariaDB 11.7.2, дочерние процессы `crash` (гибель писателя до и после COMMIT на подклассе Database), `restore` и `race`: права, 19 отказов имени, имена длиной 1 и 20, создание с хранимыми отличиями и guard, изменение со всеми частями и 11 отказов, включение/повтор/выключение, роли с сохранёнными ресурсами (удаление роли, `link` поверх локального файла и повтора адреса, адреса, различные регистром), 16 отказов ввода, экспорт → импорт → равный тип, 16 отказов импорта, удаление с материалом/категорией/файлом и снятием `node-<name>`, восстановление по БД в обе стороны и отказ при чужой версии, гонка двух созданий; NodeModelTest — 14 файлов карты; NodeQueryTest — метод экспорта, сборка без повторного SELECT при разрешающем свежем снимке; tests/Support/config_probe.php не копирует поставочный node.php (сценарии S03 о рождении файла). `php vendor/bin/phpunit --filter "NodeConfigTest|NodeModelTest|NodeQueryTest"` — 117 тестов, 1603 утверждения, пройден; `php -l`, `phpstan analyse` — без ошибок, `php-cs-fixer check` по изменённым PHP — чисто, `npm run ui:gates` — 231 тест, пройден. Полный phpunit — 1357 тестов, 19728 утверждений, 9 пропусков, один провал вне этапа (CommentIsolationTest::commentTableIsReachedThroughItsClassAlone). Обновление на стенде: дамп storage/backup/slaed_pre-s10_2026-09-23_03-36-27.sql, update6_3 через setup.php без красных строк — восемь таблиц Node InnoDB; прогон закрыл сайт и изменил global.php, modules.php (убрал presentation), scheduler.php — config/ возвращён из копии. HTTP-приёмка (Playwright, артефакт storage/backup/s10-acceptance_2026-09-23.json вне git, 13 шагов из 13 на втором прогоне и повторно после самопроверки окна; первый упал на HTML5-валидации поля url формы администратора в сценарии): экран полей — вкладка типа, версия, запертое имя, новое поле через сервис (версия 2 в БД и node.php), устаревшая форма — `_NODE_STALE` без записи; загрузки — правило типа через сервис (версия 3), правило forum без изменений; оценки — правило `node.s10probe` (432000, версия 4); права — новый администратор с `node,node-s10probe`, повторное сохранение сохраняет `node-ghost`; restore — погибший до COMMIT писатель, тип удержан, экран показывает Node-proof и кнопку, POST восстанавливает старую сторону; удаление через сервис убирает четыре области и `node-s10probe`, экраны без областей Node; error_php.log и error_sql.log не выросли, в error_site.log только строка удержания типа. После приёмки config/ байт в байт как до неё, `_node_types` пуст, uploads/s10probe удалён. Не проверено: MySQL 8.0.16+; конкурентная загрузка файла во время удаления типа (блокировка корня взята, сценарий не воспроизводился); экраны на сайте без таблиц Node — только чтением кода (`checkSqlTable()`) |
| S11 — NodeService — материалы и ресурсы | выполнен 2026-09-23 | Код: core/classes/node/service.php — конструктор `Database, NodeContext, Field, ?Point = null, ?NodeExtension = null`; `addNode`, `getNodePreview`, `updateNode`, `updateNodeStatus`, `deleteNode`, `updateNodeViews`, `updateNodeComments`, `updateNodeRating`, `updateNodeAssetHits`, `updateNodeAssetReport`, `deleteNodeAssetReport`, `updateNodePublishList`, `updateNodeCategory`, `deleteNodeCategory` (решения пользователя и реализация — 05 → «Заметки реализации S11»); core/classes/node/query.php (`KINDS`, `RMODES` публичные); core/system.php (`checkUploadModer()` в шести upload helpers, `addNodePublishTask()`, `nodepublish` в `getSchedulerJob()`/`addSchedulerSystemJob()`); core/helpers.php (`getUploadPlaceView()`, хранилища `nodes.intro`/`nodes.body` в `getEditorRoomData()`, `getCategoryModules()` с типами Node); plugins/editors/toastui/driver.php; admin/modules/categories.php (save/change/delete категорий типов через сервис); config/scheduler.php и ветка update6_3 в setup/index.php (задание `nodepublish`); тесты — новый tests/Unit/NodeServiceTest.php, режим `material` в tests/Support/node_probe.php (одноразовая MariaDB 11.7, копии reader/writer с записывающим расширением, дочерние процессы гонок дерева, ссылки, очереди и сбоев COMMIT), правки NodeConfigTest, EditorWindowTest, EditorRoomTest, UploadIntegrationTest. Документы: 02, 04, 05, 08, 10, 11, 13. Проверки: `php vendor/bin/phpunit --filter NodeServiceTest` — 20 тестов, 673 утверждения; Node-набор (Service/Config/Model/Query) — 137 тестов, 2276 утверждений; `php -l`, phpstan, `php-cs-fixer check` — чисто; `npm run ui:gates` — 231 тест; полный phpunit — 1377 тестов, 20417 утверждений, 9 пропусков, один провал вне этапа (CommentIsolationTest). Бюджеты: смена состояния 5 / 6 с заданием (предел 6/7), физическое удаление 5 Node-запросов. Штатный запуск `nodepublish` на стенде — `success`; HTTP-приёмка экрана категорий на временном типе s11probe — 7/7, журналы без новых записей. Самопроверка окна (по команде пользователя «Проверь себя») нашла и исправила три дефекта: `checkUploadModer()` давал типу Node право модератора по оставшемуся ключу удалённого одноимённого модуля; trusted-теги `[usephp]`/`[usehtml]` не снимались при записи текстов, подписей ресурсов и полей не-главным автором; выключенная `features.poll` блокировала правку материала с уже сохранённым опросом — у каждого свой сценарий пробы; приёмка повторена после исправлений (storage/backup/s11-acceptance_2026-09-23.json) |
| S12 — Файлы и кеш Node | выполнен 2026-09-23 | Код: `getFileStream()` с `mime`/`inline`/`cached`/`$start`, одним `Range`, `If-Range`, `HEAD`, `304`/`416` и потоковым чтением в core/system.php; `Cache::setPrivateHeaders()`, `checkNotModified($mtime, $etag)`, `setPageUntil()` и `until` в sidecar, `setHead()`/`setFoot()` не отдают копию после срока и не пускают связанную страницу в браузерный кеш; `FileManager::getGuardFiles()` вместо трёх списков имён; `NodeService::getNodeFile()` (сохранённая ветка и предпросмотр NOD-199), `checkTypeGuard()` при включении типа (стражи + `getSchedulerFetch()`, только `403`/`404`); `Parser::filterDoc()`/`filterContent()` с `int $nid = 0`. Тесты: новый tests/Unit/FileStreamTest.php с tests/Support/web_probe.php (`php -S`); PageCacheContractTest, ParserFixturesTest, NodeQueryTest (бюджет построения кеша 4/8/7/8), NodeConfigTest (gate), NodeServiceTest (attach). `php -l` затронутых файлов, `php vendor/bin/phpstan` — без ошибок; `php-cs-fixer check --config=.php-cs-fixer.dist.php` по 18 файлам — 0 нарушений; `npm run ui:gates` — 232 теста, пройден; полный phpunit — 1393 теста, один провал вне этапа (CommentIsolationTest на неизменённом modules/presentation/index.php); HTTP стенда: главная и forum 200, error_php.log и error_sql.log без новых записей. Попадание HTML-кеша (0 SQL) — на маршрутах S13 |
| S13 — Node — HTTP и представление | выполнен 2026-09-24 | Код: вход типов Node в index.php по реестру (`name=node` и не-index файл — `404`), ворота `is_admin_modul()` для `node` и `node-<name>`, меню без «на сайт» у `node`; core/system.php — `getModuleName()` типов, перенесённая `getNodeTypeMap()`, `getUserMail()`, `checkPageCache()` только для списка типа с пустым `op`, `setHead(array|Closure)`, `letter()` и `getEditorFileData()` для мест Node; core/security.php — `405`/`409`/`422`/`429`; core/helpers.php — `getModuleNavi()`, `getTplModuleSelect()`; blocks/modules.php; `Template::checkTemplateFile()`; Parser проверяет `[attach]` по `UPLOADS_DIR`; `final class NodeView` (core/classes/node/view.php) и строка карты load.php; modules/node/index.php (list/view/add/asset/attach/report, закрытая карта методов, `503` для типа из маркера до HTML-кеша), modules/node/admin/index.php (show, add, edit с `409` и keep/save, status, delete, report, types, type, typestatus, typedelete, clone, export, import, config, info), lang/ и admin/lang/ в шести языках, admin/info/ru.md; темы — partials/node/{list,view}.html, восемь fragments/node/, канон fragments/repeat.html в обеих темах, слот `faqtitle` lite; plugins/system/slaed.js (`setRepeatRows`), filemanager.js (`bytag`); config/modules.php (запись `node`); подписи `_CACHE_1`. Решения пользователя 2026-09-23 — 05 → «Заметки реализации S13». Тесты: новый tests/Unit/NodeRouteTest.php (15 тестов, 153 утверждения) на tests/Support/route_probe.php и route_web.php (одноразовая MariaDB, scratch-конфигурация, настоящий index.php/admin.php за `php -S`); RatingTest — оба markTestSkipped сняты; PageCacheContractTest — положительный случай списка Node; NodeModelTest (15 файлов карты), NodeServiceTest. `php vendor/bin/phpunit --filter "NodeRouteTest|NodeModelTest|RatingTest|PageCacheContractTest|EditorRoomTest"` — 111 тестов, 1068 утверждений, пройден; `php -l`, phpstan — без ошибок; `php-cs-fixer check` по 42 затронутым PHP — 0 нарушений; `npm run ui:gates` — ui-audit 0, markup 0, 232 теста; полный phpunit — 1408 тестов, 21208 утверждений, 7 пропусков, 6 провалов вне этапа: CommentIsolationTest (прежний) и пять StatsContractTest в окне 00:00–02:00 (с `-d date.timezone=Europe/Berlin` — 10/10). HTTP-приёмка на стенде (Playwright из c:\tmp, артефакт storage/backup/s13-acceptance_2026-09-24.json вне git) — 43/43, сценарии 2–7 из 13 на временном `nodeprobe`: отключённый тип `404`, включение со стражем, пустой список и его кеш, предпросмотр без записи, отправка в модерацию, публикация видна новому гостю сразу, галерея/скачивание/плеер/`[attach]` через маршруты с `HEAD` и `Range`, прямые файлы `403`, `409` с сохранением ввода → продолжение → сохранение, черновик `404` гостю и чужому пользователю и `200` модератору; уборка через интерфейс, config/ совпал с копией до приёмки, error_php.log и error_sql.log не выросли, в error_site.log только шесть ожидаемых отказов. Самопроверка окна (по команде пользователя «Проверь себя») нашла и исправила: очередь материалов показывала модератору `node-<name>` опубликованные материалы чужих активных типов (теперь только модерируемые, проба сверяет); список отличий при `409` сравнивал дату публикации посекундно при минутной форме; `op=config` проверял пределы собственной копией правил вместо единого валидатора; класс `sl-col-module` передавался из PHP в новую разметку очереди; вкладки модуля строились дважды (`info()`), ссылка «править» со страницы материала шла без типа; проба дополнена `op=info` и принятым сохранением пределов. После исправлений повторены NodeRouteTest, phpstan, php-cs-fixer, `npm run ui:gates`. Не проверено: сценарий 1 (профили — S17); группы и модератор другого типа на стенде — только NodeRouteTest; пара ui:before/ui:after не сохранена — риг дважды не вошёл в админку (altcha), front/presentation расходятся на 0,5–0,9% живыми данными (находка (ж)) |
| S14 — Комментарии и NodeSupport | выполнен 2026-09-24 | Код: `final class NodeSupport implements NodeExtension` в core/classes/node/ext/support.php (девять методов контракта, `updateNodeSupport()`, `getNodeSupportList()`, проверка карт `support.state`/`support.prio` без констант, почта `ext.mail` внутри транзакции действия) и строка `support` закрытой карты ext/load.php; core/classes/comment.php — цель Node по снимку имён типов, ленивые NodeQuery/NodeService, `getTargetMode()` с `checkNodeAction()`, пустые `getList()`/`getBranch()` для недоступной цели, фильтр `getUserList()` пакетным `getNodeTargetList()`, исключение чужих типов в `getAdminList()`/`getModuleList()`, баллы `node.<name>`, `comnum` блокировкой тип → материал до строки комментария и живым счётом `LOCK IN SHARE MODE`, `updateNodeAction()` при первой видимости; core/system.php — `addAdminMail()` читает тип как право `node-<name>`, новая `getAdminNames()` (решение пользователя 2026-09-24); core/user.php — `setComShow()` в режиме `Disabled` без формы, общее уведомление о комментарии не идёт для типа `support`; modules/node/index.php — `getNodeOps(string $ext)`, `setNodeSupport()` (POST op=support владельца), обсуждение на странице материала, `getNodeSupportLabels()`, `getNodeExtVars()`, гостю типа `support` — `403`, всем — `noindex, nofollow`; modules/node/admin/index.php — очередь типа `support` (`setNodeSupportQueue()` с фильтрами state/aid/prio), карточка `op=support`, удаление материала удаляет его комментарии; шаблоны lite partials/node/support/view.html и fragments/node/support/card.html; 15 констант в шести modules/node/lang и 5 в шести admin/lang. Решения пользователя 2026-09-24 — 05 → «Заметки реализации S14». Самопроверка окна (по команде пользователя «Проверь себя») нашла и исправила: публичный `POST op=support` пускал модератора типа, вошедшего на сайт, менять любое состояние чужого обращения — теперь маршрут только владельца и только `staff`/`closed` (проба и приёмка сверяют `403`); сбой чтения получателей письма поддержки откатывал весь ответ вопреки 09 — теперь журналируется и стоит только письма. Приёмка на стенде нашла и исправила дефект S13: общий список админки при единственном доступном типе с расширением читал его без расширения и падал `NodeException: extension` (теперь читатель получает расширение единственного типа; проба сверяет `admin.php?name=node` модератора одного типа поддержки). Тесты: новый tests/Unit/NodeSupportTest.php (11 тестов, 92 утверждения) на tests/Support/route_probe.php с аргументом `support` (тип help с расширением, владельцы anna/boris, оператор helper — аккаунт сайта и администратор `node-help`, дочерние режимы `comments` и `ext`); NodeModelTest (фабрика знает `support`), CommentNotifyTest (условие уведомления), PointOwnersTest (scope комментария), пробы node_probe переведены на добавление ключа в карту фабрики. `php vendor/bin/phpunit --filter NodeSupportTest` — 11 тестов, пройден; `--filter "NodeRouteTest|NodeSupportTest|PointOwnersTest|NodeModelTest|Comment"` — 182 теста, один провал вне этапа (CommentIsolationTest); полный phpunit (`-d date.timezone=Europe/Berlin`) — 1419 тестов, 21310 утверждений, 7 пропусков, два провала: CommentIsolationTest (прежний) и PointOwnersTest (буквальная строка вызова Point в Comment — тест обновлён, повторно пройден); `phpstan analyse` — без ошибок; `php-cs-fixer check` по изменённым PHP — 0 нарушений; `npm run ui:gates` — ui-audit 0, markup 0, 232 теста; `php -l` всех затронутых. HTTP-приёмка на стенде (Playwright и настоящие POST из c:\tmp, артефакт storage/backup/s14-acceptance_2026-09-24.json вне git) — 20/20 (повторена после самопроверки) на временном типе `s14help`, импортированном и включённом через админку, с двумя временными аккаунтами: гость `403`; обращение владельца опубликовано с `Open`, строка очереди, одно письмо подписанному администратору без текста; чужой владелец не видит обращение (`404`); ответ владельца — `comnum`, баллы `node.s14help`, ожидание поддержки; ответ оператора — ожидание автора и письмо только владельцу; чужой ответ и фрагмент чужого обсуждения отклонены; лента профиля скрывает приватный ответ от чужого; закрытие кнопкой владельца, форма ответа исчезает, поздний ответ отклонён, устаревшая версия `409`, повторное открытие; модератор через маршрут владельца — `403`; карточка назначает оператора и срочный приоритет без сдвига `activity`; очередь и `400` на неверный фильтр; ответ владельца назначенному — письмо только ему; удаление ответа модератором снижает `comnum` и компенсирует награду; удаление материала с комментариями и строкой очереди, выключение и удаление типа. config/ совпал с копией до приёмки (кроме пересобранного local.php), каталог uploads/s14help и временные аккаунты удалены; error_php.log и error_sql.log не выросли, в error_site.log — ожидаемые 403/404/409 и отказы `mail()` стенда. Пара ui:before/ui:after не снималась: CSS и канонические фрагменты не менялись, новые шаблоны — файлы режима `support`, которых на снимаемых страницах нет. Не сделано и записано: счётчик в хабе профиля `getUserCount()` по-прежнему считает приватные ответы (виден только их объём, не текст); карточка оператора показывает первую страницу переписки, полная — на странице обращения. Не проверено: настоящая конкуренция двух комментариев одного материала (порядок блокировок — чтением кода и статическим тестом), MySQL 8.0.16+; повторный показ скрытого комментария снова вызывает `updateNodeAction()` — у `_comment` нет признака прежней видимости |
| S15 — NodeSync | выполнен 2026-09-24 | Код: `final class NodeSync implements NodeExtension` в core/classes/node/ext/sync.php (конструктор `Database, NodeContext, Feed`, девять методов контракта, `updateNodeSync()`, `updateNodeSyncList()`) и строка `sync` закрытой карты load.php с `new Feed($conf['rss'] ?? [])`; `Feed::getFeedUrl()` стал публичным статическим (решение пользователя); задание `nodesync` в четырёх местах (`getSchedulerJob()`, `addSchedulerSystemJob()`, `addNodeSyncTask()` в core/system.php, config/scheduler.php, дополнение ветки update6_3 в setup/index.php); поле `limit` на экране планировщика для `nodesync` (1..50) и `nodepublish` (1..500); форма материала типа `sync` принимает `source`/`refresh` вместо тела, экран правки показывает блок источника, `POST op=sync` в modules/node/admin/index.php; 14 констант в шести локалях: `_NODE_SOURCE`, `_NODE_PERIOD`, `_NODE_PERHINT` в modules/node/lang (их читает общий modules/node/index.php), десять `_NODE_*` в админском языке Node и `_SCHEDULER_LIMIT`. Тесты: tests/Unit/NodeSyncTest.php на tests/Support/route_probe.php (режим `sync` по HTTP и дочерний `syncext` с подменённым транспортом Feed). `php vendor/bin/phpunit --filter NodeSyncTest` — 10 тестов, 83 утверждения, пройден; группа `NodeSyncTest|NodeSupportTest|NodeModelTest|FeedTest|NodeRouteTest|LanguageValidation|Scheduler` — 156 тестов, 1054 утверждения, 1 пропуск, пройдена; `php -l`, phpstan (No errors), `php-cs-fixer check` по изменённым файлам, `npm run ui:gates` (exit 0, 232 теста) — пройдены. Полный phpunit: 1429 тестов, провалы — CommentIsolationTest вне этапа и статическая проверка карты в NodeSupportTest (исправлена, затем пройдена в группе). Стенд: `/` и admin.php — 200, error_php.log без новых записей, config/local.php удалён и пересобран с `nodesync`. Самопроверка 2026-09-24 исправила два дефекта: пропуск (`skipped`) после загрузки нового тела поднимал поколение HTML-кеша без записи — теперь поколение и страж решает факт записи тела; отказ формы по адресу или периоду не называл поле — `getNodeFault()` добавляет подпись поля, а три подписи перенесены из админского языка в публичный, иначе публичный путь модератора обращался бы к неопределённой константе. Проверки после правок: NodeSyncTest — 10 тестов, 84 утверждения; группа NodeSupport/NodeModel/Feed/NodeRoute/LanguageValidation/Scheduler/ModuleStructure/SecurityValidation — 164 теста, 1 пропуск, пройдена; phpstan, php-cs-fixer, `npm run ui:gates` — пройдены |
| S16 — Остальные интеграции Node | выполнен 2026-09-24 | Код: core/system.php — адаптеры `node.<name>` в `getRatingService()` (блокировка через новый `NodeService::getLockedTarget()`, `features.rating`, `checkNodeAction`/`updateNodeAction('rate')`, без Point; контекст и карта типов читаются до транзакции), перенесённая `getNodeHandler()`, новые `getNodeTitleMap()`, `getNodeModeType()`, `getNodeBlockParam()` (решения пользователя), потоковый `addSitemapTask()` (части по 50000 URL, курсор `getNodeSitemap()` в гостевом контексте, XML-экранирование адресов), `getBlocks()`/`render_blocks()` с `$param`; core/classes/node/service.php — `getLockedTarget()`, `deleteNodePoll()`, удаление строк избранного в `deleteNode()`; core/user.php — `addFavorite()` Node в одной транзакции, списки избранного через `getNodeTitleMap()`, `getRssChannel()` для типов с `integrations.rss`; core/admin.php — `updateNodeTypePart()` с частью `integrations`, избранное админки; modules/node/index.php и templates/lite/partials/node/view.html — живой рейтинг, избранное и опрос страницы материала; blocks/node.php; admin/modules/blocks.php (параметры node.php), ratings.php (фильтр `node.<name>`), monitor.php и templates/admin/partials/basic-monitor.html (плитки Website/Files); modules/voting/admin (удаление опроса под `node.poll.<id>`), search и search/admin, rss, sitemap/admin, presentation, account; templates/lite/index.php (`faqtitle` через `getNodeModeType()`); `_blocks.param` в table.sql, `addcol` в table_update6_3.sql, сид insert.sql, колонка добавлена в базу стенда. Решения и реализация — 05 → «Заметки реализации S16». Проверки: новый tests/Unit/NodeIntegTest.php с режимом `integ` и дочерним `integext` tests/Support/route_probe.php (реальный HTTP на одноразовой MariaDB 11.7): `php vendor/bin/phpunit --filter NodeIntegTest` — 13 тестов, 57 утверждений, пройден (после самопроверки); `--filter "SchemaUpdateValidation|Node|Rating|Comment|Point|PageCache"` — 384 теста, 1 провал (давний CommentIsolationTest); полный phpunit до правки ожиданий — 1441 тест, 4 провала: CommentIsolationTest (давний), два в NodeServiceTest (новые методы в закрытом списке, удаление стало на один оператор длиннее — строки избранного; ожидания обновлены) и SchemaUpdateValidationTest (экранирование `''` в `addcol`, исправлено); после исправлений и самопроверки полный phpunit — 1442 теста, 21530 утверждений, 1 провал (давний CommentIsolationTest); `php -l` изменённых файлов, phpstan — без ошибок, `php-cs-fixer check` по изменённым файлам — чисто, `npm run ui:gates` — пройден (232 теста). Браузерной проверки на стенде нет: типов на стенде нет до S17 |
| S17 — Все десять профилей | выполнен 2026-09-24 | Код: modules/node/profiles/<name>.json — десять профилей в формате экспорта (канонические settings и fields, пустые uploads/rating = правила сайта по S10); setup/index.php — ветка `new` ставит метку `node => new` в config/update.php; admin/index.php — `addNodeProfiles()` из `addAdminAccount()`: десять `addNodeTypeImport()` + `updateNodeTypeStatus(true)` в контексте первого главного администратора, стартовая новость `addNode()`, снятие метки, предупреждение `_NODE_SETUP` с незавершёнными профилями (шаг и причина — в журнале сайта); modules/node/admin/index.php — строка выбора профиля на экране нового типа, настройки расширения из профиля (`getNodeTypePost($old, $prof)`); lang/*.php — двенадцать `_NODE_*` подписей ролей и полей, admin/lang — `_NODE_SETUP`, modules/node/admin/lang — `_NODE_PROFILE`; core/user.php — `setComShow()` без предупреждения PHP у гостя; tools/seo-audit.php, tools/ui-shots.json, tools/upload-route-check.php — маршруты Node стенда. Решения пользователя 2026-09-24 и реализация — 05 → «Заметки реализации S17». Тесты: новые tests/Support/install_probe.php (копия дерева с конфигурацией из HEAD, одноразовая MariaDB, два `php -S`, настоящие setup.php → первый администратор → панель → публичные страницы) и tests/Unit/NodeProfileTest.php — `php vendor/bin/phpunit --filter NodeProfileTest` 13 тестов, 413 утверждений, пройден: десять активных типов версии 2, четыре общие области и каталоги, стартовая новость, метка снята, повтор ничего не создаёт, экспорт каждого типа = профиль, запись и чтение девяти типов через панель (роли files/links/media, поля files/media, черновик гостю 404), приватный help (гость 403, владелец 200, посторонний 404, очередь), content — ручной sync 502 `address` и плановый `nodesync`, конструктор (10 профилей, desk из help с `mail`, soft из files с ролями и полями), витрина news (категория, 2 страницы, num=9 404, canonical), путь отказа (посторонний файл в uploads/jokes: девять типов, `jokes` в предупреждении и журнале с шагом `import`), журналы PHP/SQL пусты. Под реестр типов поправлены RatingOwnersTest и UploadFormatTest (записи зарегистрированных типов — их правила по S10), UpdateRatingsTest (метка новой установки), node_probe `addProbeScratch()` (реестр стенда не переносится в scratch). Полный phpunit после самопроверки — 1455 тестов, 22127 утверждений, 7 пропусков, 1 провал (давний CommentIsolationTest); `php -l`, phpstan — без ошибок; `php-cs-fixer check` изменённых PHP — 0; `npm run ui:gates` — пройден (232 теста). Браузер: сценарий 1 из 13 на одноразовой чистой установке (Playwright, гость, news/docs/files: списки, вторая страница, категория, материалы, 375 и 1280 px) — 16/16 страниц 200, без ошибок консоли и страниц, без горизонтальной прокрутки, canonical и JSON-LD по типу; артефакт storage/backup/s17-scene1_2026-09-24.json вне git. Стенд: через админку из профилей созданы и включены docs, jokes, content, media (storage/backup/s17-stand_2026-09-24.json), материалы docs 22/23 (дерево и related), jokes 24, media 25 записаны формами панели по HTTP (s17-items_2026-09-24.json); `php tools/upload-route-check.php https://slaed.loc read` — все строки, включая пять новых для jokes.attach; `php tools/seo-audit.php` — маршруты Node 200 (единственная ошибка — давний скачок H1→H5 у shop); `ui-shots --before --only=node-list,node-view,node-media,admin-node,admin-node-type` — снято. Копия config/ до работ — storage/backup/s17-config-before. Журналы стенда: SQL 0, PHP — 8 записей известной находки (д) (холодный кеш шаблонов, errno=13). Не проверено: news, files, faq, help, links, pages на стенде (заблокированы категориями удалённых модулей по NOD-200); пара ui:before/ui:after не делалась — CSS тем и канонические фрагменты не менялись. Самопроверка окна (по команде пользователя «Проверь себя»): неточное предупреждение при сбое только стартовой новости или включения, непроверенное снятие метки и непокрытый путь отказа исправлены (шаги в журнале, новый текст `_NODE_SETUP`, режим пробы `fail`); противоречие таблицы ресурсов `help` в 06 устранено; пустые uploads/rating профилей подтверждены пользователем и записаны в 06 |
| S18 — Целевая поставка и итоговая приёмка | выполнен 2026-09-24 | Контроль дерева: по всему репозиторию (без docs/tests) нет таблиц, каталогов, config/*.php, записей HEAD-конфигурации и сидов девяти модулей; единственный `_news` — историческая ветка update5_1 setup/index.php (оставлена по 12); `NodeService::SWAPS` — разрешение одноимённых имён, не fallback. Код: setup/index.php — сверка config/modules.php как экран модулей (записи без каталога удаляются, `node` = запись чистой установки, без `img`), удаление правил config/uploads.php девяти модулей без одноимённого типа, чтение config/db.php только при наличии (находки ф2 и э закрыты); `NodeQuery::setNodeSets(bool)` и админ-список материалов с `setNodeSets(false)` — 3 SQL; `rank` в обратных кавычках в 13 запросах 7 файлов (MySQL 8); новый tools/node-profile.php. Решения пользователя 2026-09-24 — 12 → «Заметки реализации S18», бюджеты скачивания/жалобы 4 и админ-списка — 11/13, `setNodeSets` — 05, инструмент и проба — 13. Документация выпуска: UPGRADING.md (обновление 6.2 → 6.3 через setup, Node вместо девяти модулей, правило nginx, Point/Rating/Field, минимальные версии БД), docs/VERSIONS.md (запись 2026-09-24). Обновление на настоящей базе: `php tests/Support/install_probe.php <scratch> update storage/backup/slaed_pre-s04_2026-09-20_22-33-38.sql d41badf4 sport storage/backup/update/fields` — дамп стенда до блоков данных (11848 пользователей), конфигурация 6.2 из d41badf4, определения полей стенда из снимка S06; 1-й прогон: сайт закрыт, points/ratings отмечены, реестр модулей сведён (удалены 9 записей), правила загрузок удалены, nodepublish/nodesync и пределы rss добавлены, preflight Field остановился на `sport_order 261` без записи; исходник исправлен как в S06 — 2-й прогон завершил fields (account 3, forum 3, order 9 определений); 3-й ничего не менял; баланс 340292 и `_users` не изменены, типов Node 0; структура 41 таблицы table.sql совпала с чистой установкой; панель 200, `news` отклонён по NOD-200 (`categories`), `docs` создан из профиля и включён, гость 503 (сайт закрыт), админ 200; журналы копии пусты — storage/backup/s18-update_2026-09-24.json. MySQL 8.0.41 (временный сервер в scratch): table.sql — 41 таблица со всеми CHECK; `SLAED_PROBE_DB=… php vendor/bin/phpunit --filter NodeProfileTest` — 13 тестов, 413 утверждений (до правки `rank` — провал 1064; утверждение про db.php добавлено позже и на MySQL 8 не прогонялось), группа с `rank` сохранена панелью. Профиль: `php tools/node-profile.php --out=storage/backup/s18-profile_2026-09-24.json` — exit 0, 100000 материалов, 200 категорий, 200000 доп. категорий, 179999 связей, 170000 ресурсов, наполнение 11,8 с; все бюджеты в пределах (списки 6–7/7, построение 8/8, материал 5 и 6, attach/asset 2, скачивание/жалоба 4, админ-список 3, форма 5, смена состояния 5/6, удаление 5/5), страница 1 и 100 — по 7 SQL, полных сканов нет; p95: списки ≤ 21 мс, последняя страница 51 мс, построение ≤ 33 мс, материал, форма и attach ≤ 7 мс, файловая авторизация ≤ 5 мс, запись ≤ 9,4 мс, средний SQL ≤ 6,1 мс (MariaDB 11.7.2, без Xdebug). Проверки: `php -l` изменённых PHP, phpstan — без ошибок, `php-cs-fixer check` — 0; `npm run ui:gates` — 232 теста, 1334 утверждения; полный phpunit — 1455 тестов, 22127 утверждений, 7 пропусков, 1 провал (давний CommentIsolationTest); NodeQueryTest 57/713; NodeProfileTest на MariaDB после самопроверки — 13 тестов, 414 утверждений; `php tools/seo-audit.php` — маршруты Node 200 (давний H1→H5 shop); `php tools/upload-route-check.php https://slaed.loc read` — все строки; журналы стенда за окно: PHP 0, SQL 0, site — только записи почтовых тестов phpunit. Самопроверка окна (по команде «Проверь себя»): tools/node-profile.php при ошибке до загрузки ядра падал фатально на объявлении `ProfileBase extends Database` и оставлял одноразовую базу — завершение вынесено в `setProfileEnd()`, ранняя ошибка даёт отчёт, exit 1 и удаляет базу (проверено запуском без git в PATH); install_probe сама писала пустой config/db.php и тем прятала находку (э) — запись убрана, теперь установка и обновление идут без db.php, NodeProfileTest проверяет форму setup без файла и его создание; запрос формы идёт своей куки-банкой, иначе второй заголовок Cookie терял `lang=ru`. Не делалось: пара ui:before/ui:after (CSS и фрагменты не менялись), браузерный прогон (сценарии 1–7 закрыты на S13/S17), профиль на MySQL 8 (инструмент работает на сервере config/db.php) |
| S19.1 — Безопасность входа и вывода | выполнен 2026-09-24 | Решения пользователя 2026-09-24: запирание установщика файлом-ключом config/setup.unlock (12 → «Заметки реализации S19.1»); константа подписи поля — по словарю сайта внутри Field, `NodeService::addNodeCategory()` с общим помощником прав экрана категорий, право `polls` в NodeContext (05 → «Заметки реализации S19.1»). Код: setup/index.php — на установленном сайте (имя базы в config/db.php) любой `op` без ключа даёт только экран `_SETUPLOCK` (шесть setup/lang), чистый прогон удаляет ключ, ключ в .gitignore, шаг в UPGRADING.md и README.md; modules/search/index.php — заголовок Node экранируется, фрагмент в безопасном режиме без доверенного тега; admin/modules/fields.php — `checkAdminPost('fields')`, маркер `done[<область>]` каждой области, скрытый `orig` сохранённого поля (отказ при смене имени, пропаже ключа варианта и исчезновении блока), целые до 19 цифр в пределах int64; core/classes/field.php — `checkSiteWord()`: в админке и setup константа подписи принимается только из загруженного lang/<язык>.php; modules/node/admin/index.php `delete()` — чтение материала через тип запроса; core/user.php `addFavorite()` — закрытый список forum/shop с проверкой видимой цели и предел `favorites.favorites` для всех веток; admin/modules/categories.php — `getCategoryRights()` (права с `|` больше не теряются), создание категории Node через сервис; core/admin.php `scatacess()` — снят TypeError на массиве; core/classes/node/service.php — `addNodeCategory()`, `checkCatRow()`, `checkCatParent()` (родитель того же модуля и в `updateNodeCategory()`), журнал `info` «Node: a type operation was published» с доказательством и `aid` в `setTypeWrite()` (холостая смена статуса не пишется), `deleteNodePoll()` требует `polls`; core/classes/node/context.php и `getNodeContext()` — `polls` (главный администратор или право voting); modules/shop/admin и modules/order/admin — clientset/clientdel/partnerset/partnerdel/productops и activate/delete только POST через `getTplPostAction()` + `checkAdminPost()`, `act` только 0 или 1. Тесты: новый tests/Unit/NodeGuardTest.php с режимом `guard` и дочерним `guardext` tests/Support/route_probe.php (реальный HTTP на одноразовой MariaDB) — `php vendor/bin/phpunit --filter NodeGuardTest` 10 тестов, 31 утверждение: поиск выводит `<img onerror>` и `<script>` текстом; GET save полей 302 без записи, обрезанная форма, смена имени, пропажа ключа, admin-only `_FIELDS_BAD` отклонены с неизменными config/fields.php и config/node.php, `_ACCOUNT` принят, int64 19 цифр принят, запись журнала `update docs 1→2 aid 1`; delete чужого типа 404, материал на месте; избранное `bogus`, несуществующая тема, скрытый товар и сверх предела — без строк, баллы только за 2 строки; категория news модератором docs — DENIED без строки, docs — создана, родитель другого модуля — без строки, права `2|1`, `2|1,0` сохраняются при создании и сохранении; шесть GET shop и два GET order с токеном — без изменений, POST работают; order act=2 — без смены и баллов, act=1 — статус 1 и одна награда; администратор voting удаляет опрос, контекст без `polls` — DENIED; журналы PHP и SQL пробы пусты. NodeProfileTest (install_probe): установленный setup.php без ключа не выводит форму и пароль, POST не меняет db.php/global.php/security.php и не переименовывает admin.php, с ключом форма открывается — 13 тестов пройдены; NodeModelTest (`polls`), NodeServiceTest (`addNodeCategory`), NodeConfigTest и NodeProfileTest (журнал типов считается отдельно от ошибок) обновлены. Проверки: `php -l` изменённых PHP, phpstan — без ошибок, `php-cs-fixer check` изменённых — 0; `npm run ui:gates` — 232 теста, 1334 утверждения; полный phpunit — 1467 тестов, 22172 утверждения, 7 пропусков, 1 провал (давний CommentIsolationTest). Стенд: https://slaed.loc/setup.php и `?op=config` — 200, экран ключа без полей подключения; POST `op=save` — config/db.php и config/security.php байт в байт, admin.php на месте; поиск 200; журналы стенда PHP и SQL за окно без записей. Самопроверка окна (по команде «Проверь себя»): проба `guard` проверяла обрезанную форму подстрокой, которая есть на странице и без отказа, — теперь путь `node.<тип>: form`; `productops()` превращал вложенный массив `id[]` в id 1 — берутся только скаляры; order `activate()` с недопустимым `act` отвечал «нет токена» и вёл на экран отправки — теперь `_ERROR` и список; в `getFieldBlock()` переменная `$head` несла два смысла — строка имени стала `$lead`; мутационная проверка: возврат `checkSiteToken()` и маркера области в fields, сырого заголовка в поиске и удаления без чтения через тип роняет 5 из 10 тестов NodeGuardTest, после восстановления — 10/10; повторно phpstan, php-cs-fixer, ui:gates (232), NodeGuardTest, NodeProfileTest, NodeServiceTest, NodeIntegTest — пройдены. Не делалось: браузерный прогон (разметка и CSS не менялись, использованы существующие фрагменты), пара ui:before/ui:after |
| S19.2 — Обновление 6.3 | выполнен 2026-09-25 | Решения пользователя 2026-09-25 (12 — «Заметки реализации S19.2»): преобразователь конфигурации 6.2 в ветке, старые config_*.php — в storage/backup/update/config, на чистом финише снимки удаляются (manifest остаётся), `new` сбрасывает типы прежней установки. Код setup/index.php: `getSetupConfig()`/`getSetupBase()` (оба формата, запирание и форма на db.php 6.2), setConfigFile под блокировкой каталога config с удалением local.php, `save()` — подключение → preflight → закрытие → `setUpdateConfig()` (форматы загрузок и банов, язык, стартовый модуль, тема и логотип вне дерева — значение поставки) → общие записи, preflight MariaDB 10.5.2 и `_categories`/`_voting`, `setUpdateMails()` (снимок до DDL, перенос без дублей), стоп ветки на ошибке до DDL и в DDL, newsletter.php только дополняется, снимки блоков проверяются до verified, (ю) в `new`, проверки `SHOW … LIKE ?` заменены information_schema, повтор плейсхолдера `:num` устранён; UPGRADING.md и 12. Проверки: `phpunit --filter Update(Points|Ratings|Fields|Config|Mails)Test` — 32 теста, пройден; NodeProfileTest — 13, пройден; `install_probe.php … update storage/backup/slaed_pre-s04_2026-09-20_22-33-38.sql C:/OSPanel/home/slaed-old.loc/public/config sport storage/backup/update/fields` — отчёт storage/backup/s19.2-update_2026-09-25.json: запирание на db.php 6.2, отказ preflight (`_voting` MyISAM) без изменения файлов, гость 200 до и 503 после закрытия, искусственная ошибка DDL — стоп без manifest и отметок, рассылка 4 адреса без дублей на повторах, newsletter.php сохранил `abort`, снимки удалены после чистого прогона, manifest третьим прогоном не менялись, схема = чистой установке, docs создаётся, presentation и maildrain целы ((р)); phpstan без ошибок, php-cs-fixer check — 0, ui:gates — 232 теста; полный phpunit — 1477 тестов, 6 провалов вне этапа (CommentIsolationTest давний, 5 StatsContractTest — окно 00:00–01:00 CEST: PHP CLI в Etc/GMT-1, проба ядра в Europe/Berlin; повторно в 01:01 — 10 тестов, пройден), 7 пропусков. MySQL 8 не прогонялся — модуль OSPanel не запущен |
| S19.3 — Целостность данных Node, Point и Rating | выполнен 2026-09-25 | Решения пользователя 2026-09-25 (12 и 05 — «Заметки реализации S19.3»): старые адреса — `update6_3` поднимает AUTO_INCREMENT `_nodes` выше наибольшего id девяти прежних таблиц; очистка остатков — список на экране типов и POST `op=remains` (`NodeService::getNodeRemains()`/`deleteNodeRemains()`, баллы не компенсируются); `deleteNode(int, int, Comment $com)` — комментарии удаляются `Comment::deleteTarget()` внутри транзакции; `Point::$valid` — `public private(set)`. Код: `checkNewName()` отказывает путём `remains` при строках `_comment`/`_favorites` с тем же modul; `Comment::deleteTarget()` присоединяется к транзакции владельца, блокирует строки и компенсирует `comment` каждого автора, ошибка — false и откат владельца; shop (массовое удаление товаров) и voting (обе ветки удаления опроса) — удаление целей и комментариев одной транзакцией; `NodeQuery::getNodeTarget()/getNodeTargetList()` и `getLockedTarget()` получили `bool $any` (модератор читает цель в любом состоянии), `setAssetAction()` читает с ним; адаптер Rating `node.<name>`: главный администратор аннулирует на отключённом материале, при выключенном `features.rating` и в неактивном типе, `updateNodeAction('rate')` — только при росте числа голосов; `nodepublish` при закрытом или сломанном Point снимает задачу с предупреждением в журнале; `setConfigFile()` при сбое записи фазы committed с доказательством решает по БД (`setConfigRestore()`), а не откатывает к old; account: ключ формы `pkey` — источник `adjust:<pkey>`, причина проверяется `Point::checkNote()` (стала публичной) до записи профиля, без молчаливого `strip_tags`; forum: id нового сообщения — `getSqlLastId()`; `updateNodeStatus()` в Pending/Published отказывает путём `expires`; blocks `editsave()` — вся запись одной транзакцией, RSS читается до BEGIN; `checkRowCat()` — modul без учёта регистра; три константы `_NODE_REM*` в шести языках. Не подтвердилось и не исправлялось: повтор адреса ресурса в одном вводе (роль link одна на тип и с max 1 — второй ресурс отклоняется `assets.link.max`; регрессия в NodeServiceTest). Тесты: NodeConfigTest (остатки, потеря журнала после COMMIT через дочерний режим crash `journal`), NodeServiceTest (`theIntegrityFixesHold`: откат при сбое комментариев, компенсация −5/−5 в транзакции, expires, закрытый Point, чтение модератора), новый NodeIntegrityTest на новом режиме `intact` route_probe.php (реальный HTTP: поправка применяется один раз на форму, длинная и размеченная причина не пишут профиль, сбой последнего UPDATE blocks откатывает param, аннулирование в трёх случаях), закрепления API в NodeQueryTest, NodeServiceTest, PointTest, PointOwnersTest. Проверки: `php -l` изменённых, phpstan — без ошибок, `php-cs-fixer check` изменённых — 0, `npm run ui:gates` — 232 теста, 1334 утверждения; полный phpunit — 1484 теста, 2 провала (давний CommentIsolationTest; FeedTest ловил сдвиг кода в setup — шаг перенесён в конец ветки, FeedTest и Update*Test затем 55/55), 7 пропусков; повторно затронутые наборы — 174 теста, пройдены. `install_probe.php … update storage/backup/slaed_pre-s04_2026-09-20_22-33-38.sql C:/OSPanel/home/slaed-old.loc/public/config sport storage/backup/update/fields` — отчёт storage/backup/s19.3-update_2026-09-25.json: наибольший старый id 3819, AUTO_INCREMENT `_nodes` после ветки 3820, повторы его не меняют (прогон до переноса шага в конец ветки; логика шага не менялась). Стенд (Playwright, только чтение): `admin.php?name=node&op=types` — 200, таблица «Записи удалённых разделов» faq, files, help, links, news, pages, ошибок консоли нет; журналы PHP и SQL стенда за окно пусты. Самопроверка окна (по команде «Проверь себя»): перечитан весь дифф, дефектов не найдено; мутационная проверка — возврат старого поведения восьми правок разом (компенсация в deleteTarget, отказ remains, закрытый Point, expires, enabled в адаптере записи, откат к old после COMMIT, случайный источник adjust, запись blocks без транзакции) роняет 6 тестов, а пять правок, покрытых одним `theIntegrityFixesHold`, по одной (компенсация, закрытый Point, expires, флаг `any`, удаление комментариев в транзакции deleteNode) — каждая ловится; после восстановления полный phpunit на итоговом дереве — 1484 теста, один провал (давний CommentIsolationTest), 7 пропусков; ui:gates — 232 теста. |
| S19.4 — Кеш, блокировки и коды ответа | выполнен 2026-09-25 | Решения пользователя 2026-09-25: сборка конфигурации при журнале journal/backup — из текущих источников без local.php (06 «Заметки реализации S19.4»); новые функции Comment::setWriteBegin/setWriteUndo/setWriteDone, Point::setUserLocks, FileManager::getLockKey; к протоколу guard приведены также владельцы account (удаление), shop (массовое удаление) и voting без Node (11, 05, points.md — «Заметки реализации S19.4»). Записи Comment — guard до BEGIN и addEpoch(true) после COMMIT; (ъ) — NodeService::setTargetLock() публичный, в транзакции Comment нет обычного чтения до блокировки материала; ключ файловой блокировки канонический, подкаталог области берёт корень UPLOADS_DIR/<тип>; addEpoch не переписывает нечисловое поколение; гонка marker.json; finally для `$busy` и lock в getConfig/setConfigFile/setConfigRestore, нечитаемый снимок не удаляет источник, нечитаемый источник отклоняет операцию; порядок «расширение → Point» и набор пользователей до первого события (shop, deleteTarget, deleteNode); ключ главной несёт тип; 503 раньше 404/405; config() — 422/409/500; флаг D в трёх regex. Проверки: `php -l` изменённых, phpstan — без ошибок, php-cs-fixer check изменённых — 0, `npm run ui:gates` — 232 теста, пройден; полный phpunit — 1492 теста, провал только давний CommentIsolationTest, 7 пропусков; мутационная проверка 11 правок — все пойманы; самопроверка нашла и закрыла перезапись счётчика с ведущими нулями в addEpoch (тест `padded` в RatingTest, мутация поймана); HTTP стенда: главная и списки jokes/docs 200, неизвестный op 404, error_php.log и error_sql.log без новых записей |
| S19.5 — Рендер и представление | выполнен 2026-09-25 | Решения пользователя 2026-09-25 (05 — «Заметки реализации S19.5»): доверенный рендер только внутри тегов — Parser получил последний параметр `bool $trust = false` у filterDoc()/filterContent(), NodeView и поиск Node рендерят `safe = true, trust = true` (docs/PARSER.md); автор действия — четвёртый параметр `int $uid` у NodeExtension::updateNodeAction(); первая публикация комментария — колонка `_comment.shown` (table.sql, update6_3 с заполнением). Код: getPlainText() без script/style; og:image внешней обложки — адрес источника; getNodeTplName() перенесена в core/system.php, blocks/node.php берёт `node/<mode>/block`; NodeSupport решает сторону по автору ответа, Comment::setStatus() зовёт расширение только при `shown IS NULL`; Rating::getAverage() — public static с точностью `$digits` (6 у голоса и NodeView, 2 у getRatingAsync() — решение пользователя при самопроверке); постер — роль `poster`; (п) filterAttach() читает три формы одним проходом; `op=report` возвращает к материалу (сбой чтения ресурса — к списку типа); адреса админки Node и preview — getSeoUrl(); удалено `$conf['style']` в index.php; slaed.js — DOMParser. Проверки: `php vendor/bin/phpunit --filter 'ParserFixturesTest|NodeRouteTest|NodeSupportTest|NodeIntegTest|NodeModelTest|RatingTest'` — пройдены; полный phpunit — 1497 тестов, провал только давний CommentIsolationTest, 7 пропусков; phpstan — без ошибок; php-cs-fixer check изменённых — 0; `npm run ui:gates` — 234 теста; мутационная проверка — 10 мутаций, все пойманы; HTTP стенда: главная, списки и просмотры docs/media/jokes — 200, error_php.log без новых записей. ui:before/ui:after не делались — CSS и фрагменты не менялись |
| S19.6 — Производительность и Feed | выполнен 2026-09-25 | Решения пользователя 2026-09-25 (03 «Индексы материала», 05 «Заметки реализации S19.6», points.md): упорядоченное чтение делится на ветви `pinned <> 0`/`pinned = 0`, индексы `_nodes` — `title (tid, status, pinned, title, id)`, `updated (tid, status, pinned, updated, id)` вместо `admin`, новый `expires (tid, status, expires)`; журнал баллов считает до 5000 строк (100 страниц); страница ленты account/rss хранит результат адреса 900 секунд в кеше данных (постоянная кода). Категорийный список — две непересекающиеся ветви `cid = X` и `cid <> X AND id IN (связи X)`; части страницы несут только id и ключи сортировки, полная строка и имена — по PK для строк страницы; Feed — код последнего ответа в отказе, разрешение имени внутри `rss.timeout`, `gethostbynamel()` только без записей DNS; (м) `filterWebUrl()` сохраняет регистр пути; (о) закрыта кешем; 11/13 — бюджет категорийного списка 5/6, 06 — `pview` вне предвыборки. tools/node-profile.php (MariaDB 11.7.2, 100000 материалов, 30 повторов, отчёт storage/backup/s19.6-profile_2026-09-25.json): все бюджеты соблюдены, fail пуст; новый критерий — реальные чтения Handler_read против конца страницы, план категории `n:cat` + `nc:cat`, граница времени против закреплённых и датированных строк; сортировка по заголовку 195 чтений (до этапа 9049), граница 8 (9021), счётчик категории 2505 (18722); прежний код даёт 14 провалов инструмента. Проверки: phpstan без ошибок; php-cs-fixer check изменённых — 0; npm run ui:gates — 234 OK; полный phpunit — 1501, провал только давний CommentIsolationTest, 7 пропусков; мутации — 9 из 9 пойманы; /schema — Node совпадает (включая индексы), вне Node давнее `whois.shost/sdc`; HTTP стенда: списки docs/jokes/media с сортировками, POST модуля rss, журнал баллов через вход администратора (Playwright c:/tmp/s196-journal.js) — журналы ошибок не выросли. |
| S19.7 — Остатки девяти модулей и потерянные функции | выполнен 2026-09-25 | Решения пользователя 2026-09-25 (05 и 12 — «Заметки реализации S19.7»): профиль по типам через новый `NodeQuery::getNodeAuthorStat()`, числа категорий через новый `NodeQuery::getNodeCategoryCount()`, ссылки темы на старые id и cat убраны, `maildrain` — приоритет 8 и починка в update6_3. Код: core/classes/node/query.php — два метода; core/user.php — `getProfileModules(int $uid = 0)` с типами Node (кроме `support`) и числами на автора, вкладки типов в `getProfileLastView()`, пункт типа поддержки в `getUserNavItems()`, `<dc:creator>` в RSS Node при `list.show` с `author`, выдача shop только при `is_active('shop')`; modules/account/index.php — плитки хаба из тех же чисел; core/admin.php — строки модерируемых типов в блоке «Новое» (`Pending` или очередь поддержки), числа категорий Node, `getAdminCountRow()` принимает имя константы или готовый текст (`getConst()`); modules/voting/admin/index.php — владельцы опроса: shop, типы с `features.poll` и сохранённое значение; modules/rss/index.php — по умолчанию первый тип с RSS, `mod` вне списка отбрасывается; modules/search/admin/index.php — модуль `node` не проверяется как таблица; admin/modules/blocks.php `editsave()` — `bkey` и `url` пишутся во всех ветках, свежая загрузка RSS сдвигает `time`; config/scheduler.php и setup/index.php — `maildrain` 8, обновление разводит занятый приоритет; шесть modules/search/lang — удалены `_MDIRECTOR`, `_MROLES`, `_MTITLE`, `_MYEAR` и найденная тем же поиском `_SEARCHFROM`; templates/lite/partials/menu.html, site-footer.html, site-header.html — пункты на старые id и cat удалены (раздел «Партнерам» опустел и удалён), «Скачать систему» ведёт на `name=files`, wordmark, «Made in Germany» и логотипы технологий без ссылок; theme.css — `.sl-partners span` вместо `a` (правило `:hover` без ссылки снято). Тесты: NodeQueryTest (контракт методов и `authorNumbersAndCategoryCountsFollowTheirRules`, прогон `author` в node_probe), CommentTargetTest и contract_probe (сравнение ленты с прежним UNION — без типов Node, строка охраны обновлена). Проверки: phpstan без ошибок; php-cs-fixer check изменённых — 0; `php -l` всех изменённых; полный phpunit — 1502 теста, провал только давний CommentIsolationTest (modules/presentation/index.php), 7 пропусков; ui:gates — 234 OK; ui:before (216 кадров) / ui:after — 141 кадр страниц lite сменил высоту из-за подвала, админские кадры без изменений, подвал сверен глазами, счётчики аудита не выросли; мутационная проверка условия `modul` избранного поймана; HTTP-приёмка на стенде (storage/backup/s19.7-accept_2026-09-25.json, скрипт c:/tmp/s197-accept.js) — 13 шагов ok, логи php и sql пусты. |
| S19.8 — Правила кода и прежние находки | выполнен 2026-09-25 | Код: стартовая новость `addNodeProfiles()` — константы `_NODE_START_TITLE`, `_NODE_START_INTRO`, `_NODE_START_BODY` в admin/lang/*.php шести языков; 73 строки длиннее 180 в 22 файлах перенесены (SQL — продолжением `.' …'`, параметры — строками массива, комментарии сокращены), комментарии в телах функций вынесены в шапки (core/helpers.php `getTplModuleSelect()`, setup/index.php `save()`, plugins/system/filemanager.js `addInsertRows()`, три теста); 97 приведений `(string)`/`(bool)` над строками PDO в query.php, service.php, support.php, sync.php и два в admin/modules/ratings.php, config.php сняты; (я) `getUserInfo(): array`; (ь) константы каталогов core/system.php под `defined()`; (к) `filterFields()` и ветка массива `filterText()` удалены вместе с тестом InputFilterTest, `aemail` в admin/index.php читается через `getVar()`; (л) удалены `getTplRefreshTimeSelect()`, `getAsyncPager()`, `getTranslit()`, `url_types()` (вызовов нет в PHP, JS, шаблонах); (х) StatsContractTest считает даты в поясе сайта `gtime`; (ш) route_probe.php без строк длиннее 180 — подтверждено; CommentIsolationTest — демо-запрос presentation читает `_forum` вместо несуществующей `_comments`. Проверки: `php -l` изменённых, `php-cs-fixer check` — 0 файлов, phpstan — без ошибок, полный phpunit — 1501 тест, 0 провалов, 7 пропусков (данные стенда), `npm run ui:gates` — 234 теста; StatsContractTest в поясах CLI Pacific/Kiritimati и Pacific/Pago_Pago — 10 тестов ok; скан `git diff d41badf4` + неотслеживаемые: строк длиннее 180 и комментариев в теле функций вне lang-файлов нет; HTTP стенда: главная, jokes, docs, материал jokes, users, search, presentation, account, rss — 200, error_php.log и error_sql.log без новых записей |
| S20.1 — Безопасность публичной формы Node | выполнен | Решения пользователя 2026-09-25: капча `comment`, окно по IP `node.limits.send = 60` (внесены в 11, 05, 06). Загрузка формы — право `checkEditorUploadAccess()` до цикла, неактивная и link-роль файл не берут, предел `max` роли и `maxfiles`; капча гостя на каждом POST до чтения файлов; `NodeException::LIMITED` → 429 `_CERROR5`, модератор типа освобождён; `NodeQuery::checkNodeCategory()` — закрытая категория 404; поиск — `title` ссылки через `html_entity_decode`, одно экранирование. Проверки: `php -l` изменённых — чисто; phpstan — без ошибок; `php-cs-fixer check` — 0; `npm run ui:gates` — 234 теста OK; NodeRouteTest (режим пробы `secure`) — 20 тестов, 175 утверждений OK; полный phpunit — 1505 тестов, 22480 утверждений, 7 пропусков, 9 провалов только NodeProfileTest: install_probe берёт config/* из `git show HEAD:`, где у config/node.php ещё нет `send`; с индексом, содержащим одну строку `send`, NodeProfileTest — 13 тестов, 418 утверждений OK (проверка откатана); журналы проб error_php/error_sql пусты. |
| S20.2 — Установщик и обновление 6.3 | выполнен 2026-09-25 | Решения пользователя 2026-09-25 (12 — «Заметки реализации S20.2»): форма не выводит пароль БД, пустое поле сохраняет пароль config/db.php; отказ оставляет config/setup.unlock; префикс `[A-Za-z0-9_]{1,32}`, имя файла админки — `filterVar()`; сверка реестра вынесена в новую `setUpdateModules()`. Код setup/index.php: `setUpdateModules(Database, string): string` — `active`, `view`, `inmenu`, `mod_group`, `blocks`, `blocks_c` таблицы `_modules` побеждают поставочный config/modules.php и запись по умолчанию, сайт без таблицы — как раньше (заодно запись реестра больше не затирает глобальный `$title` страницы); `checkUpdateBase(..., bool $fresh = false)` — версия сервера для `new` и `update6_3`, `new` отказан при любой таблице `<префикс>_…` (LIKE с экранированным `_`), `update6_3` требует `_users` и `_admins`; `save()` — пароль, грамматика префикса и файла админки до подключения, preflight `new` до первой записи, отметки update.php только после table.sql и insert.sql без красной строки (insert.sql не запускается после упавшего table.sql); форма `config()` — поле пароля пустое, подсказка `_CONF_3_INFO` на установленном сайте; подключён core/classes/logger.php. core/classes/pdo.php — в установщике отказ подключения всегда выводит причину через `setExit()` экранированной (раньше при `error = 0` — `setError()`, которого у установщика нет). Три константы `_CONF_3_INFO`, `_SETUPPREFIX`, `_SETUPAFILE` в шести setup/lang. UPGRADING.md — пароль, грамматика, preflight `new` и `update6_3`, реестр `_modules`. Тесты: новый tests/Unit/UpdateSetupTest.php на режиме `setup` tests/Support/update_probe.php (в схему пробы добавлена `_admins`, подъём `setUpdateModules`): реестр сайта с `_modules` (выключенный forum с группой 3 и блоками сохраняет их, shop — свои, extra — запись по умолчанию, node — запись чистой установки, запись news удалена и названа, права `1,3,shop,9` → `forum,news,shop`, повтор ничего не меняет), сайт без таблицы, preflight `new` (занятый, свободный, `prob`, `prob_`, сервер 10.5.1) и `update6_3` (нет обеих таблиц, нет `_admins`), статическая проверка формы и порядка в `save()`; NodeProfileTest::theUnlockedInstallerRefusesBeforeItWrites на install_probe (новая `getInstallRefuse()`, реальный HTTP к setup.php копии): в форме нет пароля, пустое поле берёт сохранённый пароль, неверный пароль — причина без fatal, `new` поверх таблиц префикса, `site-1`, `../moved`, `update6_3` с префиксом без таблиц — отказ, config/ байт в байт и ключ на месте; `new` с упавшим insert.sql под свободным префиксом — update.php не создан, ключ на месте; журналы — только две строки отказа подключения в error_site. `php vendor/bin/phpunit --filter "UpdateSetupTest|UpdatePointsTest|UpdateRatingsTest|UpdateFieldsTest|UpdateConfigTest|UpdateMailsTest"` — 36 тестов, 305 утверждений, пройден; `phpstan analyse` — без ошибок; `php-cs-fixer check` по изменённым PHP — 0; `npm run ui:gates` — 234 теста; `php -l` изменённых PHP. Полный phpunit — 1510 тестов, 22516 утверждений, 7 пропусков, 9 провалов — все в NodeProfileTest от незакоммиченной строки `send` config/node.php S20.1 (десять профилей не создаются в копии из HEAD); новый тест отказов этого класса проходит. Самопроверка окна: снято лишнее приведение `(string)` у пароля в `save()`, шаг `keep` пробы получил сверку хэшей config/; повторно `UpdateSetupTest|UpdatePointsTest` и тест отказов — 11 тестов, 97 утверждений, пройден. Сквозное обновление настоящего дампа: `php tests/Support/install_probe.php <scratch> update storage/backup/slaed_pre-s04_2026-09-20_22-33-38.sql d41badf4 sport storage/backup/update/fields` (артефакт storage/backup/s20.2-update_2026-09-25.json вне git) — ошибок пробы нет, отказ по ключу и preflight на месте, сломанный DDL без отметок, 1-й прогон остановлен Field на `sport_order 261` как в S18, 2-й завершил, 3-й ничего не менял, реестр удалил записи девяти модулей, структура 41 таблицы совпала с чистой установкой, ключ снят; в дампе стенда нет таблицы `sport_modules`, поэтому ветка «запись `_modules` побеждает» проверена только UpdateSetupTest. Вне этапа (карточка): ветки `update4_1`…`update6_2` без проверки версии источника, `update5_0` перехеширует пароли |
| S20.3 — Целостность комментариев, баллов и категорий | выполнен 2026-09-25 | Решение пользователя 2026-09-25 (05 «Заметки реализации S20.3»): путь категории — по реестру новым `NodeService::checkTypeRegistry(array $names): bool`. Код: core/classes/comment.php — `setNodeLock(..., bool $need)` (NOTFOUND отказывает новому комментарию и публикации), `addComment()` блокирует материал и для pending и перечитывает режим под блокировкой, `setStatus()` отказывает публикации комментария исчезнувшего материала, счётчик/расширение/баллы в одном `try` с откатом и записью журнала, `updateTargetPoints(): bool` (сбой компенсации откатывает запись, `Point::$valid = false` — без компенсации с предупреждением), `deleteTarget()` откатывается на сбое компенсации; core/classes/node/service.php — `deleteNode()` откатывается на `false` компенсации публикации (`STORAGE`), `checkTypeRegistry()`, `getCatTypes()` отказывает зарегистрированному нечитаемому типу, `updateNodeCategory()` — `category.used` и при подкатегориях; core/classes/node/ext/sync.php — отказ отката держит guard; admin/modules/categories.php — четыре обработчика по реестру. Проверки: `php -l` изменённых файлов — без ошибок; phpstan — без ошибок; `php-cs-fixer check` — 0; `npm run ui:gates` — exit 0, 234 теста; `phpunit --filter NodeServiceTest` — 23 теста OK (новый `theCommentAndPointFixesHold`, категории, поверхность), `--filter NodeIntegrityTest` — 5 тестов OK (HTTP: категория типа с повреждённой версией не удаляется, после восстановления удаляется), NodeSyncTest (`undo`) — в полном прогоне OK; полный phpunit после самопроверки — 1512 тестов, 22544 утверждения, 7 пропусков, 9 провалов только NodeProfileTest (известная причина S20.1: install_probe берёт config/node.php из HEAD, где нет `send`, `checkNodeWait()` отказывает STORAGE и профили не создаются — `git show HEAD:config/node.php` без `send` сверен). `sport_points` сверены SQL в одноразовых схемах пробы; стенд: `sport_points` max id 851 как до окна, журналы error_php/error_sql 701/370 строк — не выросли |
| S20.4 — Дерево документов | выполнен 2026-09-26 | Решения пользователя 2026-09-26 (05 — «Заметки реализации S20.4»): дерево только в полном виде материала, состав — ветка (путь, уровень, дети текущего; самопроверкой добавлено окно: до 10 соседей с каждой стороны, первые 20 детей, «…» на обрезанном крае), соседи — книжный обход, партии `getNodeTree()` без потолка; индекс `_nodes.tree` (`tid, status, id`) в table.sql и table_update6_3.sql, добавлен и на стенд. Код: `getNodeTreeData()` в modules/node/index.php, `tree_html` среди живых частей `getNodeViewHtml()`, фрагмент templates/lite/fragments/node/tree.html, стили `.sl-node-tree*` в theme.css lite. Проверки: `php -l` затронутых PHP — без ошибок; phpstan — без ошибок; `php-cs-fixer check` — 0; `npm run ui:gates` — 234 теста, 1350 утверждений; `php tools/ui-audit.php --theme=lite` — exit 0 (промежуточный `dup` 1 устранён); `php vendor/phpunit/phpunit/phpunit --filter "theDocumentShowsItsBranchOfTheTree|theTreeKeepsTheBudgetOfTheView|aWideLevelShowsAWindowAroundTheDocument"` — 3 теста, 22 утверждения (HTTP пробы `tree`: ветка, путь, соседи, скрытый родитель, модератор, тип без дерева, однократное экранирование, окно широкого уровня и широкого родителя; SQL: вид 6 + 1 партия на 7 документов, 6 + 2 на 607); полный phpunit — 1514 тестов, 22563 утверждения, 7 пропусков, провалы только NodeProfileTest (9, конфиг из HEAD без `limits.send` S20.1) и NodeModelTest индексов (исправлен, `--filter theIndexesAreExactlyTheContract` пройден); профиль — копия tools/node-profile.php с `limits.send` (сам инструмент берёт config из HEAD и до коммита S20.1 падает на `getNodeLimits()`), 100000 материалов: «view docs tree» 26 SQL при бюджете 26, p50 59 мс (без индекса — 160 мс, ≈110000 строк), fail пуст; `npm run ui:before`/`ui:after` — сдвинулись node-view (блок дерева, ожидаемо) и front/presentation (шум отладочной панели стенда со списком SQL, страницы не менялись), блок снят Playwright и просмотрен (на / и presentation нет ни одного класса `sl-node-tree`, новые селекторы их не задевают; обе страницы несут отладочную панель SQL стенда); окно широкого уровня снято Playwright с HTML пробы; правило `.sl-node-tree-more` добавлено после `ui:after` и стендовых страниц не касается (на стенде нет уровней шире окна), повторный `ui:after` не запускался. |
| S20.5 — Действие `moderate` и мелкий рендер | выполнен 2026-09-26 | Решения пользователя 2026-09-26 (points.md — «Заметки реализации S20.5», 06, 08, 09): `moderate` награждает одобрение чужого материала pending → published (`approve:<id>`), решение по открытой жалобе на ресурс, кроме своей (`report:<id>:<16 hex>`), первую публикацию чужого комментария (`comment:<id>`) и первый ответ сотрудника в чужом обращении support (`reply:<id обращения>`); получатель — учётная запись сайта того же запроса, `aid` — администратор, без компенсации; `search.html` и лёгкие режимы `block`/`search` удалены, лёгкая цель — только `card`; sitemap читает партиями `limits.syncbatch`, дерево — `NodeQuery::TREEPART = 500`; `form` и `admin` зарезервированы пустыми (06). Находка про support/view.html для help не подтвердилась (расширение support запрещает рейтинг и прочее), решение: слоты добавлены для типа без расширения с `view.mode = support`, критерий карточки переписан. Код: `NodeService::addNodePoint(..., int $self = 0)`, `updateNodeStatus()`, `deleteNodeAssetReport()`; `Comment::addModerPoint()`, `updateNodeAction(..., int $id)`, `setStatus()`; `NodeView::LIGHT`; связанные карточки с `is_views = false`; support/view.html с `fav_html`, `poll_html`, `assets_html`, `rating_html`, `tree_html`, связанными. Проверки: `php -l` затронутых PHP — без ошибок; phpstan — без ошибок; `php-cs-fixer check` — 0; `npm run ui:gates` — 234 теста, 1350 утверждений; `php tools/ui-audit.php --theme=lite` — exit 0; `phpunit --filter NodeServiceTest` — 23 теста, 779 утверждений (одобрение, своё, без ревью, жалобы, своя жалоба); `--filter "NodeIntegTest|NodeSupportTest"` — 27 тестов, 159 утверждений (HTTP: связанная карточка без чипа — тест падает при откате правки контроллера; режим support у news показывает рейтинг, опрос, избранное; sitemap при syncbatch 100 — на 5 SQL больше; ответ сотрудника и одобрение комментария — по одной строке `moderate`); `--filter NodeRouteTest` — 23 теста, 198 утверждений; полный phpunit — 1517 тестов, 22575 утверждений, 7 пропусков, провалы только NodeProfileTest (9, install_probe берёт config/node.php из HEAD без `limits.send` S20.1, как в S20.4; доказано опытом: с `send` в копии HEAD NodeProfileTest — 14 тестов, 425 утверждений, проба возвращена); `npm run ui:before`/`ui:after` — сдвинулись node-view (docs 23: у связанной карточки ушёл пустой чип просмотров, снято Playwright до и после) и front/presentation (прежний шум отладочной панели стенда); оба прогона after (повтор — тот же набор) не вошли на сайт по таймауту: риг входит двумя режимами параллельно, компиляция шаблона account ловит errno=13 — находки (е) и (д); страница account отдаёт форму, S20.5 её не трогал; журналы стенда: error_php 725 → 744 только errno=13 во время съёмки, error_sql 370 без изменений, error_site — строки почтовых тестов полного phpunit. |
| S20.6 — Режимы представления | выполнен 2026-09-26 | Решения пользователя 2026-09-26 (08 — «Выбор представления типом»): `article` — базовый комплект без своего каталога; `docs` — оглавление (строка: заголовок, анонс в одну строку, чип категории; вид без автора и просмотров); `faq` — аккордеон на `<details>` (вид без автора и просмотров); `files` — карточка файла с прямой кнопкой скачивания, размером и числом скачиваний первого ресурса роли `download` (вид — ресурсы над текстом); `media` — плитки с постером и чипом года или даты (вид — плеер, галерея и поля над текстом, связанные сеткой); обложка любой карточки берёт сначала роль `poster`. Код: `getNodeCover()` с `poster`, новый `getNodeDownload()` и ключи `download`, `download_label`, `hits_label`; 4 фрагмента и 5 partials в templates/lite/{fragments,partials}/node/{docs,faq,files,media}/; классы `sl-node-toc`, `sl-node-toc-intro`, `sl-node-faq`, `sl-node-faq-body`, `sl-node-file`, `sl-node-tiles`, `sl-node-tile`, `sl-node-tile-cover`; дубль тела `.sl-node-file .sl-meta-foot` внесён с причиной в группу tools/ui-contract.php. Проверки: `npm run ui:before` до правки (216 снимков); `php -l` затронутых PHP — без ошибок; phpstan — без ошибок; `php-cs-fixer check` — 0; `php tools/ui-audit.php --theme=lite` — exit 0, single 199 → 198, `--store` выполнен; `npm run ui:gates` — 234 теста, 1350 утверждений; `phpunit --filter everyTypeShowsItsDisplayMode` — 1 тест, 17 утверждений (проба `route_probe.php <dir> modes` до правки давала пустые режимы — тест падал); HTTP в браузере: `route_probe.php <dir> serve modes` + Playwright, news/docs/faq/files/media — список и вид на 375 и 1280 px: 20 страниц, все 200, горизонтальная прокрутка 0, ошибок консоли, страницы и запросов нет, аккордеон раскрывается кликом (отчёт storage/backup/s20.6-modes_2026-09-26.json); `npm run ui:after` — сдвинулись только node-list (docs стал оглавлением), node-view (docs без автора и просмотров), node-media (плеер над текстом) и прежний шум front/presentation (живые счётчики); вход рига прошёл; полный phpunit — 1518 тестов, 22592 утверждения, 7 пропусков, провалы только NodeProfileTest (9, прежняя причина S20.1); журналы стенда: error_php 744 → 761 только errno=13 и `session.log` во время съёмки (находка (д)), error_sql 370 без изменений. |
| S20.7 — Правила кода и консолидация | выполнен 2026-09-26 | Решения пользователя 2026-09-26 (05 — «Заметки реализации S20.7»): фабрики `getNodeReader()`/`getNodeWriter()` перенесены в core/system.php, писатель всегда с `$pnt`; письмо Node — одна `addNodeMail()`, адрес — `getPublicUrl()`; `getConst()` разрешает только определённые имена на `_`, `getNodeLabel()` удалена; стрелочные функции типизированы; 405 — двумя путями. Код: `getBlocks()` без `list()` и snake_case (`$qlang`, `$qargs`, `$where`, `$show`, `$fid`, мёртвый глобал `$b_id` снят), `render_blocks()` → `setBlockView(..., string $btitle, ..., int $bid, ...)`, `addSitemapTask()` без `list()`; 22 `(int)getVar(..., 'num', ...)` сняты (modules/node, admin/modules/scheduler.php), `(string)` над `homeurl`, `mtemp`, `_BLOCKPROBLEM` и NOT NULL-столбцами PDO сняты (service: `modules`, `ppost`, `type`, `created`, `modul` ×3; sync: `now`); 22 встроенных `new NodeQuery/NodeService` контекста запроса → фабрики, ставшие лишними `global $fld/$pnt/$db` и `use ($db, $fld)` сняты; 92 стрелочные функции на строках плана (d41badf4..дерево, кроме tests/) получили типы параметров и возврата. Проверки: `php -l` всех изменённых PHP — без ошибок; phpstan — No errors; `php-cs-fixer check` — 0 файлов; `npm run ui:gates` — 234 теста, 1350 утверждений OK, markup 0; полный phpunit — 1518 тестов, 22592 утверждения, 7 пропусков, 9 провалов только NodeProfileTest (прежняя причина S20.1, как при закрытии S20.6); поиск `list(` и `(int)getVar(` по строкам плана пуст, стрелочных функций без типов на строках плана — 0; `render_blocks`/`getNodeLabel` в PHP, шаблонах, JS и docs вне 14/PROGRESS не найдены; HTTP стенда (главная, docs, media, jokes, search, presentation, account, материалы docs 23 и media 25, sitemap.xml, RSS docs) — 200 без фаталов, error_php 761 и error_sql 370 не выросли. |
| S20.8 — Документы плана | выполнен 2026-09-26 | Все пять находок карточки живы на дереве окна и закрыты, код не менялся. Статус README — план выполнен; строки статуса 01–11 и 13 и заодно 12 — действующий контракт реализованного Node со ссылкой на PROGRESS; 13 «Структура тестов» дополнена NodeIntegTest (S16), NodeGuardTest (S19.1), NodeIntegrityTest (S19.3, S20.3), UpdateConfigTest, UpdateMailsTest, UpdateSetupTest с update_probe.php и режимами `secure`, `tree`, `modes` route_probe, абзацем о тестах Point, Rating, Field и ConfigFileTest по этапам; 13 — абзац «требования к будущей реализации» переписан; 02 «Файлы тестирования» — те же файлы; 05 — блок NodeContext с `bool $polls = false` и пункты о `polls` и `task`, сигнатуры `getNodeTarget()`/`getNodeTargetList()` с `bool $any = false` и пункт о флаге; 06 — метка обложки `_NODE_COVER` в обоих блоках и, сверкой всех меток блоков, `_SOURCE` примера формата поля → `_URL` (константы `_SOURCE` в lang нет, Field отклонил бы такую подпись); 04 — `profiles/<name>.json` в дереве модуля и пункт о каталоге. Проверки: скрипт контроля docs/node/*.md — 21 файл, UTF-8 без BOM, один корневой заголовок, парные блоки кода, относительные ссылки живы — 0 проблем; `git diff --check -- docs/node` — чисто. Сверка с кодом: context.php:29 (`polls`), query.php:1063 и 1100 (`any`), вызовы с `any` — service.php:1329, 1367, system.php:6055–6056; `_NODE_COVER` в lang/*.php:352 и трёх профилях; modules/node/profiles/ — десять JSON, читатели admin/index.php:139 (`addNodeProfiles()`) и modules/node/admin/index.php:662, 932. PHP-проверки не запускались — PHP не менялся. Самопроверка окна исправила две собственные неточности: 04 называл «экран профилей» — профили выбирает форма нового типа `op=type&profile=<name>`; 13 относил контракт Field к 05 и 12 без 06, где формат определений |

## Передача следующему окну

- План выполнен: S00–S20.8 закрыты, следующего этапа нет. S00–S19.8 закоммичены и запушены (1334a08d); S20.1–S20.8 не закоммичены — коммит только по команде пользователя (правила стейджа ниже: строка `send` config/node.php патчем, стендовые файлы не стейджить, секрет config/security.php обнулить). Удаление плана и перенос долговечного в постоянную справку — отдельный шаг по команде пользователя (решение 2026-09-24). Открытые записи ниже в план не входят и ждут отдельных задач пользователя.
- S20.8 закрыт 2026-09-26 (не закоммичен). Изменены только документы: docs/node/README.md, 01–13 (строки статуса; 12 — сверх карточки, та же устаревшая формулировка), 02 (таблица файлов тестирования), 04 (дерево модуля), 05 (NodeContext, цели), 06 (`_NODE_COVER`, `_URL`), 13 (структура тестов, абзац требований), 14-roadmap.md (вводная строка), PROGRESS.md. Контракт и код не менялись.
- S20.7 закрыт 2026-09-26 (не закоммичен). Изменены: core/system.php (`getBlocks()`, `setBlockView()` вместо `render_blocks()`, `addSitemapTask()`, `getConst()`, `getNodeReader()`/`getNodeWriter()`/`addNodeMail()` рядом с `getNodeHandler()`, фабрики в `getNodeTypeMap()`, `getNodeTitleMap()`, `getRatingService()`), modules/node/index.php (фабрики и `getNodeLabel()` удалены, приведения, типы), modules/node/admin/index.php (`setNodeResultMail()` через `addNodeMail()`, приведения, типы, переносы строк), core/classes/node/ext/support.php (`addSupportMail()` через `addNodeMail()`), core/classes/node/{query,service,view}.php и ext/sync.php (приведения, типы), blocks/node.php, core/admin.php, core/user.php, core/helpers.php, core/security.php, core/classes/{cache,comment,feed,field,parser,point}.php, admin/modules/{categories,fields,monitor,ratings,scheduler}.php, modules/{presentation,search,voting/admin,search/admin,shop/admin,sitemap/admin}/index.php, templates/lite/index.php, setup/index.php, tools/node-profile.php (типы стрелочных функций, фабрики), tests/Unit/NodeServiceTest.php (ожидаемая строка `getCategoryModules()` с типами), docs/node/05 («Заметки реализации S20.7»), 02 и 05 (`setBlockView()`). Изменение контракта: `render_blocks()` → `setBlockView()` с `int $bid`; `getConst()` больше не разрешает константы без ведущего `_` (все языковые константы начинаются с `_`, проверено по lang/, admin/lang, modules/*/lang); `getNodeReader()`/`getNodeWriter()` доступны везде из ядра; новая `addNodeMail()`. Схема БД, lang и шаблоны не менялись.
- Вне приёмки S20.7: tests/ не типизировались (карточка называла код плана), в них остаются нетипизированные стрелочные функции; `list()` вне функций плана в core/system.php (прежние циклы 2474, 4225, 4472, 5399, 5694, 5720) и прежние строки длиннее 180 (core/classes/parser.php:593, core/system.php:2902, 2959, modules/presentation/index.php:635) не трогались; OauthTest `jwtRejectsInvalidTokens` (nbf/iat future) провалился в прогоне, который шёл дольше пяти минут: провайдер считает «будущее» как `now + 300` в начале прогона — хрупкость теста, в повторных прогонах зелёный; `new NodeQuery/NodeService` остались внутри классов и в фоновых контекстах (планировщик `addNodePublishTask()`, sitemap, первый администратор admin/index.php) по решению; кандидат передачи S20.6 — три одинаковых мемоизатора пробы в tests/Unit/NodeRouteTest.php (`getSecure()`, `getTree()`, `getModes()`) — пользователю не задавался и не объединялся: тесты вне кода плана карточки.
- S20.6 закрыт 2026-09-26 (не закоммичен). Изменены: modules/node/index.php (`getNodeCover()` с ролью `poster`, новый `getNodeDownload()`, ключи `download_label`/`hits_label` в `getNodeViewVars()`, `download` карточки списка), templates/lite/fragments/node/{docs,faq,files,media}/card.html (новые), templates/lite/partials/node/{docs,faq,files}/view.html и media/{list,view}.html (новые), templates/lite/assets/css/theme.css (блок «Node display modes» после дерева документов), tools/ui-contract.php (группа дублей с `.sl-node-file .sl-meta-foot`), tools/ui-audit-baseline.json (`--store`), tests/Support/route_probe.php (режим `modes`, `serve modes`, `addRouteModeTypes()`, `getRouteModeBits()`, `getRouteModeOrder()`, `getRouteModes()`), tests/Support/route_web.php (css/js/шрифты/картинки из templates/ и plugins/ отдаются встроенным сервером — иначе браузер на пробе видел страницу без стилей), tests/Unit/NodeRouteTest.php (`getModes()`, тест `everyTypeShowsItsDisplayMode`), docs/node/08 (состав режимов, классы, ключи). Изменение контракта: у карточки списка новый ключ `download`, у всех шаблонов материала — `download_label`, `hits_label`; обложка карточки и `og:image` полного материала предпочитают роль `poster` для любого типа. lang, схема БД и тема admin не менялись.
- Вне приёмки S20.6: docs/view.html и faq/view.html сейчас совпадают (оба — базовый вид без чипов автора и просмотров, как выбрал пользователь); длительность в плитке media не выводится — `duration` ресурса при загрузке не заполняется, чип показывает год из поля `year` или дату; на стенде нет типов news, faq и files — все пять режимов проверены на одноразовой БД пробы, риг снял стендовые docs и media 23 и 25.
- S20.5 закрыт 2026-09-26 (не закоммичен). Изменены: core/classes/node/service.php (`addNodePoint()` с `$self`, `moderate` в `updateNodeStatus()` и `deleteNodeAssetReport()`), core/classes/comment.php (`addModerPoint()`, `getPointScope()` — scope событий комментария в одном месте, решение пользователя 2026-09-26 при самопроверке, `updateNodeAction()` с id комментария, `setStatus()`), core/classes/node/view.php (`LIGHT = ['card']`), core/classes/node/query.php (`TREEPART`), core/system.php (партии sitemap `syncbatch`), modules/node/index.php (`is_views = false` связанных карточек, `NodeQuery::TREEPART` в `getNodeTreeData()`), templates/lite/partials/node/support/view.html, templates/lite/fragments/node/search.html (удалён, в индекс не добавлялся), tests/Support/node_probe.php (`moderate` в `getProbeMatPoint()`, одобрение в `getProbeMatStatus()`, жалобы в `getProbeMatAssetOps()`), tests/Support/route_probe.php (лёгкие режимы, `moderate` сценария support, `render` и партии sitemap в integ), tests/Unit/NodeServiceTest.php, NodeRouteTest.php, NodeIntegTest.php, NodeSupportTest.php, docs/node/points.md (карта владельцев, заметки S20.5), 02, 05, 06, 08, 09, 13, 14 (критерий S20.5). Изменение контракта: `NodeView` у `NodeTarget` принимает только `card`; фрагмента `fragments/node/search.html` в теме нет; sitemap читает партиями `limits.syncbatch`; новые события Point `moderate` с `aid`. Шаблон admin, lang и схема БД не менялись.
- Вне приёмки S20.5, не исправлялось: `view.mode` в конструкторе типа — свободное поле, режим `support` может выбрать тип без расширения (теперь его функции видны, но карточка состояния — нет); `getNodeTargetList()` связанных карточек режет `array_slice(..., 500)`, а пакет принимает до `min(500, syncbatch)` — при `syncbatch` меньше числа связей материал откажет, хотя 05 ограничивает `rels` тем же `syncbatch` при записи. Страницы рига, требующие входа на сайт, в after S20.5 не сверены: вход рига сорвался дважды (находки (е), (д)).
- S20.4 закрыт 2026-09-26 (не закоммичен). Изменены: modules/node/index.php (`getNodeTreeData()`, ключ `tree` в `$live` `setNodeView()`, `tree_html` и комментарий `getNodeViewHtml()`), templates/lite/fragments/node/tree.html (новый), templates/lite/partials/node/view.html (`{{{ tree_html }}}` перед связанными), templates/lite/assets/css/theme.css (`.sl-node-tree`, `.sl-node-tree .sl-sublist`, `.sl-node-tree-cur`, `.sl-node-tree-more`, `.sl-node-tree-pager`), setup/sql/table.sql и setup/sql/table_update6_3.sql (KEY `tree`), tools/node-profile.php (сценарий «view docs tree», бюджет со слагаемым `extra`), tests/Support/route_probe.php (режим `tree`, дочерний `treeext`, `addRouteTreeTypes()`, `getRouteTreeNav()`, `getRouteTree()`, `getRouteTreeData()`), tests/Unit/NodeRouteTest.php (`getTree()`, три теста S20.4, `tree` в списке фрагментов), tests/Unit/NodeModelTest.php (индекс `tree`), docs/node/03 (строка индекса), 05 («Заметки реализации S20.4»), 08 (`tree.html`, девять фрагментов), 10 (ссылка на заметки), 11 (бюджет полного материала с деревом). Изменение контракта: новая схема `_nodes` — индекс `tree`; бюджет полного материала типа с деревом — плюс `floor(N/500) + 1` партий и предвыборка прав категорий. Стенд: `ALTER TABLE sport_nodes ADD KEY tree (tid, status, id)` выполнен. lang не менялся: подписи — `_CONTENT`, `_BACK`, `_NEXT`.
- Вне приёмки S20.4, не исправлялось: `_BACK` в lang/pl.php и lang/uk.php переведён как «тому назад» (`Temu`, `Тому`), а используется как «назад» пейджером (core/helpers.php:176) и кнопками админки (core/admin.php:1383, 1485) — теперь и подписью предыдущего документа; пользователю отдельной задачей. Template (core/classes/template.php:169) молча не передаёт ключи шаблона `data`, `file`, `path`, `scope` (`extract` с `EXTR_SKIP`) — ключ трассы назван `trail`. Журнал error_php.log стенда вырос с 701 до 725 строк только во время съёмки: `ui:before` (00:47–00:55, до правок шаблонов) и старт `ui:after` (01:02, сброс кешей при параллельном входе) — `include(): errno=13` скомпилированных шаблонов account, admin и главной, известная находка (д), плюс `file_get_contents` мониторинга; запросы Node и прогоны тестов строк не добавили; error_sql.log — 370 без изменений.
- S20.3 закрыт 2026-09-25 (не закоммичен). Изменены: core/classes/comment.php, core/classes/node/service.php, core/classes/node/ext/sync.php, admin/modules/categories.php, docs/node/05-core-api.md («Заметки реализации S20.3»), docs/node/02-naming.md (`checkTypeRegistry`), tests/Support/node_probe.php (класс `ProbeSaveDb`, дочерний режим `mcomm`, `getProbeMatAward()`, `getProbeMatComm()`, `getProbeLines()`, `getProbeMatPoint(..., ?Database)`, подкатегории и повреждённый тип в `getProbeMatCategories()`), tests/Support/route_probe.php (`undo` в syncext, `broken` в intact), tests/Unit/NodeServiceTest.php, tests/Unit/NodeIntegrityTest.php, tests/Unit/NodeSyncTest.php. Изменение контракта: новый публичный `NodeService::checkTypeRegistry()`; `deleteNode()` отказывает `STORAGE` «The award of the material cannot be compensated», когда Point не подтвердил компенсацию; премодерируемый комментарий материала Node теперь блокирует материал; экран категорий на сбое чтения реестра отказывает `_ERROR_UP`, а не уходит в сырой SQL. Шаблоны и lang не менялись.
- Вне приёмки S20.3, не исправлялось: прочие владельцы компенсаций принимают `false` из `getEventId()` за «награды не было» и отбрасывают `false` обратного `addEvent()`, сверено 2026-09-25: modules/forum/index.php:945–947, modules/order/admin/index.php:166–167 и 189–190, modules/shop/admin/index.php:205–206, 394, 421 — карточка называла только Node и Comment, пользователю отдельной задачей; `deleteTarget()` при сбое не пишет свою строку журнала (её пишет Point, владелец получает `false`).
- S20.2 закрыт 2026-09-25 (не закоммичен). Изменены: setup/index.php (`setUpdateModules()`, `checkUpdateBase(..., bool $fresh)`, `save()`, `config()`, подключение logger.php), core/classes/pdo.php (отказ подключения в установщике), шесть setup/lang (`_CONF_3_INFO`, `_SETUPPREFIX`, `_SETUPAFILE`), UPGRADING.md, docs/node/12-migration.md («Заметки реализации S20.2»), tests/Support/update_probe.php (режим `setup`, `_admins` в схеме), tests/Support/install_probe.php (`getInstallRefuse()`), tests/Unit/UpdateSetupTest.php (новый), tests/Unit/NodeProfileTest.php (тест отказов). Изменение контракта: `checkUpdateBase()` получил третий параметр; установщик отказывает префиксу вне `[A-Za-z0-9_]{1,32}` и имени админки вне `[a-zA-Z0-9_-]` — сайт 6.2 с таким префиксом обновление не пройдёт. Живой стенд не трогался: setup.php стенда заперт, все сценарии — на копии install_probe и одноразовой схеме. Остаток S20.2: повторный `update6_3` после того, как администратор 6.3 поменял модули, снова применит старую таблицу `_modules` (таблица не удаляется обновлением, по 12 она вне миграции) — решение пользователя 2026-09-25: оставить как риск, не исправлять.
- S20.1 закрыт 2026-09-25 (не закоммичен). Изменены: core/classes/node/exception.php (`LIMITED = 6`), core/classes/node/query.php (`send` в `getNodeLimits()`, `checkNodeCategory()`), core/classes/node/service.php (`checkNodeWait()` в `addNode()`), modules/node/index.php (`getNodeFault()`/`getNodeStatus()`, `getNodeAssetPost()` с `$upload`, `getNodeFormPost(..., bool $upload = true)`, капча `setNodeForm()`, 404 категории `setNodeList()`), modules/node/admin/index.php (`send` в `config()`, 0 допустим), `_NODE_SEND` в шести modules/node/admin/lang, modules/search/index.php (`getSearchLine()`), config/node.php (`limits.send => 60`), tests/Support/route_probe.php (режим `secure`, `send = 0` в общем конфиге пробы, категория и `send` в прежних сценариях), tests/Support/node_probe.php (`send = 0` в обоих конфигах пробы), tests/Support/install_probe.php (материалы типов стареют на сутки перед сценарием поддержки), tests/Unit/NodeRouteTest.php (четыре теста S20.1, категория и `send`), tests/Unit/NodeModelTest.php (шесть кодов), tests/Unit/NodeQueryTest.php (`checkNodeCategory`), docs 05, 06, 11. Шаблоны не менялись: слот `captcha` в lite `form-add` уже был.
- Коммит S20.1: config/node.php на стенде несёт и строку `'send' => 60`, и стендовые типы — в коммит идёт только строка `send` (патч через `git apply --cached`), стендовые типы не стейджить; до этого коммита NodeProfileTest красный (см. строку S20.1). config/local.php стенда удалён после правки config/node.php.
- Не проверено на живом стенде: у стендовых типов с `submit` (jokes, media) `workflow.access` — `user`, гость получает 403, капча на стенде выключена; все сценарии S20.1 прогнаны реальным HTTP пробы `secure` (сервер `php -S`, одноразовая БД). Журналы стенда error_php/error_sql после запросов не выросли (701/370 строк).
- Второй аудит реализации (2026-09-25, HEAD 1334a08d): шесть срезов, находки сверены с кодом и разнесены по S20.1–S20.8 в 14-roadmap.md. S00–S19.8 закоммичены и запушены (S00–S18 — 70224f65, план S19 — f9ecac63, S19.1–S19.8 — 1334a08d). Решение пользователя 2026-09-25: находки — этапом S20 этого плана; дерево документов, `moderate` и режимы представления — реализовать. Проверки на момент аудита: `php -l` всех PHP в git — без ошибок; phpstan — без ошибок; `php-cs-fixer check` — 0; `npm run ui:gates` — 234 теста; полный phpunit — 1501 тест, 22529 утверждений, 7 пропусков, без провалов. Код аудитом не менялся.
- Судьба плана (решение пользователя 2026-09-24): docs/node/ остаётся в дереве; удаление плана и перенос долговечного в постоянную справку — отдельный шаг по команде пользователя. docs/PAGE-CACHE-ROUTES-2026.md можно переписывать под маршруты Node (NOD-231).
- Изменения контракта S19.8: `getUserInfo(): array` (гость и удалённая учётная запись — `[]`); `filterText(string $message, int $type = 0)` без массива, поле `aemail` формы первого администратора читается через `getVar()`; удалены `filterFields()`, `getTplRefreshTimeSelect()`, `getAsyncPager()`, `getTranslit()`, `url_types()`; константы каталогов core/system.php не переопределяют заданные раньше.
- Остаток правил, сознательно вне приёмки: переводы в lang-файлах длиннее 180 (admin/lang `_NODE_SETUP`, `_NODE_START_INTRO`, `_NODE_START_BODY` и строка 218, modules/node/admin/lang de/fr, setup/lang) — переводная строка `define()` одной строкой, как весь прежний массив lang-файлов, карточка их не считала; длинные строки тестов, которых не было в диффе плана (contract_probe, EditorWindowTest, UploadIntegrationTest, ParserFixturesTest), и прежние комментарии в телах функций (setup/index.php `save()`, SchemaUpdateValidationTest) не трогались; `intval()` над целыми столбцами PDO оставлен — правило называет только приведения.
- Прежние находки вне Node, сверенные с кодом 2026-09-25 (живые — пользователю отдельной задачей, в S19.8 не исправлялись): (а) форма auto_links (и форма блокировки account `name_b`) шлёт поле `name`, которое перекрывает `name` модуля в `getVar('req')` — modules/auto_links/admin/index.php:263; (б) shop `op=clients` — `getTplPager()` без `table`/`where`, отсюда `COUNT(id) FROM sport`, плюс лишние ключи `count`/`pages`; неоднозначный `status` списка товаров (:469); массовое действие пишет `acomm = c<id>` (:526, :733); `checkSiteToken()` без POST в обработчиках shop и order; (в) касса читает cookie `shop` (modules/shop/index.php:338), корзина пишется в `<user_c>-shop`, очистка через `setcookie`; (д) core/classes/template.php:145 пишет скомпилированный шаблон прямо в конечный файл с `LOCK_EX`, параллельный `include` (:172) получает errno=13 — нужна запись через временный файл и `rename()`; (е) tools/ui-shots.mjs входит двумя режимами параллельно без повтора; (ж) `--before --only` стирает весь каталог пары; (з) config/fields.php — демо-поле account `field3` с `req => true` блокирует сохранение профиля; (ц) стенд: `var_view => 1` и флаги COOKIE/SESSION в config/global.php, `getVariables()` выводит эти секции без экранирования. Не живы на стенде: (г) и (и) — при `db.sync = 1` сессия БД получает пояс PHP, отставание возвращается только при `sync = 0`.
- Прежние находки S19.1–S19.6 (не исправлялись): S19.6 — `getNodeCount()` без категории читает все опубликованные строки типа; пейджер админки не показывает общее число. S19.5 — plugins/system/slaed.js:850 разбирает ввод через `innerHTML`; `getSearchSnippet()` оставляет текст `<script>` из `[usehtml]`. S19.4 — `Comment::updateBody()` пишет автокоммитом без guard; отложенный пересчёт `comments` shop/voting идёт после ответа вне guard; ранний bump pdo.php в админке подавляет финальный `addEpoch()`. S19.3 — повторная форма account с тем же `pkey` и другой суммой даёт пустой успех; типы jokes, content, media показывают комментарии старых модулей с теми же id; остатки faq, files, help, links, news, pages на стенде — удалять только по команде пользователя. S19.2 — поставочный config/security.php в HEAD несёт тестовые баны стенда; `_privat` вне списка InnoDB preflight; язык сайта при обновлении берётся из cookie установщика. S19.7 — числа категорий Node через HTTP не увидены (на стенде нет материалов с категорией); install_probe (update) до коммита ждёт приоритет `maildrain` 2 из HEAD.
- Прочие открытые записи: tools/label-audit-baseline.json не перезаписан; полный phpunit пишет строки почтовых тестов в storage/logs/error_site.log стенда.
- Стенд: БД — колонка `_comment.shown` добавлена в S19.5; в `sport_points` у пользователя 7885 две пары «publish +10 / reverse −10» (id 848–851) от приёмки S19.7. Не стейджить стендовые отличия: config/node.php, config/uploads.php, config/ratings.php, config/fields.php, config/security.php (секрет), uploads/docs/, uploads/jokes/, uploads/content/.htaccess, uploads/media/.htaccess.
- Артефакты вне git: storage/backup/s18-update_2026-09-24.json, s18-profile_2026-09-24.json, s19.2-update_2026-09-25.json, s19.3-update_2026-09-25.json, s19.6-profile_2026-09-25.json, s19.7-accept_2026-09-25.json, s20.2-update_2026-09-25.json, s20.4-profile_2026-09-26.json (профиль S20.4 на копии инструмента с `limits.send`); скрипты c:/tmp/s193-types.js, s196-journal.js, s197-accept.js, s197-help.php и снимок c:/tmp/s193-types.png.

Для завершённого этапа в таблице указываются команды и результат либо ссылка на сохранённый отчёт/тестовый артефакт. Если коммит уже создан по указанию пользователя, можно указать hash; создание коммита не является условием передачи. Незавершённое записывается явно, без статуса «готово».
